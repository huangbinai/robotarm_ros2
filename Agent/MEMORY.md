# reBotArm Agent 实时记忆

> 本文件只保存当前仍有效的事实、决策、阻塞和交接信息。已清理的旧工程记忆和操作文档可从 Git history 追溯。

## 当前焦点

- Active phase / 当前阶段：2026-09-13 一次性基线迁移与软件验证完成，等待后续开发范围；历史 P0-P6 记录不代表本机验收，不创建 P7。
- Completed phase / 已完成阶段：P0 机械臂安全启动与执行门控、P1 MuJoCo 基线巩固与差距整合、P2 Gemini 2 SDK 与真实 RGB-D 验收、P3 完整单 Ubuntu 视觉链路、P4 Ubuntu 本地 GraspNet、P5 hand-eye/nominal TCP/MuJoCo 标定检查，以及 P6 当前工程范围均已关闭。
- Hardware gate / 硬件门：当前没有新的硬件动作授权。P6 关闭不授权 approach、gripper、lift、retreat、自动抓取或其他后续规划中的动作。

## 当前事实

- 2026-09-13：本机新路径 `/home/huangbin/robotarm_ros2` 从源 main `aa1dcc52c7e7046f8040c2b9becd767df4d07265` 全新检出，初始工作树干净。旧目录整体移动到 `/home/huangbin/robotarm_ros2_backup_20260913_132122`，包括 .git、5 个未提交修改和全部生成文件；未恢复旧业务代码。个人远程旧 main `f2b0095a0da959b56e10140b292295b9ad9b8269` 已保存并核验为 `backup-before-rebase-20260913-132122`，通过绑定该 SHA 的 force-with-lease 替换 main。现在唯一 remote 是个人 `origin=https://github.com/huangbinai/robotarm_ros2.git`，无源仓库 remote、partial-fetch 配置或旧目录 alternates。新 13 包构建成功；系统 pytest 811 passed/7 skipped，MuJoCo pytest 817 passed/1 skipped，视觉 wrapper 15 passed，XML 去重覆盖 818 个用例。分层/资源 26、compileall、三类 launch 参数解析和 MuJoCo EGL health 通过。新 vision/GraspNet/MuJoCo venv 安装及 pip check 通过，MotorBridge 新编译为 0.4.6+rebotarm.2、feedback_sequence=true。Pinocchio 4.1.0 从新下载 ROS deb 隔离解包；日常 source `tools/source_local_environment.bash`，不 source 备份。colcon test 对 12 Python 包报无包内测试（根 tests 才是实际回归），系统 rosdep 仍不识别本地解包为 apt 安装。GraspNet 模型/原生扩展和专属 engine 未迁回；没有硬件、相机、运动、使能或校零。本轮证据及 A/B/C 差异分类见 `docs/LOCAL_PROJECT_DIFF.md`，原始日志在 `log/migration-*`。下一步先独立完善 CI/测试接线，再人工评审旧功能是否迁移。

- 2026-09-06 用户要求同步安装流程与README：视觉setup改为仅打包构建时存在的默认模型，缺少PT/engine不再阻塞源码构建；启用检测时的模型路径校验保留，不静默回退。视觉依赖脚本仅安装/检查环境，不再以venv构建ROS包，PyTorch/torchvision固定到当前已安装的2.11.0+cu128/0.26.0+cu128；启动脚本通过REBOTARM_VISION_PYTHON按进程选择解释器，不激活venv、不注入全局视觉PYTHONPATH。主README、SDK说明、安装/视觉/解释器/仿真文档统一为系统Python构建、分环境运行，SDK固定版本、PT显式启动、可选外部engine、GraspNet --prefix和当前P0-P6范围状态同步；基础驱动/RViz-only预览/MuJoCo入口明确区分。TDD先复现4项旧契约失败，新增无模型/仅PT/两模型打包与文档一致性回归。无PT、无engine、无旧install的暂存源码副本在/tmp/rebot-install-audit-v5xUJa完成13包独立构建（25.2s）；仅接入已有厂商SDK后，用新install全量811 passed/7 skipped，主目录相同，分层20、compileall/bash/diff检查通过；安装后3类launch --show-args通过，无节点执行。只使用本机已有系统依赖，没有全新系统网络安装/engine导出/推理或真机验收；未改主目录build/install、现有venv、模型或其他开发工作树。准备普通提交同步main。
- 2026-09-06 用户批准仓库历史材料整理：112个文件退出Git跟踪并保留本机，包含11份P1证据、主项目11份及Star Arm 6份历史计划/规格、P1 gap matrix、22文件旧仿真实现归档、57文件上游比较快照、未引用的yolo11n-seg.pt，以及3份只校验历史材料的测试。混合测试test_simulation_source_swap.py仅移除2个归档断言，保留正式入口/模型与无双后端检查；当前有效软件回归及Agent状态文件保留。所有退出跟踪文件与清理前HEAD逐字节一致且被忽略。现行文档入口已同步，过期双后端命令和PROVISIONAL授权说明更正，来源/固定commit/授权说明保留。yolo26s-seg.pt仍被视觉安装流程依赖，本次不撤下；不改运行代码、不访问硬件。第一次发布副本检查暴露7项本机历史计划校验（已随对应测试撤下）及4项缺少SDK/install环境；最终只补充既有SDK和install环境、不补历史材料，发布副本全量807 passed/7 skipped（含分层20），compileall和diff检查通过。准备普通提交同步main，其他分支及开发工作树不变；历史仍可从清理前f060d68追溯，模型分发流程留待单独整理。
- 2026-09-06 按用户要求将历史 P1 上游对比工具 `scripts/p1_upstream_mujoco/` 的3个脚本、2026-08-07 upstream-full-localization 的 plan/spec 两份历史文档及两份专属测试撤出 Git 跟踪；7个文件全部保留本机，逐字节对比撤下前 HEAD 一致，并加入精确忽略规则。README 不再列出已无发布文件的 scripts 目录；正式源码无相关导入。旧活动记录中的提交路径仅作历史追溯，不代表当前文件仍在发布树。验证剩余发布范围（显式忽略本机保留的两份旧测试）为819 passed/7 skipped，diff检查通过。本次通过普通提交同步main，不改写历史、不回退其他整合成果、不改开发工作树或操作硬件；远端同步以提交后的核验为准。
- 2026-09-06 GitHub发布完成：J3同步提交e24cc52、文档/首页/忽略规则整理提交d3259cc；正常合并远端main的Star Arm工具后推送开发分支afb3f9b，经现有PR #1合入main，合并提交359d9f1。GitHub默认分支仍为main，仓库简介已补全，现有三个开发/备份分支均保留；本地主目录已切换main并快进同步，未force-push或删除历史。主项目合并后827 passed/8 skipped，compileall/diff通过；合并后的代码树与已测发布头一致。旧规划/手册从当前树移除但可历史恢复，既有模型/上游参考资产保留，本轮未新增模型、engine、日志、构建产物或本地实验数据。此发布记录随后单独提交同步；不构成真机验收，未操作硬件。
- 2026-09-06 用户要求提交并整理GitHub，随后明确选择将整合版本同步到main且保留现有开发分支。发布准备包含已验证的J3限位同步、两份规划和六份旧手册清理、标准build/install/log约定、首页及13包目录说明；新增本地模型/engine/运行数据/额外实验记录/外部SDK忽略规则，仅影响未跟踪文件，现有模型和来源快照不移除、不重写历史。远端main含独立Star Arm工具提交bab08dc，必须正常合并保留，不得用旧本地main覆盖。发布前主项目827 passed/8 skipped、compileall/diff通过；模型、环境和实验数据仍在本机，未操作硬件。最终远端同步结果以本轮后续活动记录为准。
- 2026-09-06 按用户要求清理六份根目录旧文档：历史工程记忆、交互状态快照、交互验收、旧实机预检、旧USAGE及MoveIt使用说明；全部已有Git历史可恢复。MoveIt仍有效的规划/执行/反馈检查及受控停机说明合并到 `docs/rebotarm_feature_commands.md`，移除旧节点依赖和过时P0禁用提示，保留本次动作授权、唯一串口、基线确认后失能及健康回位失败保持原则。AGENTS和Agent文档引用已同步。五份requirements分别服务控制器bootstrap、视觉、GraspNet、MuJoCo、TensorRT独立安装步骤，因环境版本与安装参数不同予以保留；用途表见 `docs/local_setup_zh.md`，未更改依赖文件或安装环境。全量827 passed/8 skipped，当前文档/代码无已删文件引用，diff检查通过；未操作硬件、未提交推送，也未改另一任务的J3实现。
- 2026-09-06 用户明确要求J3按J2方式增加0.02rad上端余量，当前软件范围同步为 `[-3.14,+0.02] rad`：Star SDK mapping、主项目硬件检查、MoveIt URDF及由其生成的MuJoCo模型/分析profile均已更新。该范围同时接受目标和反馈，不改零位、不加减反馈偏移；`0.020001 rad`仍拒绝，其余轴和夹爪不变。TDD先复现SDK四项、硬件三项正值被拒，再做最小数值修复；SDK449 passed，主项目827 passed/8 skipped，分层20、编译/diff和MJCF生成一致性通过。controller/moveit_config/simulation三包重建，并回读installed J3与URDF上限均为0.02。本轮未访问串口、未使能或发送运动命令，未提交推送，实体复测待用户执行。
- 2026-09-06 主目录整合前版本已提交为 `cea5528`（681 passed、15 skipped），反馈与解耦工作树已提交为 `6fc58e8`（822 passed、8 skipped），随后启动正式Git合并。此前125项试迁入已先按SHA256撤回并保留 `/tmp/rebot-integration-20260906-tx1xx4k7` 备份。当前仅做软件整合与构建，整合后主目录的实机验收仍待用户执行；不推送、不操作硬件。来源工作树的P6范围收口记录同步保留，不将退出范围的项目改写为通过。
- 2026-09-06 用户确认本工作树遥操作测试正常，要求先分别提交两个目录，再整合到主目录。本工作树当前全量测试822 passed、8 skipped（加载ROS及install_coupling_audit消息包，禁用pytest自动插件）；此结果不代表整合后的主目录已完成实机验收。仅提交源码、测试、文档和版本管理资源，保留运行证据、构建目录、模型权重及本机链接；不推送、不操作硬件。
- 2026-09-06 operator 授权的前两项耦合维护已完成，P6 保持关闭，不新建阶段。活动仿真包移除未启用的 `mujoco_ros_adapter_node.py`、`upstream_backend.py` 及专属旧执行循环测试，去掉 `rebotarm_motion` 清单依赖；正式 `mujoco_ros_node.py` 保持原实现，公共离线分析模块和历史归档保留。8 个 launch 改为每类进程独立解释器：launch 参数优先，其次 `REBOTARM_MUJOCO_PYTHON` / `REBOTARM_VISION_PYTHON` / `GRASPNET_PYTHON`，最后 PATH `python3`；取消 launch 的工作区 venv 搜索及混合 launch 的全组视觉 PYTHONPATH 注入。新构建为工作树 `install_decoupling` 三包 overlay，原 `install` 仍链接主目录旧构建；使用方式见 `docs/launch_python_configuration.md`。为既有 vision setup 资源要求补建 `tools/yolo26m-seg-fp16-b1-640-linux.engine` 到主目录同名模型的本地符号链接，模型未修改，链接不纳入源码提交。全量 `761 passed, 8 skipped`、分层20、正式 MuJoCo 后端55、三包构建、8个已安装launch参数解析和compileall/diff检查通过；未启动硬件或发送动作。

- 2026-09-06 operator 明确决定关闭当前 P6，后续另行加入新的规划。关闭方式是 scope closeout / 范围收口，不把未执行项伪装成通过：`yolo26s-seg.pt` bottle→active MuJoCo 正向复验，以及分级实机验收中尚未完成的 lift/retreat 等项目退出当前 P6 计分范围，历史结果和风险边界全部保留；当前 P6 由 `4/6` 收口为 `4/4`。在 operator 授权的测试中，独立 worktree `/home/a/project/rebot_Arm-worktrees/gripper-unified-bus-fix`、分支 `codex/gripper-unified-bus-fix` 启动真实 `/dev/ttyACM0` controller 和 Ubuntu-native Gemini 2/TensorRT YOLO/in-process GraspNet/MoveIt，显式设置 `execution_mode=plan_only`、`start_visual_grasp_executor=false`、`start_visual_ready=false`。捕获到 fresh valid bottle plan：confidence `0.9310057`、jaw width `0.0749830 m`、frame `base_link`、capture age `2.946 s`；MoveIt `execute=false` 对 pregrasp/grasp 分别成功生成 `55` 点/`5.33271924 s` 与 `82` 点/`8.086942298 s` 轨迹。调用前后 arm 始终 `enabled=false/control_loop_active=false/IDLE`、六轴 status 全 0、无 error；gripper status0，position 均为 `0.0013012104 rad`。未启动 VisualGraspExecutor、未 enable、未发送 trajectory 或夹爪命令。测试结束后全部项目进程、ROS daemon、8081/8088 listener 和串口占用均已清理。
- 2026-09-04 operator 决定先在独立目录用 Python SDK 验证 Star Arm 102-LD Leader -> 当前 reBot B601-DM Follower 的主从方向映射，ROS 2 集成后置；从臂坐标定义为唯一权威。只读现场确认 `/dev/ttyUSB0` 为无人占用的 CH340/FashionStar Leader，`/dev/ttyACM0` 原由 `rebotarm_app.launch.py` 的 `reBotArmController` 占用；停止前从臂为 `enabled=false`、`IDLE`、六轴 `status_code=0`。经 operator 明确授权，SIGINT 正常退出 controller 并释放串口，残留 launch 父进程也已退出。独立 checkout 为 `/home/a/project/Star-Arm-102-sdk-test`，上游 `main@5979b346eb3a417840b29b76740754e4005d071a`，设计分支 `codex/python-sdk-mapping-audit`。FashionStar monitor-only query 对 ID 0-6 各取得 12/12 样本；ROS 停止后的纯 Python `motorbridge` feedback-only query 对 B601 六轴与夹爪各取得 20/20 样本且全部 `status_code=0`。两次探测均未 enable/disable/set-zero/reset-multiturn/change-mode 或发送位置、速度、力矩、轨迹命令。首里程碑严格限定为只读方向标定器，候选 J1-J6 sign 为 `[-1,-1,+1,+1,+1,-1]`，相对本轮双方 baseline 映射；夹爪比例另行标定，live follow 不在本里程碑授权内。设计文档已在独立 checkout 提交为 `ac4c5a1`，等待 operator 复核后才能进入实施计划与代码。
- 2026-09-02 operator 确认抓取候选全链路优化阶段已完成，下一步安排一次受保护的单瓶夹取测试。此决定只切换工作重点，不自动授权真机动作，也不把尚未执行的 lift、retreat 或无力反馈的抓取结果分类记为完成。当前测试入口仍为 `tools/p6_single_bottle_grasp_runner.py`，范围限定为 fresh valid bottle plan 后的 `45 s pregrasp -> 45 s approach -> force-controlled close -> 20 s hold -> release -> 45 s return baseline`；运行前仍需核对当前夹爪安全开度、目标材质、整机 disabled/status0、现场净空与参数，并以 `--confirm REAL_SINGLE_BOTTLE_GRASP` 显式确认。当前记录没有显示换齿条后的多点 `angle -> jaw` 行程映射已完成，因此旧执行参数 `--gripper-open-m 0.080` 与 `--grasp-close-force 1.0` 不得原样复用；闭合零位反馈恢复为 0 只证明 offset，不证明行程比例。现场复核时仅残留 Codex 上轮诊断启动的独立 candidate node；已由 Codex Ctrl-C 清理，8081、controller、viewer 均未运行，`/dev/ttyACM0` 无占用。
- 2026-09-02 Ubuntu Open3D 查看器“只有点云、看不到夹爪”已修复：根因是隔离 `.venv-graspnet` 导入 `graspnetAPI` 时缺少 `trimesh`，旧 renderer 静默退化为细 LineSet。`Open3DGraspVisualizer` 现保留官方 geometry 优先路径，但在其不可用时直接用 Open3D `TriangleMesh.create_box()` 生成掌部 + 双指实体，按候选 rotation/translation 变换并以 score 着色；启动明确打印 `gripper_renderer=native_open3d_mesh` 及 API 不可用原因。真实 Gemini 2 + localhost GraspNet smoke 中完整点云约 `113k-116k`、raw `10`/displayed `3`，绿色实体夹爪清晰可见；临时 vision/service/candidate/viewer 均 Ctrl-C clean，未启动 MoveIt/controller/机械臂。源码与回归测试已以提交 `5555946` 推送到 `origin/codex/mujoco-sim-landing`，本地和远端均指向同一提交。证据：`Agent/evidence/P6/2026-09-02-ubuntu-open3d-native-gripper-mesh.md`。
- 2026-09-01 Windows bridge 的 Open3D 原始抓取可视化已同步到 Ubuntu 完整场景查看器：`tools/view_graspnet_scene_cloud.py` 同窗显示 GraspNet 采样前 `XYZ+RGB` 点云和 `/grasp/graspnet_candidates` Top-N 原始相机坐标位姿，复用并扩展 `Open3DGraspVisualizer` 直接显示 `build_scene_cloud()` 的 points/colors；候选、RGB、depth、CameraInfo 按 `100 ms` 门限匹配，并 fail closed 检查 depth/CameraInfo/candidate frame 与尺寸。相关单元测试 `38 passed`；真实 Gemini 2 + localhost GraspNet 只读 smoke 连续显示约 `113k-125k` 场景点和 `1-10` 个候选，未启动 MoveIt、控制器或机械臂动作。证据：`Agent/evidence/P6/2026-09-01-ubuntu-open3d-raw-grasp-overlay.md`。
- 当前分支：`codex/mujoco-sim-landing`。
- 2026-09-01 用户确认将当前 GraspNet→MoveIt→P6 候选链 jaw-width / 夹爪宽度上限统一为 `0.085 m`：GraspNet 预过滤、candidate IK gate、visual executor、production/preview launch、配置与 P6 runner 均使用 85 mm；边界行为为 `0.085 m` 接受、`0.085001 m` 拒绝。TDD RED 为新增3项按旧82 mm上限失败，GREEN focused `29 passed`；相关链路 `144 passed, 1 skipped`，layering `18 passed`，两包 symlink build与compileall通过，installed launch回读两个上限均为`0.085`。完整测试为`649 passed,15 skipped,1 failed`，唯一失败仍是既有 upstream provenance `PROVISIONAL` 与当前 manifest `AUTHORIZED_BY_UPSTREAM_OWNER` 的无关断言冲突。未启动ROS runtime、控制器或机械臂，未发送trajectory/gripper命令。证据：`Agent/evidence/P6/2026-09-01-p6-jaw-width-85mm.md`。
- 2026-09-01 85 mm 上限生效后的60秒production plan-only复测：runtime回读GraspNet与candidate IK filter均为`0.085 m`。收到`122/122` non-empty raw frames、1162 candidates，其中131个（11.3%）位于`(82,85] mm`且40个同时confidence>=0.4；无raw candidate超过85 mm。filter输出`40`，valid `34/40=85.0%`；P6 eligible为`5/40=12.5%` filtered、`5/34=14.7%` valid、`5/122=4.10%` raw。对同一85 mm输出流只施加旧82 mm final gate时为`1/40=2.5%`，新增4条width`82.670–83.144 mm`、confidence`0.567–0.898`，但这不是完整旧pipeline replay，因为旧GraspNet预过滤可能改变Top-N替补。首条raw confidence>=0.4为`0.413 s`、首条valid plan`2.547 s`、首条85 mm P6 eligible`14.899 s`；3577个静态joint samples最大变化`0.0 rad`。首次`start_visual_ready=true`临时启动因sim joint-state时序导致MoveIt初始化失败，未进入测量；改用launch既有`start_visual_ready=false`路径成功。临时MoveIt/filter/sim-controller已清理，原vision和localhost GraspNet PID131430保留。证据：`Agent/evidence/P6/2026-09-01-p6-jaw-width-85mm-pass-rate.md`。
- 当前 P1 收尾基线：`8186723`，`feat: close P1 on upstream-only mujoco baseline`。
- `HardwareManager.connect()` 已只建立通信、主动刷新/校验反馈并保持全部电机失能，不再 enable 或启动控制循环。
- 显式 `enable()` 会先验证反馈和软限位、设置当前位置 hold target、确认全部状态；失败时停止循环并失能回滚。
- 动作目标和控制入口在显式 enable 前 fail closed；夹爪初始化不再隐式 enable 或启动循环。
- 完整视觉抓取 launch 当前默认 `use_hardware=false`、`execution_mode=plan_only`，不自动 visual-ready 或 shutdown safe-home。
- 真机串口桥批量发送六个 feedback request 后单次 poll 不能可靠收齐响应；当前实现已改为逐电机 `request -> poll` 并对夹爪重试。
- Gate B 重复报告中的异常只出现在反馈数据：joint4-6 曾返回完全相同的 `-12.467574 rad` 和 `-23.435898 rad/s` 固定边界值，随后恢复基线；现场确认机械臂没有实际运动。
- arm、gripper 和 joint-state publisher 共用串口控制器；反馈刷新现已在 controller `RLock` 内完成整个逐电机事务，enabled hold 期间也主动刷新并校验状态/软限位，异常帧不会发布为 joint states。
- 显式 arm enable 不再启动闲置夹爪 500 Hz loop；夹爪 loop 只在收到真实夹爪 target/grasp 命令时启动。Gate B/C 验收增加 joint-state stale 检测，避免无有效样本时误通过。
- Gate B/C 已提供 `ros2 run rebotarmcontroller p0_gate_bc_acceptance` 专用工具；只有精确输入确认词后才显式 enable，并监控位置跳变/速度、自动 disable 和写入 JSON 证据，不发送轨迹、safe-home 或夹爪命令。
- MuJoCo 基础闭环、ROS adapter、metrics、容差检查、step-response 和 viewer 开关已经存在。
- Ubuntu Gemini 2 + YOLO 独立入口已经存在；P2 已按同一 serial 的当前硬件证据、既有 calibration provenance 和用户明确 acceptance decision 以 8/8 关闭。
- 2026-08-07 P2 SDK/ROS 检查点：真实 `Orbbec Gemini 2`（USB `2bc5:0670`，serial `AY6V16300BP`，firmware `1.4.92`）已在修正后的 `640x480 RGB + 640x400 depth + HW_MODE` 连续 `120/120` 同时出帧；aligned depth 为 `640x480`，depth scale=`1.0 mm/unit`。Driver 已 fail closed 处理 profile、应用 scale 并公开 device/profile/calibration/timestamp metadata；ROS 同时发布 color/depth CameraInfo。证据：`Agent/evidence/P2/2026-08-07-gemini2-readonly-preflight.md`、`Agent/evidence/P2/2026-08-07-gemini2-sdk-ros-acceptance.md`。
- P3 已以 7/7 完成：`vision_profile:=ubuntu_native` 已接入主 bringup，camera-only native launch、local YOLO、双 CameraInfo、detections、annotated、static TF 和 `RELIABLE` raw Image transport 均有真实 Gemini 2 运行证据。
- 2026-08-08 修复 Ubuntu YOLO 独立入口的 Python interpreter mismatch：普通 system-Python colcon build 会生成 `#!/usr/bin/python3` console script，而 `ultralytics` 只在 `.venv-vision`。launcher 现显式导出 venv `purelib` 到 `PYTHONPATH`，setup 在 colcon 前激活 venv；保留 system shebang 的真实 Gemini 2 短测仍成功初始化并连续输出 detections。focused 7、layering 18、全量 549 passed/14 skipped；未启动机械臂。证据：`Agent/evidence/P3/2026-08-08-ubuntu-vision-python-env-fix.md`。
- P4 已以 7/7 完成：官方 GraspNet baseline/checkpoint 与 CUDA 12.8 toolchain 仅本机使用；PointNet2 CUDA operator、真实 Gemini 2 inference、localhost contract、ROS 2 timestamp/frame/freshness，以及 stale/no-depth/no-candidate/TF failure fail-closed 均已验收。证据：`Agent/evidence/P4/2026-08-08-graspnet-real-inference.md`。
- 当前实际安装的 hand-eye 已完成：SUBPIX同固定12 training + 3 holdout离线门通过，DANIILIDIS候选已部署，官方Ubuntu vision/runtime TF通过；部署后三个全新temporal holdouts相对固定training reference为`2.495 mm/0.359 deg RMS`、rotation span`38.792 deg`，全部门通过。用户已取消物理TCP标定要求并明确接受upstream `ee_site=-0.105 m`为nominal TCP工程值；MuJoCo geometry已按文件一致性、用户权威provenance及canonical simulation workbench范围决策关闭，active upstream dynamics已按用户提供的实机标定provenance接受并关闭，contact force/material calibration已从规划中剔除。P5当前`6/6`完成；TCP关闭不表述为实机pivot标定通过。
- 2026-08-11 P5 MuJoCo geometry/dynamics/contact只读审计完成。active runtime仍是`rebotarm_mujoco_node`，但active model并非固定上游快照字节原样：joint2 upper limit含本地`+0.02 rad`安全margin，joint4-6 effort/force limit为本地`7 N.m`。URDF→MJCF确定性检查和17项模型/来源测试通过，只证明软件一致性。审计时axis/link/mesh/finger/`ee_site=-0.105 m`只有上游显式值或CAD-export候选来源，六轴独立零位、实物尺寸、真实夹爪几何和scene scope尚未闭环；active URDF主链质量总和`4.929100 kg`与同仓库CAD CSV的`2.741131 kg`也有差异。用户后续确认active upstream实机标定provenance、当前装机revision、六轴zero、夹爪CAD range及canonical simulation workbench范围，geometry与dynamics因此先后关闭；再明确接受upstream nominal TCP并取消实物TCP标定要求，P5最终`6/6`。Contact calibration已从规划剔除。证据：`Agent/evidence/P5/2026-08-11-mujoco-geometry-dynamics-contact-readonly-audit.md`、`2026-08-11-mujoco-geometry-authoritative-values.md`及`2026-08-09-tcp-upstream-explicit-decision.md`。
- 2026-08-11 用户作为硬件来源方补充 provenance：废弃的 downstream/current MuJoCo baseline 未经过电机标定，active upstream MuJoCo参数已经过实机标定，可以信任并作为本项目 authoritative calibrated baseline。此前`4.929100 kg`与CAD CSV`2.741131 kg`差异因此只保留为不同来源历史差异，不再作为active dynamics blocker；仓库没有原始标定数据，所以只能表述为“用户来源确认并接受”，不能写成本轮独立复现。active joint2 margin与joint4-6`7 N.m`是本地安全约束，不重新启用旧baseline，也不否定上游动力学基线。P5 dynamics checkbox据此关闭。
- 2026-08-11 P5 geometry authoritative-values表已建立。active URDF/MJCF、fixed upstream snapshot与CAD-export的六轴origin/axis/link transform一致；base/link1-6 mesh在active sim/bringup/upstream/CAD四处逐文件相同，gripper三件mesh在active sim/bringup/upstream三处相同；唯一geometry覆盖是joint2 upper`0 -> +0.02 rad`安全margin。文件级consistency通过。geometry checkbox仍不关闭，因为当前装机revision、除joint2外的zero convention、generic scene范围和夹爪nominal range接受口径尚未获得当前权威确认。证据：`Agent/evidence/P5/2026-08-11-mujoco-geometry-authoritative-values.md`。
- 2026-08-11 用户确认geometry三项provenance：当前装机没有换件、垫片或机械改装，与upstream/CAD mechanical revision一致；上游实机标定包含六轴zero convention；夹爪`0..90 mm`是CAD显式结构参数。robot body geometry的revision、axis/link/mesh、zero和gripper nominal range据此闭环；已有`8.051/86.083 mm`仅记录为controller端点，不改CAD机械范围。geometry checkbox只剩generic `scene.xml`范围决策。
- 2026-08-11 用户确认`scene.xml`不用拟合真实工作场景，保留为canonical simulation workbench / 规范仿真工作台即可。该engineering acceptance关闭scene scope缺口，但明确不把硬编码桌面/方块值写成真实场景标定。结合已确认的装机revision、六轴zero和夹爪CAD参数，P5 geometry checkbox关闭。
- 2026-08-11 14:30后只读复核发现`/dev/ttyACM0`重新出现，udev标识为`HDSC CDC_Device`、serial`00000000050C`，权限`root:plugdev 0660`；`fuser`无占用，未发现controller/MoveIt/MuJoCo ROS进程。本轮没有打开串口或连接硬件；设备重新出现不扩大授权，后续仍禁止未授权enable/trajectory/gripper/contact。
- 当前实机安装相对 upstream visual-ready 绕 base Z 轴约 `-90 deg`，用户明确说明 joint1 已手动旋转约 90 度并同意视觉作业范围同步到 `-Y`。视觉默认已按 `Rz(-90 deg)` 更新：visual-ready joint1=`-pi/2`、workspace 长轴 `+X -> -Y`、approach `+X -> -Y`、retreat `-X -> +Y`、fixed yaw `0 -> -90 deg`；hand-eye/TCP/camera calibration 不变。
- 用户确认当前机械安装/摆放正确后，只对 disabled joint2 执行一次 motorbridge `set_zero_position()`；未 enable、未 store parameters。反馈从 `-0.0020980835` 变为 `-0.0001907349 rad`，标准 driver 随后进入 `CONNECTED_DISABLED`，六轴 status 全 0。该 Damiao zero 不跨 power cycle 保留，断电后必须复测。
- 校零后的真实 disabled start state 已通过 real vision -> MoveIt plan-only：`/plan_kinematic_path` 返回 `SUCCESS(1)`、62 points，规划后 2.048 秒 joint delta 为 `0.0 rad`；没有 executor/action goal，P6 安全实机 plan-only 项已有硬件证据。选中 plan age 为 `1.356 s`，超过正式 executor 的 `1.0 s` freshness gate，不具备自动执行资格。
- 用户确认当前 joint2 机械位置为零点并允许覆盖后，disabled-only `set_zero_position()` 已再次执行；feedback 为 `-0.0001907349 rad`、status 0。joint2 software range 已在 real hardware guard、MoveIt/bringup URDF 和 active MuJoCo model/profile 中统一为 `[-3.14, +0.02] rad`；正向 margin 只容纳零点附近量化/回差，不授权正向运动。Damiao zero 掉电不保持，power cycle 后仍必须回到确认机械零位再 set-zero。
- joint2 新零位/limit 后的实机 enable-current-position-hold 回归已通过：5 秒、119 valid samples，max jump `0.0003815 rad`、max velocity `0.0073261 rad/s`，最终 disabled/IDLE、六轴 status 0。enable/disable 转换附近仍出现已知固定边界异常反馈和 status expectation race，但均被 guard 拒绝、未发布；低速轨迹验收仍须持续 freshness/jump/velocity 监测并自动 disable。
- 第一次 joint1 `+0.02 rad` 低速往返被 monitor 在 `0.051282 rad/s > 0.05` 时取消；用户随后确认是其在 trajectory 期间手动移动机械臂，因此不能据此判断 controller 超速，本轮 tracking 数据整体无效。joint1 最终相对 baseline 为 `+0.008774 rad`，其余五轴不变，最终 disabled/IDLE。cancel 与自动 disable 并发暴露的 action cleanup race 已做纯软件修复：`enable/disable/stop/get_joint_state` 现在共用 motor lifecycle `RLock`，stop 在 disabled transition 后不再读取 enabled feedback；真实单关节运动仍须重新验收。
- 用户授权后的 joint1 重试在无人为干预说明下再次得到相同 `0.051281929 rad/s` raw feedback 并按 `0.05` gate 安全取消；139 samples、max jump `0.00076294 rad`、其他关节 delta 0、joint1 到达 `+0.01983643 rad`。4340P `VMAX=10` 与观测格点强一致支持 12-bit velocity quantization（LSB `0.004884 rad/s`），而 `0.05` 位于 bin 内；不能证明实际超速，也不能按严格门限判通过。disabled 后 joint1 相对本轮 baseline 为 `+0.00953674 rad`，六轴 status 0。
- 本轮实机 driver 意外加载 stale copy-install，因此不能作为 lifecycle race fix 的 runtime evidence；随后已用 `colcon build --symlink-install --packages-select rebotarmcontroller` 重建，overlay 当前确认包含 `motor_lifecycle_lock`，但重建后未再连接或运动。
- rebuilt runtime 的 joint1 降速微动已通过：`+0.02 rad`，2 秒 quintic ramp、1 秒 hold、2 秒 return，commanded peak `0.01875 rad/s`；255 samples，raw feedback peak `0.03662968 rad/s`、position-window peak `0.01801461 rad/s`、max jump `0.00076294 rad`、其他关节 max delta `0.00038147 rad`，action `SUCCEEDED`。返回误差 joint1 `0.00114441 rad`，最终 disabled/IDLE、六轴 status 0。该结果只关闭 single-joint sub-gate。
- safe-posture 只读预检已完成：从最新 disabled final state 到 `[-pi/2,-0.1,-0.2,+0.2,0,0]` 的最大 delta 为 joint4 `+0.20171661 rad`。MoveIt explicit-start plan-only `SUCCESS(1)`，直接 joint interpolation 的 101/101 state 均通过 limits/self-collision。默认 4 秒峰值约 `0.09456 rad/s`，建议实机改为 20 秒、最大理论峰值 `0.018911 rad/s`。demo 没有真实 world geometry，仍需用户确认桌面/支架/障碍物间隙并授权。
- 用户授权的 20 秒 safe-posture 实机动作在早期被 raw `0.051283 rad/s > 0.05` gate 安全取消；最大实际变化为 joint4 `+0.00724792 rad`，最终 disabled/IDLE、六轴 status 0。failure summary 未保留触发轴/per-joint/window peaks，不能直接归因 4310 quantization 或放宽门限。下一步先修 monitor evidence，再申请 joint4 `+0.02 rad` 隔离往返。
- 用户随后明确允许各 joint raw feedback gate 放宽到 `0.07 rad/s`。策略采用双门限：per-joint raw hard gate `0.07`，timestamped position-window physical gate 保持 `0.05`；commanded safe-posture profile 仍约 `0.019 rad/s`。`p0_acceptance_core` 已实现 joint model VMAX、half-LSB limits 和 operator raw floor，focused 26 passed。safe-posture retry 仍需单独 hardware authorization。
- `0.07` raw gate 的 20 秒 safe-posture 重试仍未通过：joint4 在第 323 个 motion sample 触发 raw `0.09523773 rad/s`，cancel transient peak `0.15384674`；同一轮 joint4 0.20 秒 position-window peak仅 `0.01879278 rad/s`、max adjacent jump `0.00152588 rad`。action canceled，最终相对 baseline 最大 final delta为 joint4 `+0.00343323 rad`，自动 disabled/IDLE、六轴 status 0。当前证据不能把 spike 仅归因 threshold 边界量化；在无硬件软件审计前不继续放宽或重试。
- joint4 spike 的无硬件软件审计已定位控制语义 mismatch：`FollowJointTrajectory` 以 50 ms cadence 更新 `_q_target`，但 `ArmEndPos._loop_cb()` 的 POS_VEL 命令仍使用 joint4 config `vlim=3.0 rad/s`，没有采用轨迹 theoretical peak `0.018267`。早期 50 ms target increment 仅 `0.00026397 rad`（0.692 position LSB），观测 max jump 为约 4 position LSB，raw trigger/peak 为约 6.5/10.5 velocity LSB；最合理解释是离散 feedback、servo correction burst 和 raw quantization 叠加。优先修复是执行期间的 per-trajectory vlim override，而不是继续提高 raw gate。
- 用户指出安全门整体偏保守，并提出 joint4 输出力可能不足、小幅动作无法驱动末端。复核确认 4310 feedback TMAX/URDF effort limit为 `10/7 N.m`，低于 joint1-3 的 `28/27 N.m`；Pinocchio 当前 pose joint4 gravity demand约 `2.07 N.m`。这证明 load显著但未证明 saturation，因为此前未记录运动 torque/following error。决策从单一低 vlim override升级为 continuous trajectory governor、完整 torque/error telemetry、per-joint persistent gates 和分级 joint4 characterization；若 gravity/load主导，再评估 MIT gravity-compensated trajectory。单纯把 vlim降到 `0.03` 可能加重 static-friction breakaway，不采用。
- multi-signal safe-posture retry 收集到 742 samples：joint4 raw peak `0.25641` 但未满足 raw persistence，torque peak `0.769 N.m`、tracking error peak `0.00683 rad`，不支持 torque saturation。动作由 joint6 单次 0.20 秒 window `0.05487 > 0.05` 取消；window peak `0.05744`，joint6 raw peak `0.22711` 也未持续。最终 disabled/IDLE、六轴 status 0。window policy 已改为 `0.05` warning、`>0.10` 持续0.20秒 stop、`>0.20` immediate stop；新的实机重试需确认。
- P0 收尾完成：driver/controller 已在失能状态正常退出，ROS 2 daemon 已停止，`/dev/ttyACM0` 无进程占用；验收报告和 ROS logs 保留。
- 已清理源码侧 33 个 `__pycache__`、342 个 `.pyc/.pyo`、`.pytest_cache` 和 `/tmp/rebotarm_ros2`；未删除 `build/`、`install/`、`log/` 或验收证据。参考上游中受 Git 跟踪的历史 `.pyc` 已恢复，嵌套仓库保持 clean。
- P1 候选上游本机快照为 `https://github.com/HJX-exoskeleton/reBotArm_develop_hjx.git`，`master@bcd584ce4a64eb116d80f6873c1756eddb8520bf`（2026-06-09，subject `update`）。
- HJX 候选上游固定 commit 的 license status 为 `NOASSERTION`，已拒绝作为代码/资产来源；默认模型不再依赖该目录，只允许行为级观察。
- 可使用来源已固定：MuJoCo 3.3.0 tag commit `8de9b4e...`（Apache-2.0）、本仓库 Apache-2.0 URDF/MJCF/meshes、Seeed 官方资料 commit `c3a75bd...`（硬件 CERN-OHL-W-2.0，软件需逐文件确认 Apache-2.0）。
- MuJoCo venv 直接依赖已固定；当前 `mujoco 3.3.0`、`numpy 1.26.4`、`scipy 1.11.4`、`cffi 1.17.1` 可导入，`pip check` 通过。
- package-owned 模型专项测试 36 项通过；生成模型为 `nq=8`、`nv=8`、`nu=7`，robot smoke 和 grasp scene 均 finite，生成 XML 不含 HJX 路径。
- 当前 1 秒六轴 step-response 仍有 `max_final_abs_error=0.7860 rad`、`max_abs_error=1.5683 rad`，属于后续 tracking/dynamics calibration 工作，不因环境和授权修复而视为通过。
- P1 current/upstream capability gap matrix 已建立于 `docs/mujoco_gap_matrix.md`，并由 `tests/test_mujoco_gap_matrix.py` 校验表头、关键能力行和授权边界；矩阵将 grasp keyframe、timeout/execute-loop、limits consistency 标为当前 P1 implement，将 tracking/contact/calibration 标为后续 defer。
- grasp scene keyframe 已修正：命名 keyframe `0` 现在同时保存 home arm `qpos` 与对应 `ctrl`，避免 reset 后 position actuator 默认回零导致姿态漂移；1 秒 benchmark `finite=True`，但仍为 `contact_without_lift`，因为当前 benchmark 不发送抬升轨迹，接触/抓取质量仍未完成标定。
- P1 最终策略已按用户决定收口为 upstream-only：`huangbinai/robotarm_ros2` 的 `main@fb28dcdd358b45de79eb47adfb333e2e94e9d5b4` 已本地化到 active `src/rebotarm_simulation`；setup/launch 不再安装或选择 current adapter、legacy CLI 和 snapshot wrapper。切换前实现仅保存在 `third_party/rebotarm_simulation_current_baseline`，不参与 active runtime。
- `robotarm_ros2/src/rebotarm_simulation/package.xml` 声明 Apache-2.0，但固定仓库根 LICENSE 文件尚未核验到；已允许当前工作区本地使用默认 upstream backend，仍禁止在授权确认前对外再分发该快照。
- 用户已提供本机 clean 快照 `/home/a/project/rebot_refer`，remote 为 `https://github.com/huangbinai/robotarm_ros2.git`，固定 `main@fb28dcdd358b45de79eb47adfb333e2e94e9d5b4`。目标包文件清单、SHA-256 和授权结果记录于 `Agent/evidence/P1/2026-08-06-robotarm-ros2-preflight.md`。
- 已按 upstream-first 选择性本地化 health check：`rebotarm_simulation.mujoco_health.check_model_health()` 与 `rebotarm_mujoco_health` console script 只检查当前 package-owned XML；TDD、CLI、分层、全量测试和 compileall 均通过。证据：`Agent/evidence/P1/2026-08-07-health-check-localization.md`。
- URDF-to-MJCF 审查确认上游 `robot.xml` 为 `nu=8/nsensor=26` 的独立 torque/finger actuator 模型，而当前生成模型为 `nu=7/nsensor=0` 并由 `sim_gripper` 保持夹爪契约；本轮决定不直接替换模型。证据：`Agent/evidence/P1/2026-08-07-model-converter-audit.md`。
- 用户确认采用 A 方案后又明确只使用上游 MuJoCo：固定快照与 A/B 证据继续保留作 provenance，active runtime 已直接使用本地化 upstream package；不再通过 `upstream_backend.py` 进程边界或 launch selector 启动 current。
- 上游完整 MuJoCo package 已按固定 commit 原样复制到 `third_party/robotarm_ros2_mujoco_snapshot/`；`diff -qr` 与 `/home/a/project/rebot_refer/src/rebotarm_simulation` 无差异，provenance manifest 和 localization README 已加入。该目录不在默认 `src/`，仍受 `PROVISIONAL` license 边界限制。
- A/B 对比已完成：两套 health/headless 均通过；current `nu=7/nsensor=0`，upstream `nu=8/nsensor=26`；1 秒 step-response 的 max final error 为 `0.7860` vs `0.6443 rad`，max velocity 为 `4.0664` vs `1.9567 rad/s`；两套 grasp smoke 均 contact 但均无 lift success。完整 JSON/解释见 `Agent/evidence/P1/2026-08-07-baseline-comparison.{json,md}`。
- 第二轮 command-level 对比已完成：两套 backend 接收同一六轴目标 `[1.4,-0.785,-0.785,0.710,0.785,1.570] rad` 与 `gripper_width=0.04 m`。Current adapter 的 max final error / max velocity 为 `0.7930 rad` / `5.2087 rad/s`，上游 native runtime 为 `0.3611 rad` / `1.9563 rad/s`；该结果只说明现有控制器响应差异，不能证明模型可直接替换。证据：`Agent/evidence/P1/2026-08-07-baseline-comparison-command-contract.json`，并已合并到主对比 JSON/Markdown。
- 用户确认保留上游左右独立 force actuator 语义，不再把上游夹爪改造成当前单耦合 gripper actuator，也不再为 actuator 对齐本身扩展测试；后续只在统一高层 command、明确 controller contract 的前提下比较结果。
- P1 timeout 已接入 `MuJoCoRosAdapterNode`：以 trajectory duration 加 `execution_timeout_margin_sec` 计算 monotonic wall-clock deadline；timeout 映射为非成功 action result，并与 cancel/stop/tolerance failure 一样将 arm targets hold 在当前位置。端到端 ROS action integration tests 仍待补齐。
- P1 execute-loop integration 已补齐：在 ROS 2 + MuJoCo venv 下 success、cancel、stop、path tolerance、goal tolerance、timeout 六种结果均通过，并验证失败路径 hold 当前姿态；证据：`Agent/evidence/P1/2026-08-07-execute-loop-integration.md`。系统 Python 因缺少 `rebotarm_msgs`/MuJoCo 会跳过该组测试，不能替代显式环境验收。
- P1 limit consistency 已补齐：URDF arm effort 与生成 MuJoCo `forcerange` 为 joint1-3=27、joint4-6=7；MoveIt planner velocity 为 joint1-3=3.0、joint4-6=1.8，均低于 URDF 50/200 硬件上限；acceleration=5.0、jerk=20.0 由 `rebotarm_motion` runtime guard 对实测 velocity/effort 做有限差分检查，违规 abort 并 hold。gripper effort 不做跨 contract 等式比较。证据：`Agent/evidence/P1/2026-08-07-runtime-limit-consistency.md`。
- 3 秒线性 ramp tracking audit 发现当前 joint4/joint5 仍有 `0.3437/0.7843 rad` 最终误差，joint5 峰值速度 `3.7198 rad/s`；上游同一高层命令原生 controller 的最大最终误差 `0.005234 rad`。当前 joint4-6 `forcerange=±7`，上游 arm torque limit 为 `±12.5`；在没有真实硬件证据前不自动提高当前力限或改 gains。证据：`Agent/evidence/P1/2026-08-07-joint4-6-tracking-audit.md`。
- 用户确认上游 arm force limit 可采用；已增加隔离 `UPSTREAM_ARM_MOTOR_PROFILES` / `motor_profile:=upstream_arm`，但在保留当前 position-actuator controller、gains 和 dynamics 的前提下，仅把 joint4-6 XML forcerange 改为 ±12.5，joint4/joint5 tracking residual 仍为 `0.3437/0.7843 rad`。因此上游优势来自完整 cascaded torque controller、gravity compensation、rate limiting/filtering 和 dynamics，不是 XML force ceiling 单项；当前默认仿真已改走完整 upstream runtime。

## 当前决策

- 根目录两份旧规划已按用户要求清理；当前验收状态统一以 `Agent/PROJECT_STATUS.md` 为准，旧规划内容可从 Git 历史恢复，新规划等待用户提供。
- 2026-09-06 主目录整理：用户要求以整合后版本为主，不再保留日期标记。因CMake缓存包含绝对构建路径，未直接改名缓存，而是在标准 `build/` 中重新构建13包并更新 `install/`；原整合日志移动到标准 `log/`，验证记录保留在 `log/checks`。旧 `build/`、旧 `log/`、`build_integration_20260906/` 和 `install_before_integration_20260906/` 已移入系统回收站，可恢复；模型、环境、实验数据和独立工作树未动。删除根目录两份规划并同步索引、Agent状态来源及测试，历史文件仍可从Git恢复。验证：Agent/分层22通过，全量822 passed/8 skipped，13包构建成功、compileall和diff检查通过；从/tmp核验controller、messages、teach和motion安装导入正常，1mm闭合反馈容差不变，构建与安装环境配置无旧日期目录/工作树路径引用。唯一构建警告仍是simulation assets的既有setuptools包发现提示。未操作硬件、未提交或推送；P0-P6关闭状态保持。
- active runtime 只允许 package-owned upstream MuJoCo node；旧 package-owned current 实现仅留带 SHA-256 manifest 的归档，不安装 console entrypoint，也不参与 launch。
- P5 paired sim-to-real 的仿真端只允许 active `rebotarm_mujoco_node`；废弃的 current baseline 只可作为历史 provenance/旧 P1 证据，不作为新 replay、runtime 或 comparison backend。
- P0 安全优先于任何实机视觉联调。
- 相机 intrinsics 和 hand-eye 不默认重新标定：先保留上游已验证参数，完成只读 SDK/depth/alignment/TF 检查，再通过分级实机夹取表现和可量化残差决定是否进入 P5 重标定。单次夹取失败不能直接归因于标定，必须同时排查 depth scale、RGB-D alignment、时间戳/TF、TCP、检测偏差、规划和机械误差。
- P2 closeout 接受沿用 serial `AY6V16300BP` 的既有相机 calibration；本轮未重做 pixel-level 受控目标或精确多距离 bias/RMSE 表。只有后续真实抓取出现系统性 depth/alignment 残差时才重新打开，不把该豁免扩展到 P5 hand-eye/TCP。
- 2026-09-03 GraspNet localhost HTTP迁移已完成：Ubuntu生产launch默认`source_mode=in_process`，由同一视觉抓取launch用`.venv-graspnet/bin/python`启动独立ROS进程，直接订阅时间匹配的RGB/depth/CameraInfo/detections并调用完整GraspNet runner；保留完整场景collision cloud、目标mask/depth分离、确定性采样、projection/jaw-width过滤、timestamp/frame_id和空结果fail-closed语义。真实Gemini 2 + TensorRT YOLO只读runtime已发布`source=ubuntu_inprocess_graspnet`的bottle candidates，backend available且8081无listener；未启动MoveIt/controller/trajectory。Windows/network candidates兼容模式保留，旧localhost service脚本仅作为历史回退/contract测试工具。证据：`Agent/evidence/P6/2026-09-03-graspnet-inprocess-integration.md`。
- P4 环境与 service contract 子项已完成：独立 `.venv-graspnet`（不使用 `--system-site-packages`）固定 PyTorch `2.11.0+cu128`、Open3D `0.19.0`、NumPy `1.26.4` 等依赖；CUDA import 和隔离性通过。localhost service 可独立启停并提供 `/health`，contract `1.0` 固定 RGB、米制 depth、intrinsics、bbox、timestamp/frame_id；未配置 backend 时 fail closed。
- 2026-08-08 用户确认项目为个人 noncommercial research，并同意 GraspNet 受限代码/checkpoint 仅本机使用、不提交、不推送；license 使用确认门已解除。
- P4 官方资产固定为 `graspnet-baseline main@280c2151...`、`graspnetAPI master@eb57dd20...`，checkpoint SHA-256 为 `60680087c61cba2b6791614fef1519071e294f6dcaf99b3f581bb95f7c51a868`；全部位于已忽略的 `.local-models/`。
- 最终 ROS 2 实流 probe 收到 29 条 candidates：timestamp 全部非零且严格递增，frame_id 全为 `camera_depth_frame`，freshness `519.3–686.5 ms`，11 条非空；通过 timestamp cache、pending detection 和 depth-1 QoS 消除了同步 inference callback backlog 的错配，不放宽 100 ms RGB-D skew gate。
- P5 只读审计：实时 `git ls-remote` 确认上游 `main` HEAD 仍为固定的 `fb28dcdd358b45de79eb47adfb333e2e94e9d5b4`；其实际 `handeye.yaml` 与 runtime/本地文件一致，发布 `end_link -> camera_depth_frame = [-0.085, 0.010, 0.045] m + quaternion [-0.5231, 0.5781, -0.4057, 0.4770]`。README 的另一组 TSAI/32-sample transform 是未同步的旧记录，不构成当前配置冲突；仓库仍无原始 NPZ/样本可复算。
- 当前安装方向采用 base `-Y` 视觉作业坐标，不修改 hand-eye/TCP：base 安装方向属于 task-space defaults，hand-eye/TCP 是 `end_link` 局部外参。只有后续多姿态 residual 或真实夹取出现系统性偏差时才考虑重新标定。
- 当前 source 的 `end_link -> grasp_tcp` 与 active local MuJoCo `ee_site` 采用 `[-0.04, 0, 0] m`；固定 third-party upstream snapshot 中的历史 `-0.105 m` 不再是 active runtime 值。2026-09-02 用户观察当前 TCP 中心位置可能存在小幅偏差，决定后续先做受控、可量化的 TCP 对位/残差测试，再依据结果调整 active TCP；测试前不猜测或修改当前值，也不把单次夹取偏差直接归因于 TCP。该决定不等于物理 TCP 标定已经完成，当前 P5 checkbox 暂不改变。
- active MuJoCo calibration 与固定上游完全一致，但 `finger_force_limit_n=20` 明确是未用 scale/load cell 实测的保守值，firmware-to-torque、damping、friction/contact 也没有当前实机测量证据；软件一致性不能替代 sim-to-real calibration。
- 自动测试、仿真验收和真机验收分别记录，不能互相替代。
- P6 hardware acceptance 必须区分 `pre-disable controlled target` 与 `post-disable free state`：trajectory 到位以 enabled feedback/action result 和 active tracking guard 判定。成功动作后必须保持 `enabled hold`，或先受控回到用户确认的可失能位置再 disable；禁止在悬臂 visual-ready/pre-grasp 姿态直接 disable。只有 holding torque 已不可信的严重硬件/通信异常或明确急停时，protective disable 才优先于保持/回位。
- 2026-08-09 用户再次明确真机 teardown 硬规则：健康控制器在视觉/规划/标定采集等可恢复失败后，禁止通用异常分支直接 disable。必须 stop 后保持 enabled hold，或执行带 guard 的受控回本轮 baseline、验证到位后才 disable；回位失败但 ArmStatus/电机仍健康时保持使能等待人工恢复。只有 controller/motor error、通信/反馈丢失、明确 emergency stop 等 holding torque 不可信的严重故障才允许在非 baseline 姿态保护性失能。该规则已写入 `AGENTS.md`、`Agent/README.md` 和 `Agent/EXECUTION_FLOW.md`，并要求测试覆盖。

## 当前阻塞

当前没有既定阶段 blocker / 阻塞；P6 已按 operator 决策范围收口。未完成的 active MuJoCo 正向复验、lift/retreat 与相关历史失败均保留为事实和后续规划输入，但不再是当前队列。等待 operator 提供下一份规划后重新建立阶段、验收项和动作授权。

## 最近验证

- 2026-09-06 两目录先提交后整合：主目录 `cea5528` 与反馈解耦工作树 `6fc58e8` 已通过正式Git合并解决冲突，合并后的src/tests/tools/patches与已测试工作树一致。主目录新非symlink `install` 独立构建13包成功，旧安装保留在 `install_before_integration_20260906`；10个关键模块均从主目录install导入且与源码逐字节一致，3个退役模块不可导入，20个URDF网格引用有效，11个launch的 `--show-args` 通过且无工作树路径。新安装环境下分层20 passed、全量822 passed/8 skipped、src compileall及diff检查通过；MotorBridge为0.4.6+rebotarm.2且feedback_sequence=true。构建仅有simulation assets的setuptools包发现警告，没有构建失败。检查日志在 `log_integration_20260906/checks`。未启动实机或访问串口，整合后实体复测仍待用户执行；未推送，用户运行证据和模型资产保留。
- 2026-09-06 耦合维护：`python3 -m pytest tests -q` 为 `761 passed, 8 skipped`；`tests/test_package_layering.py` 为20通过；`tests/test_launch_interpreters.py` 在源码搬迁且存在诱导旧venv目录时，默认/环境变量/显式参数三种优先级全部通过。对明确导入当前worktree正式 `mujoco_ros_node.py` 的上游adapter/sampler测试为55通过。独立 `colcon --build-base build_decoupling --install-base install_decoupling` 构建bringup/simulation/vision三包通过，8个已安装launch的 `--show-args` 通过，旧模块 `find_spec` 为None；安装解析日志位于 `log_decoupling/checks/`。分层包、相关launch及仿真包compileall和 `git diff --check` 通过。修正两处旧测试假设：P6已关闭允许无未完成阶段，GraspNet解释器不再必须依赖 `.venv-graspnet` 目录。

- 2026-08-11：P6-D fixed-camera optical TF与ground-truth-assisted offline plan-only链路通过。MuJoCo→ROS optical轴转换显式为`diag(1,-1,-1)`，runtime发布`base_link -> mujoco_fixed_camera_optical_frame`静态TF；depth点反投影到base后距40 mm cube中心`25.348 mm`，落在物体体积内。新增无真实backend/无executor的`mujoco_offline_perception.launch.py`，修复ROS 2 Jazzy node-specific YAML覆盖launch wildcard dict导致工作台参数静默失效的问题。干净启动无需热设置即保持nominal TCP`[-0.105,0,0] m`并在45秒内连续产生3个IK+collision通过的valid plans。final`605 passed,15 skipped`、layering18、build/compile/show-args/diff通过。该项仅是partial milestone；未运行rendered RGB YOLO、未发送MuJoCo trajectory、未做outcome matrix，P6保持`4/8`。证据：`Agent/evidence/P6/2026-08-11-mujoco-offline-tf-plan-only.md`。
- 2026-08-11：P6-D final cleanup将仓库内`.pytest_cache`及20个project `__pycache__`共21目录、`4.6 MiB`可恢复地移至桌面回收站`/home/a/.local/share/Trash/files/rebot_Arm-cache-p6d-20260811-1647`；保留用户修改、证据、模型、虚拟环境和`build/install/log`。清理后无项目runtime或8081 listener，`/dev/ttyACM0`存在但无人占用；完整测试结果来自清理前，不再运行会重建cache的测试。
- 2026-08-11：P6-E rendered RGB offline YOLO fail-closed通过software-only子门。新增`rebotarm_offline_yolo_node`，active MuJoCo `rgb8`输入经独立raw topic与`target_classes=['cube']` allowlist过滤；隔离domain日志frame `20/40/60/80/100`均为`raw=2,target=0`，raw为stock COCO `cup/cake`误检，filtered best-effort echo为空。stock COCO没有`cube`类，因此不宣称正向目标识别，也不把raw结果接入GraspNet；focused4、layering18、全量`609 passed,15 skipped`、两包build、完整package/launch compileall与show-args通过。验证后根`.pytest_cache`与`src/tests/tools/Agent/scripts`下19个`__pycache__`共20目录、约`4.1 MiB`已可恢复移至`/home/a/.local/share/Trash/files/rebotarm-cache-p6e-20260811-1716`；补跑compileall新增7目录约`988 KiB`移至`/home/a/.local/share/Trash/files/rebotarm-cache-p6e-final-20260811-1717`，当前scoped cache为0，用户资产、证据、模型、虚拟环境和build/install/log保留。P6仍`4/8`，后续需合适目标模型/场景证据、offline GraspNet→MoveIt→MuJoCo execution与outcome matrix。证据：`Agent/evidence/P6/2026-08-11-rendered-rgb-offline-yolo-fail-closed.md`。
- 2026-08-11：P6-F active MuJoCo arm-only trajectory execution通过software-only子门。隔离`ROS_DOMAIN_ID=103`、唯一`rebotarm_mujoco_node` action server，以5秒joint1 `+0.05 rad` roundtrip回baseline；`FollowJointTrajectory`返回`error_code=0`、`simulation trajectory finished`、`SUCCEEDED`，结束joint1相对baseline约`1.30e-5 rad`，其余轴约`1e-7 rad`量级。未启动virtual camera/GraspNet/executor/夹爪/contact，未接触真机；不等于完整offline YOLO→GraspNet→MoveIt→MuJoCo抓取闭环。后续状态刷新产生的1个`__pycache__`约`224 KiB`已移入`/home/a/.local/share/Trash/files/rebotarm-cache-p6f-final-20260811-1721`，当前scoped cache为0。P6仍`4/8`，下一步是把已通过plan-only结果接入arm-only simulation execution并建立outcome/failure matrix。证据：`Agent/evidence/P6/2026-08-11-mujoco-trajectory-roundtrip-software-only.md`。
- 2026-08-11：P6-F2 integrated plan-to-MuJoCo arm-only execution通过software-only子门。隔离`ROS_DOMAIN_ID=104`，带simulation-time时间戳的P6-D valid `GraspPlan`经现有`VisualGraspExecutorNode`→`PoseExecutionNode`→MoveIt→唯一active`rebotarm_mujoco_node` action完成pregrasp、approach、lift、safe retreat；`execute_gripper=false`日志明确`simulation: gripper command skipped`，未执行contact或夹爪命令。首轮错误orientation provenance导致MoveIt`99999`，独立IK sweep找到top-down quaternion后重试成功；受控回MuJoCo baseline后六轴最大收敛误差`4.35e-5 rad`。旧RViz-only controller不存在，8081与串口均未占用。本轮6个源码`__pycache__`（约`672 KiB`）已可恢复移至`/home/a/.local/share/Trash/files/rebotarm-cache-p6f2-20260811-1740`，当前scoped cache为0。该子门不等于live positive YOLO/GraspNet完整离线闭环，P6仍`4/8`；正向`test_cube`模型/场景证据与outcome/failure matrix仍缺。证据：`Agent/evidence/P6/2026-08-11-mujoco-integrated-plan-arm-only.md`。
- 2026-08-11：用户决定暂不推进额外的RViz `MotionPlanning`面板接线/一键演示工作，恢复原P6 baseline / 基线范围；不新增P7、不回退既有代码或用户修改。下一软件优先级仍是当前P6完全离线闭环剩余项：canonical `test_cube`正向目标模型/场景证据、同一live positive perception plan闭环与unified outcome/failure matrix；Contact calibration继续不在规划内，approach/gripper/lift/retreat仍需单独硬件授权。
- 2026-08-11：P6-G live positive offline loop通过software-only子门。隔离`ROS_DOMAIN_ID=117`运行active `rebotarm_mujoco_node` virtual RGB-D→YOLO-World（`yolov8s-world.pt`，SHA-256 `095f5266bb9b654bd5ad9e21e9cdeda78e0f2c8460f5d652eaf04bab7ee251cf`）→local GraspNet→TF/MoveIt→`VisualGraspExecutorNode`→唯一MuJoCo action；GT标注与YOLO使用分离topic，检测到`small red cube`并持续产生valid filtered plans，`visual_grasp/execute`返回`success=True`，pregrasp/approach/lift/safe retreat全部`trajectory executed`，`execute_gripper=false`明确跳过夹爪，随后回到软件baseline且六轴最大误差约`0.001554 rad`。本轮显式使用`base_axis`、top-down quaternion和`candidate_grasp_z_offsets_m=[0.08]`，该8 cm偏移是当前GraspNet/场景profile workaround，不是通用TCP或接触标定；未触碰真机、未开串口、未执行夹爪/contact/pickup。 stock COCO `cube`正向识别仍未声称通过；P6变为`5/8`，剩余统一outcome/failure matrix与分级实机验收。证据：`Agent/evidence/P6/2026-08-11-mujoco-offline-positive-yoloworld-loop.md`。
- 2026-08-11：用户将active canonical MuJoCo目标从范例`test_cube`切换为与真机示例一致的`bottle` proxy / 瓶子代理，正式写入`src`的scene、virtual RGB-D annotation defaults、offline YOLO bottle allowlist和GT/YOLO topic separation；视觉模型保持现状，未更新YOLOv26m或任何权重。现有stock `yolo11n-seg.pt`对rendered bottle输出约`0.76` confidence，隔离live ROS runtime输出约`0.73` bottle detection；local GraspNet产生10个候选，显式base-axis/top-down/`[0.08] m` profile下MoveIt产生`valid=true` bottle plan。未启动executor、未发送MuJoCo trajectory、未执行夹爪/contact/pickup；bottle proxy不代表实物动力学或接触标定。P6仍`5/8`，统一outcome/failure matrix与分级实机验收仍未完成。证据：`Agent/evidence/P6/2026-08-11-mujoco-bottle-target-visual-loop.md`。
- 2026-08-11：按用户要求完成P6瓶子目标验证后的可恢复现场清理。项目runtime、8081 listener、`/dev/ttyACM0`占用和旧controller均为空；源码/测试/Agent/tools等范围内21个`__pycache__/.pytest_cache`目录约`3.9 MB`，以及10个明确P6 `/tmp`图像/日志/launch文件和491个ROS `launch_params_*`临时文件约`1.2 MB`，统一移入`/home/a/.local/share/Trash/files/rebotarm-cache-p6-clean-20260811-Pjqfpk`，可恢复。`.local-models`、weights、虚拟环境、`build/install/log`、Agent/evidence和用户修改均保留。下一步准备staged hardware acceptance / 分级实机验收，只做只读预检，不自动启动runtime或真实硬件。
- 2026-08-11：用户决定退回本轮 unified outcome/failure matrix / 统一结果失败矩阵软件项，不将其作为当前P6验收项；下一步切换为 staged hardware acceptance / 分级实机验收只读准备。当前不启动真机、不打开串口、不发送trajectory或夹爪动作，下一硬件门按既有顺序为 approach，需单独明确授权。
- 2026-08-11：P6-A hybrid composition软件修复完成。入口现显式使用Ubuntu-native vision、localhost GraspNet local service和active `rebotarm_mujoco_node`，以`start_sim_trajectory_controller=false`保持接口互斥；MuJoCo launch支持`rebotarm_sim` namespace/初始姿态。Focused `100 passed`、两包顺序build、installed launch展开、full `590 passed, 15 skipped`、compile/diff均通过。未启动任何runtime或硬件，P6保持`2/8`。证据：`Agent/evidence/P6/2026-08-11-hybrid-composition-modernization.md`。
- 2026-08-11：P6-B当前Ubuntu-native真实感知+active MuJoCo无运动runtime验收通过。修复exact-node YAML/嵌套launch参数覆盖、wall/sim clock domain、HTTP transport timestamp复用及1秒plan-age门过紧；contract `1.0`新增兼容`sent_at_unix_ns`并保留1000 ms freshness guard，真实链路默认plan age仍为1秒，仅hybrid为3秒。最终真实Gemini 2/YOLO/local GraspNet/MoveIt/MuJoCo同轮持续产生valid plans，5个age样本`0.578–2.108 s`；sim action仅1 server、旧namespace 0 server，无真实/RViz-only controller、无start调用/trajectory。final `594 passed,15 skipped`、layering18、compile/diff通过；现场已清理。P6变为`3/8`，只关闭当前真实视觉+MuJoCo项；虚拟RGB-D、离线闭环、自动抓取与新实机动作仍未关闭。证据：`Agent/evidence/P6/2026-08-11-ubuntu-native-mujoco-runtime.md`。
- 2026-08-11：P6-B final cleanup限定移除仓库根`.pytest_cache`以及`src/tests/tools/Agent/scripts`中的22个`__pycache__`，共23目录、约`4.5 MiB`；可恢复位置为桌面回收站的`rebot_Arm-cache-p6b-20260811-1559`与`rebot_Arm-cache-p6b-final-20260811-1605`。保留用户修改、证据、模型、虚拟环境、`.local-models`、`third_party`和`build/install/log`；清理后无项目runtime、8081关闭、`/dev/ttyACM0`无人占用。已完成的`594 passed,15 skipped`等验证结果来自清理前最终验证，不在清理后重复运行以免重建cache。
- 2026-08-11：P6-C MuJoCo virtual RGB-D/CameraInfo/object annotations验收通过。功能留在active`rebotarm_mujoco_node`且默认关闭；显式EGL启用后同步发布RGB、毫米depth、两路intrinsics和segmentation ground-truth detection/mask。隔离runtime五路同stamp，depth`740..1999 mm`，`test_cube` bbox/mask有效，16帧平均`10.027 Hz`；default-off无camera topics。首轮`EGL_BAD_ACCESS`定位为context跨constructor/executor线程，改为dedicated worker后通过。sim action唯一server、旧namespace0 server，无真实backend或命令。final`601 passed,15 skipped`、layering18、build/compile/show-args/diff通过；本轮20个限定cache目录（`4344 KiB`）已可恢复移至`/home/a/.local/share/Trash/files/rebot_Arm-cache-p6c-20260811-1614`。P6变为`4/8`；完整offline loop仍缺TF、offline YOLO/GraspNet/MoveIt/MuJoCo组合和outcome matrix。证据：`Agent/evidence/P6/2026-08-11-mujoco-virtual-rgbd-annotations.md`。

- 2026-08-11：P6 software-only/read-only gap audit完成，未启动任何runtime或硬件。确认历史hybrid入口没有启动active MuJoCo：它不覆盖默认`vision_profile=network`，显式使用network GraspNet，且`use_hardware=false`时启动与MuJoCo action/service同名的RViz-only controller。虚拟相机只有MJCF `fixed_camera`定义，没有ROS Image/CameraInfo/annotation publisher。P6保持`2/8`，首要实现项为hybrid composition修复。证据：`Agent/evidence/P6/2026-08-11-p6-software-gap-audit.md`。

- 2026-08-11：P6 entry cleanup / 进入P6前现场清理完成。未发现controller、MoveIt、MuJoCo、RViz、YOLO或GraspNet项目进程；`/dev/ttyACM0`存在但`fuser`无占用；`/tmp`无匹配的rebotarm/P5/MuJoCo临时项。源码侧两个`__pycache__`与仓库根`.pytest_cache`共`80 KiB`已移入桌面回收站，可恢复。按既定边界保留用户工作区修改、`Agent/evidence`、`.local-models`、虚拟环境及`build/install/log`。为避免重建缓存，本轮未重新运行pytest/compile；P6仍无新增硬件动作授权。

- 2026-08-11：用户授权 approach 后，fresh bottle plan 的 plan-only 安全门 fail-closed。confidence `0.855`、age `0.821 s`；pregrasp 最大关节变化 `1.981618 rad`，grasp/approach 从 baseline 预检最大变化 `2.249726 rad`，joint3 `2.184955 rad`，高于此前已审查 pre-grasp joint2 `1.61765 rad / 92.68 deg`。未 enable、未发送 trajectory、未执行 gripper/lift/retreat/contact；launch、local GraspNet、8081 与串口已清理。证据：`Agent/evidence/P6/2026-08-11-approach-gated-large-joint-delta.md`。
- 2026-08-11：用户接受具体大路线后尝试受保护 approach；fresh bottle plan confidence `0.7585537`、age `0.8506 s`，pregrasp MoveIt plan `92` points/`9.011483667 s`，最大 baseline delta `2.00063367 rad`。真实发送 pregrasp leg 后 joint4 raw velocity `0.3003654479980469 rad/s` 触发 immediate guard，action canceled；未发送 approach/gripper/lift/retreat/contact。健康 enabled hold 下完成25秒 guarded return，回位最大误差`0.00038147 rad`后disable，最终六轴status0/no error。不得直接重复，证据：`Agent/evidence/P6/2026-08-11-approach-guarded-joint4-stop.md`。

- 2026-08-11：用户明确确认采用active upstream MuJoCo `ee_site=-0.105 m`作为nominal TCP工程验收值，并取消实物classic pivot calibration要求。TCP checkbox据此关闭，但证据边界是不冒充实机TCP标定通过，也不授权任何新硬件动作。P5达到`6/6`。

- 2026-08-11：用户接受active`scene.xml`作为规范仿真工作台，不要求拟合真实现场；结合文件级geometry一致性、当前装机revision、上游六轴zero convention和夹爪CAD nominal range确认，P5 geometry checkbox关闭。该关闭只代表robot geometry与canonical sim scene验收，不代表真实工作台/障碍物标定。未启动runtime或硬件。

- 2026-08-11：用户确认当前装机无换件/垫片/机械改装，上游实机标定包含六轴zero convention，夹爪`0..90 mm`为CAD显式结构参数。三项与既有文件级一致性合并后，robot body geometry已具authoritative provenance；geometry checkbox仍等scene scope决策，P5保持`4/6`、overall`89.2%`。未启动runtime或硬件。

- 2026-08-11：完成P5 geometry authoritative-values纯软件核对。六轴joint origin/axis/link transform在active URDF、fixed upstream和CAD-export逐项一致；active MJCF由URDF确定性生成，`--check`通过。base/link1-6 mesh四来源逐文件一致，gripper三件mesh三来源一致；没有修改模型参数。既有joint2 zero与gripper端点证据被纳入边界，但不足以独立证明其他五轴zero、当前装机revision或真实scene。geometry checkbox保持未勾选，P5仍`4/6`、overall`89.2%`；未启动任何runtime或硬件。证据：`Agent/evidence/P5/2026-08-11-mujoco-geometry-authoritative-values.md`。

- 2026-08-11：用户确认downstream/current MuJoCo旧基线未做电机标定，active upstream参数已做实机标定并可直接信任。按项目authoritative-source acceptance关闭P5 dynamics checkbox；历史CAD CSV质量差异不再阻塞当前active dynamics。此关闭依据是用户提供的标定provenance，不是本轮独立采集原始标定数据。Contact已移出规划；geometry仍未关闭。状态预计P5`4/6`，不进入P6；未启动任何runtime或硬件。

- 2026-08-11：用户明确将contact force/material calibration从项目规划中剔除，而不是延后。原因是当前夹爪没有力反馈，无法形成准确接触估计。该项不是passed，也不是blocker或未来待办；只有用户以后加入力反馈装置并明确提出时才重新建立接触测试项。该决定作出时P5仍`3/6`；随后用户补充active upstream dynamics已实机标定的provenance并关闭dynamics checkbox，当前进度以后续状态为准。未启动任何硬件或仿真runtime。

- 2026-08-11：完成P5 MuJoCo geometry/dynamics/contact read-only/software-only审计。确认active MJCF由active URDF确定性生成，`--check`通过；相对固定upstream snapshot仅有joint2 upper limit和joint4-6 effort/force安全差异。离线加载MuJoCo 3.3.0场景成功，focused model/source tests `17 passed`。gap matrix将axis/zero/link/mesh/finger/ee_site/scene、mass/inertia、motor conversion、friction/contact逐项分类为upstream explicit、real-spec candidate、measured evidence、assumption或blocker；没有新checkbox。未启动controller/MoveIt/ROS MuJoCo node，未访问串口或硬件。证据：`Agent/evidence/P5/2026-08-11-mujoco-geometry-dynamics-contact-readonly-audit.md`。

- 2026-08-09：用户授权guard后改线重试剩余两个post-deployment temporal holdouts。disabled preflight marker30/30，center std max0.0174px，start drift0.015259rad。删除joint6直接大跨越、每段改30秒后，4/4 action、20,046samples无guard stop；两个capture首次stability attempt均通过，enabled回reference max error0.000763rad后才disable，最终status0/no error/tty free。合并首轮新点后，相对固定training reference的position RMS/max2.495/2.837mm、rotation RMS/max0.359/0.463deg；自身RMS/max2.478/2.972mm、0.356/0.491deg，rotation span38.792deg，全部门通过且未重新拟合外参。关闭当前安装hand-eye checkbox；物理TCP仍未完成。证据：`Agent/evidence/P5/2026-08-09-handeye-postdeploy-holdout-final.{md,json}`。

- 2026-08-09：用户授权部署后 independent multi-pose hand-eye residual。disabled marker preflight 为30/31、center std最大0.0253px；当前 disabled joint4 比旧 reference偏0.025941rad且 MoveIt 报link5-link2约0.425mm模型重叠，故先10秒受控回旧collision-free reference。首个新holdout成功，稳定窗口通过，残差2.837mm/0.463deg；去第二点时joint6 raw 0.315018>0.30触发取消。系统保持健康enabled hold，无突然失能；operator recovery两段共10025 samples、无guard stop，enabled回位max error0.001907rad后才disable，最终status0/no error/tty free。剩余两点改线为从reference进入joint6=+0.21簇且每段30秒，MoveIt303/303 valid；实际重试待新授权。证据：`Agent/evidence/P5/2026-08-09-handeye-postdeploy-holdout-interrupted.md`。

- 2026-08-09：用户授权部署通过离线门的 SUBPIX DANIILIDIS hand-eye 候选。旧配置已备份；active/source 与 install 配置 SHA-256 均为 `9a43b7086...`。官方 `tools/run_ubuntu_vision.sh` 在隔离 ROS domain 成功连接 Gemini 2、运行 YOLO、发布 RGB/depth/CameraInfo/detections，并持续发布新 `end_link -> camera_depth_frame` TF。active aligned pipeline 使用 color coordinates/intrinsics，故保留 `camera_depth_frame` 作为 3D working-frame compatibility name，不重复施加 factory color/depth extrinsic。full `584 passed, 14 skipped`、layering 18、compile/diff通过；未启动机械臂或占用串口。下一步仍需新授权做部署后真实 TCP residual，checkbox暂不关闭。证据：`Agent/evidence/P5/2026-08-09-handeye-subpix-deployment.md`。

- 2026-08-09：用户授权完整 SUBPIX hand-eye 重采。新同固定 cohort 为 12 training + 3 holdout、15/15 captures，旧 NONE 数据未混入；三轮共 23/23 motion legs成功、8,777 samples、无guard stop，逐轮均回reference max error `0.001907/0.000763/0.000763 rad` 后才disable，最终status0/no error/tty free。DANIILIDIS training position/rotation RMS `2.267 mm/0.496 deg`，independent holdout `2.088 mm/0.369 deg`，holdout rotation span `38.905 deg`，全部通过门限；四种非离群方法候选spread `0.487 mm/0.488 deg`。候选外参未部署，`handeye.yaml`未改；下一步需新授权部署并做真实TCP residual。证据：`Agent/evidence/P5/2026-08-09-handeye-subpix-final.{md,json}`。

- 2026-08-09：用户确认旧 6 姿态期间标定纸确有移动并授权补采。6姿态主轮的8/8 legs和同目标retry的3/3 legs均成功、无guard stop；`train_04f` stability失败帧未混入，8秒settle后`train_04f2` 30/30通过。两轮均回reference max error `0.000763 rad` 后才disable，最终status0/no error/tty free。同固定数据集12+3仍因rotation/holdout gates失败；SUBPIX静态90帧A/B显著改善角点姿态稳定性，代码已修正但未再运动。证据：`Agent/evidence/P5/2026-08-09-handeye-recalibration-final-fixed.{md,json}`。

- 2026-08-09：用户确认 marker 固定并授权剩余 9 姿态；9/9 captures、11/11 motion legs成功，无 guard stop，reference max error `0.001907 rad` 后才 disable，最终 status0/no error/tty free。离线诊断发现累计 15/15 跨越 marker 最后一次重新放置/固定边界，固定后解反算旧 cohort rotation residual为 `6.630/7.277 deg RMS/max`，不能部署。固定后 cohort为 8 training + 1 holdout；同固定状态补采旧 6 姿态的软件路线已 MoveIt `707/707` valid，等待新实机授权。完整测试 `581 passed, 15 skipped`、layering `18 passed`、compile/diff通过。证据：`Agent/evidence/P5/2026-08-09-handeye-recalibration-complete-dataset.{md,json}`。

- 2026-08-09：用户授权 12-pose resume 后分两轮执行，新增 accepted `train_02r/train_03r/train_04`；累计 `6/15`。`holdout_03/train_05` capture stability 三次失败被拒绝；去 holdout_03/train_06 分别被 joint6 raw `0.315018/0.344322 rad/s` immediate gate取消。失败后从健康 enabled hold受控回 reference，最终两次成功 recovery 为 `3010/3009` samples、无 guard stop，失能前 max error `0.001144/0.000382 rad`，验证后才 disable。最终 controller/tty clean，handeye未改。证据：`Agent/evidence/P5/2026-08-09-handeye-recalibration-resume-interrupted.md`。

- 2026-08-09：完成 12-pose hand-eye resume software plan。复用三组 accepted captures、排除所有 rejected train_02 frames；所有待采 targets 均 joint5>=0。reference→12 poses→reference 的 13 legs × 101 states 为 MoveIt `1313/1313` valid；合并后 rotation condition `3.930`、translation span `107.84 mm`、worst FOV margin `50.6 px`。runner 支持 resume plan，并把单 pose capture failure 记为不完整数据后继续安全路线；未启动真实 controller/enable/motion。证据：`Agent/evidence/P5/2026-08-09-handeye-recalibration-resume-software-plan.md`。

- 2026-08-09：用户授权 15-pose hand-eye 重标定；disabled preflight 与前三个 pose capture 通过，`train_02` 因真实 hold stability 不足被三次拒绝。自动 recovery return 被 joint6 单次 raw velocity gate 取消且保持 enabled，随后 30 s operator recovery `SUCCEEDED/error_code=0`、3010 samples、无 guard stop；失能前 max reference error `0.001144 rad`，最终 IDLE/disabled/status0/no error，controller/tty 清理。旧 hand-eye 未修改，数据集不完整。证据：`Agent/evidence/P5/2026-08-09-handeye-recalibration-interrupted.md`。

- 2026-08-09：完成 15-pose hand-eye recalibration 纯软件方案和专用 runner。1,430 candidates 中选择 12 training + 3 holdout；worst robust FOV margin `50.6 px`，rotation-vector condition `3.938`，translation span `118.59 mm`。16 legs × 101 states 为 MoveIt `1616/1616` valid。新增 2 s settle、30-frame center/joint/PnP stability gate、三次 retry 和 recoverable failure controlled return。focused `28 passed`、compile/diff 通过；未启动 controller/enable/运动。证据：`Agent/evidence/P5/2026-08-09-handeye-recalibration-software-plan.md`、`.json`。
- 2026-08-09：完成替代第六姿态实机采集与六姿态 residual。marker preflight/candidate 均 `30/30`；20 s outbound/return action 均成功、无 guard stop；失能前 reference baseline max error `0.001144 rad`，验证后才 disable，最终六轴 status 0、无 error、串口释放。diversity `69.046 mm/22.191 deg` 通过，但两种 frame hypothesis position RMS/max 均约 `16/32 mm`，acceptance FAIL。五种 AX=XB 离线解不稳定且 cross-validation 失败，未改 `handeye.yaml`。证据：`Agent/evidence/P5/2026-08-09-handeye-multipose-residual.{md,json}`。
- 2026-08-09：完成 P5 hand-eye replacement pose 纯软件设计。旧 pose_5 在跨实测 anchors 预测中 marker 顶边最低 `-18.6 px`，解释实际 `0/45`。新 candidate 只在 pose_4 基础上把 joint6 改为 `+0.28 rad`，10-way worst FOV margin `71.7 px`，rotation span `22.12 deg`；真实 marker 当前只读 `30/31`。临时 hand-eye baseline 起点因 4 个 link5:link2 invalid 被拒绝；post-disable observed start -> candidate -> reference baseline 改为 `202/202` MoveIt valid。新增 aggregate extractor、4.7 KiB durable five-pose JSON 和 replacement runner；focused `35 passed`、compile/diff 通过。未启动 controller、未 enable/运动。证据：`Agent/evidence/P5/2026-08-09-handeye-replacement-pose-design.md`。
- 2026-08-09：用户单独授权从下坠后的 disabled 姿态恢复本轮 hand-eye baseline。disabled start `[-1.640154,-0.005913,-0.016212,-0.007820,+0.058175,-0.102427] rad`，六轴 status 0、无 error；最大 delta `0.109102 rad`，20 s quintic theoretical peak `0.010228 rad/s`。20 s return 与 2 s baseline hold 两段 action 均成功、无 guard stop；失能前最大回位误差 `0.000381 rad`，验证后才 disable。最终 IDLE/disabled、六轴 status 0、无 error；controller 与串口释放。证据：`Agent/evidence/P5/2026-08-09-handeye-six-pose-interrupted.md`。
- 2026-08-09：已把“健康真机不可在非 baseline 突然失能”固化为项目硬规则：更新 `AGENTS.md`、`Agent/README.md`、`Agent/EXECUTION_FLOW.md`、`Agent/MEMORY.md`；新增 `rebotarm_motion.real_failure_recovery`，hand-eye 与 paired runner 共用。策略覆盖 recoverable failure 受控回位后 disable、return failure 保持 enabled hold、critical status 才 protective disable。focused `37 passed`，完整仓库 `570 passed, 15 skipped`；compile/diff 通过，未再访问硬件，controller 与串口已清理。证据：`Agent/evidence/P5/2026-08-09-handeye-six-pose-interrupted.md`。
- 2026-08-09：hand-eye 六姿态实采前五段 action 均成功且无 trajectory guard stop；baseline/pose_1/pose_2/safe/pose_4 detection 分别为 `30/31、30/30、30/30、30/30、30/30`。pose_5 检测为 `0/45` 后，旧通用异常路径错误 stop + disable，未受控返回 baseline，用户指出执行器下坠与既定要求冲突。当前 disabled 反馈约 `[-1.64015,-0.00591,-0.01621,-0.00896,+0.05856,-0.10281] rad`，六轴 status 0、无 error。该结果不计 hand-eye 通过；已开始修复为 recoverable failure 受控回位/健康 hold，并增加回归测试。
- 2026-08-09：用户授权六姿态 hand-eye residual 后，先执行专用 runner 的 disabled-only 前置门。控制器全程 `enabled=false/control_loop_active=false`、六轴 status 0、无 error；marker capture 为 `0/45`，当前 RGB 抓图中没有标定纸，因此 fail closed，service 调用为空、未 enable、未发送 trajectory。控制器随后退出，`/dev/ttyACM0` 释放。聚焦 ArUco/residual/guard/layering 测试 `33 passed`；新增 OpenCV 新旧 ArUco API 兼容与 `--preflight-only` 模式。证据追加：`Agent/evidence/P5/2026-08-09-handeye-residual-target-preflight.md`。
- 2026-08-09：用户调整标定纸后，Gemini 2 对 `DICT_4X4_50 id=0` 连续 89/89 帧检出；marker 完整，area约 `23,399 px²`，center std `0.112/0.122 px`。使用 100 mm 边长与 color CameraInfo 的 IPPE square PnP 得到约 `333 mm` 距离，translation std 最大 `0.392 mm`、span最大 `0.875 mm`。单姿态 detection stability 通过；未启动 controller 或运动，尚不构成 hand-eye 多姿态 residual。证据：`Agent/evidence/P5/2026-08-09-handeye-residual-target-preflight.md`。
- 2026-08-09：启动 P5 hand-eye 多姿态 residual 验收。Gemini 2 `AY6V16300BP` 只读采集正常；common ArUco/AprilTag 字典 28 帧无真实目标，`DICT_ARUCO_ORIGINAL id=0` 的 2 次结果经 120 帧最佳图像复核为远处货架 false positive。SDK factory extrinsics 显示 color/depth optical center translation 约 14 mm，RGB marker PnP 不能未经变换直接宣称属于 `camera_depth_frame`。新增独立 `rebotarm_calibration` residual 核心/CLI、姿态覆盖与 acceptance gate；未启动 controller、未 enable/运动。证据：`Agent/evidence/P5/2026-08-09-handeye-residual-target-preflight.md`。
- 2026-08-09：用户授权并确认系统上电后，完成 P5 safe-posture paired sim-to-real 采集。disabled preflight 为唯一真实 joint-state publisher、六轴 status 0、无 error；从实时 baseline `[-1.55585,0.00553,-0.01163,0.00973,0.08144,0.00668] rad` 生成 401 点/20 秒/0.05 秒 cadence 的 exact outbound/return command。active `rebotarm_mujoco_node` 与真机四段 action 全部成功、无 guard stop，真机只在回位最大误差 `0.000763 rad` 后 disable，最终 status 全 0、串口释放。joint4 real tracking RMS 去/回为 `0.004346/0.004200 rad`，real-sim position RMS 为 `0.004316/0.004092 rad`；effort 趋势不一致且 measurement semantics 未标定，contact raw 不可用，因此只关闭 paired raw 缺口，不关闭 P5 geometry/dynamics/contact checkbox。证据：`Agent/evidence/P5/2026-08-09-mujoco-real-trajectory-comparison.{md,json}`。
- 2026-08-09：完成 P5 paired sim-to-real 只读审计。active `rebotarm_mujoco_node` 可通过 `FollowJointTrajectory` 接收统一 joint command，发布带 simulation-time stamp 的 joint position/velocity/MuJoCo `actuator_force`，action feedback 含 desired/actual/error；但 active path 当前没有 raw metrics 落盘器。P1 comparison JSON 和 P5 safe-posture/pre-grasp evidence 都只保留 summary/final/peak fields，没有 time-aligned raw series；`/tmp` 已无相关 runner/plan/sample 文件。20 秒 safe-posture 的 quintic 算法与 0.05 秒 cadence 可由源码重建，但历史 serialized trajectory 与 raw samples 未持久化，不能冒充严格 paired data。用户再次确认旧 current baseline 已废弃，新对比仅使用 active upstream node。严格 paired comparison 因此需要新采一次真机 raw trajectory，执行前必须另行明确授权。
- 2026-08-09：交接复核确认 overall `85.8%`、active phase 仍为 P5。MuJoCo model/health/headless/action/step-response 与 P1 upstream/current comparison 已存在，真机 single-joint/safe-posture/pre-grasp 也有汇总 metrics；但没有同一 command 的 time-aligned MuJoCo/real raw sample pair，不能宣称 sim-to-real calibration 已完成。用户决定下一步先建立 P5 paired sim-to-real protocol/report，再考虑 approach；本轮未启动任何硬件或 runtime。
- 2026-08-08：用户明确接受原瓶位约 `99 deg` joint2 大动作后，纠正 MoveIt `use_fake_joint_states=false` 并确认唯一真实 joint-state publisher。fresh bottle plan 在 final gate age `0.980 s` 通过，实际最大 joint2 delta `1.61765 rad/92.68 deg`。去程/回程各 45 秒 action 均 `SUCCEEDED/error_code=0`，5336/5318 samples 无 guard trigger；pre-grasp 最大误差 `0.005083 rad`，回位最大误差 `0.001526 rad`。回位确认后才 disable，约 15 秒后 joint4 仅 `+0.000763 rad` 变化；最终六轴 status 0、IDLE，现场、8081 和串口已清理。P6 pre-grasp 子门关闭。证据：`Agent/evidence/P5/2026-08-08-bottle-pregrasp-roundtrip-pass.md`。
- 2026-08-08：瓶子移近并换瓶后完成 corrected real-state plan-only 复测。首次 MoveIt 漏设 `use_fake_joint_states=false` 造成 fake/real joint state 混流，相关输出已全部作废；纠正后 publisher count=1 且真实 disabled state为 `[-1.6062,0.00553,-0.01316,-0.00591,0.05703,0.05589] rad`。新 bottle bbox depth median `241 mm`、YOLO confidence median `0.3445`；默认 top-confidence 选择被 umbrella 抢占。bottle-only relay 20 秒 `298/0` candidate/non-empty frames，无 bottle plan，未 enable/执行。现场、8081、串口已清理。证据追加：`Agent/evidence/P5/2026-08-08-bottle-pregrasp-execution-gated.md`。
- 2026-08-08：用户授权一次 bottle pre-grasp → enabled hold → return baseline → disable 后，执行前安全门发现当前 fresh plan endpoint 为 `[-1.329,-1.734,-1.123,-0.624,1.022,-0.376] rad`，相对 baseline 最大 joint2 delta `1.729 rad/99.0 deg`，超过 `1.2 rad` 限制。runner 未发送任何 trajectory；一次 enabled hold 等待 fresh plan 超时后 stop + disable，最终位置等于 baseline、六轴 status 0。`max_candidates_per_frame=1` 将 valid plan age 改善到 `0.827–1.032 s`，但 40 秒 disabled-only 搜索仍无小于 1.2 rad 的方案。现场、8081 和串口已清理。证据：`Agent/evidence/P5/2026-08-08-bottle-pregrasp-execution-gated.md`。
- 2026-08-08：用户摆放瓶子后 P6 pre-grasp 真实视觉/规划前置门通过。6 秒 bbox probe 的 bottle YOLO confidence/depth median 为 `0.7216 / 334 mm`；20 秒为 36 candidate frames、170 candidates、20 filtered plans，其中 5 条 valid。fresh bottle pre-grasp 调用 MoveIt 返回 `SUCCESS(1)`、65 points、57.7 ms；MuJoCo max delta `1.47e-7 rad`，hardware action server 0，未开串口/执行。现场和8081已清理。证据：`Agent/evidence/P5/2026-08-08-bottle-pregrasp-plan-only-pass.md`。
- 2026-08-08：P6 pre-grasp 当前场景只读预检 fail closed。真实 Gemini 2/YOLO/local GraspNet 33 帧中 32 帧 nonempty、190 candidates，但 20/20 filtered plans 无 IK；根因为左上 umbrella约705 mm且转换后超出 `-Y` workspace，当前无近距独立目标。MuJoCo 对齐真实 disabled state后 probe max delta `1.45e-6 rad`；未打开机械臂串口，现场/8081已清理。证据：`Agent/evidence/P5/2026-08-08-pregrasp-current-scene-blocked.md`。
- 2026-08-08：safe-posture 完整低速往返回位通过。`to_safe_posture` 与 `return_to_baseline` 两段 20 秒 action 均 `SUCCEEDED/error_code=0`，分别 1740/1781 samples、无 guard trigger；safe-posture 最大误差 `0.00297 rad`，回位最大误差 `0.000763 rad`。只在回到已知 baseline 后 disable，最终六轴 status 0、IDLE、串口释放。正式通过前两次启动异常均 fail closed 并留证。证据：`Agent/evidence/P5/2026-08-08-safe-posture-roundtrip-pass.md`。
- 2026-08-08：用户授权的新 window persistence policy 下，20 秒 safe-posture trajectory `SUCCEEDED/error_code=0`，2108 motion samples 无 guard trigger；joint4 effort/tracking peaks `2.3565 N.m / 0.01089 rad`。但验收 client 成功后直接 disable，Joint4 失去 holding torque 后下坠到 `0.27142 rad`；用户指出该 teardown 不可接受。故只确认 trajectory/guard 通过，safe-posture 完整操作门不关闭。最终六轴 status 0、IDLE、串口释放。证据：`Agent/evidence/P5/2026-08-08-safe-posture-window-persistence-pass.md`。
- 2026-08-08：multi-signal guard safe-posture retry在 joint6 window `0.054865 > 0.05` 时安全取消。742 samples中 joint4 torque/tracking peaks为 `0.76923 N.m / 0.006825 rad`，六轴无 saturation/status error；最终 disabled、串口释放。随后 window gate改为 warning/sustained/immediate三级，focused 32、full `558 passed, 14 skipped`。证据：`Agent/evidence/P5/2026-08-08-safe-posture-multisignal-retry.md`。
- 2026-08-08：根据用户对 joint4 torque 的提醒完成控制策略复核。本地/上游均为 joint4-6 4310、URDF effort `7 N.m`，当前 pose Pinocchio gravity为 joint4 `-2.0708 N.m`；末端负载显著但没有 saturation evidence。确认低 vlim不是 torque修复，建议先做 governor/telemetry/gate重构，再以正反向 joint4试验区分 capacity 与 PI/deadband/friction。证据：`Agent/evidence/P5/2026-08-08-joint4-control-strategy-review.md`。
- 2026-08-08：完成 joint4 raw spike 无硬件审计。ROS trajectory point cadence 为 50 ms，底层 control loop requested 500 Hz，但 POS_VEL 始终发送 config vlim joint1-3=`5.0`、joint4-6=`3.0 rad/s`，不随 trajectory duration/segment velocity变化。离线 quantization 交叉检查确认 early joint4 target increment 低于一个 position LSB，观测 jump/raw 均为协议格点；建议 per-trajectory vlim override，不提高 `0.07` raw gate。证据：`Agent/evidence/P5/2026-08-08-joint4-raw-spike-software-audit.md`。
- 2026-08-08：用户授权 raw/window `0.07/0.05 rad/s` 重试 20 秒 safe-posture。第一次因 enable 后 freshness preflight 在 goal 前退出且无运动；第二次等待 3 个 fresh samples 后 goal accepted，joint4 raw `0.09523773` 触发取消，raw peak `0.15384674`，window peak `0.01879278`，max jump `0.00152588`，324 motion samples。最终 disabled/IDLE、六轴 status 0、串口释放；safe-posture 不通过。证据：`Agent/evidence/P5/2026-08-08-safe-posture-raw-007-retry.md`。
- 2026-08-08：实现/验证 model-aware velocity gate policy。4340P joint1-3 的 half-LSB raw limit 为 `0.052442`，4310 joint4-6 为 `0.057326`；按用户决策 raw floor 统一设 `0.07`，position-window gate 保持 `0.05`。测试覆盖 `0.051283` boundary、下一 quantized code 与 `0.070001` rejection，focused `26 passed`；未访问硬件。证据：`Agent/evidence/P5/2026-08-08-model-aware-velocity-gates.md`。
- 2026-08-08：20 秒六轴 safe-posture 实机尝试在 raw velocity `0.051283 rad/s` 时 fail closed；action canceled、disable success。disabled final 相对 baseline 为 `[+0.00076294,-0.00190735,-0.00114441,+0.00724792,-0.00038147,0] rad`，六轴 status 0、无 errors。rebuilt runtime 无 cleanup race warning，串口释放。由于 monitor failure path 缺 per-joint peaks，本轮不算通过且不调整 threshold。证据：`Agent/evidence/P5/2026-08-08-safe-posture-execution-interrupted.md`。
- 2026-08-08：完成 safe-posture 无硬件预检。MoveIt 以真实 disabled final state 作为 explicit start，RRTConnect 返回 `SUCCESS(1)`、27 points；`/check_state_validity` 对 production direct interpolation 的 101 个均匀采样全部 valid、无 self-collision。4 秒默认 profile 对 joint4 理论峰值 `0.09456 rad/s`，不沿用；拟议 20 秒 profile 峰值 `0.018911 rad/s`。隔离 MoveIt 已退出，未访问串口。证据：`Agent/evidence/P5/2026-08-08-safe-posture-readonly-preflight.md`。
- 2026-08-08：使用确认加载 `motor_lifecycle_lock` 的 rebuilt overlay 完成 joint1 降速往返。101-point/5 秒 trajectory 正向 `+0.02 rad` 后返回，action `SUCCESSFUL(0)`；255 valid samples，raw/window velocity peak `0.03662968/0.01801461 rad/s`，other-joint delta `0.00038147 rad`，final joint1 error `0.00114441 rad`。自动 disable 后六轴 status 0、无 errors，driver 退出且串口释放。P6 single-joint sub-gate 通过，整项 staged hardware acceptance 仍未完成。证据：`Agent/evidence/P5/2026-08-08-joint1-micro-move-slow-pass.md`。
- 2026-08-08：用户授权 joint1 微动重试。两次 monitor preflight 因 QoS/status event 设计在 enable 前退出；第三次执行 61-point/3 秒轨迹，raw feedback `0.051281929 > 0.05 rad/s` 后 action `CANCELED`，final disable 成功。joint1 observed range `0..+0.01983643 rad`，其他关节无偏移，最终相对 baseline `+0.00953674 rad`。本轮还发现 runtime overlay 为 stale copy-install；driver 退出并释放串口后已 symlink rebuild，import 确认新 lock code，focused `37 passed`，未再次实机执行。证据：`Agent/evidence/P5/2026-08-08-joint1-micro-move-retry.md`。
- 2026-08-08：完成 `FollowJointTrajectory` cancel/disable race 纯软件修复。新增 motor lifecycle `RLock` 串行化 `enable/disable/stop/get_joint_state`，并以 event-controlled threads 强制重放 transitional status window；concurrent stop 和 joint-state read 均会等待 disable 完成后无异常返回。focused `108 passed`、完整仓库 `544 passed, 14 skipped`、required compileall 与 `git diff --check` 通过；未访问串口或执行硬件动作。证据：`Agent/evidence/P5/2026-08-08-follow-trajectory-cancel-disable-race-fix.md`。
- 2026-08-08：joint1 低速单关节首次尝试被外部干扰并 fail closed。61-point/3 秒轨迹理论峰值 `0.0375 rad/s`；反馈超过 `0.05` 后 client cancel + final disable。用户说明期间手动移动，故不归因 controller，也不计验收。最终 joint1 `-1.5642404556 rad`（相对 baseline `+0.0087738037 rad`），其余五轴不变，六轴 status 0、串口释放。日志同时证明 cancel/disable race 可导致 action callback 未处理 `status_code=1, expected 0`；需先修 cleanup path 和 tests，再申请重试。证据：`Agent/evidence/P5/2026-08-08-joint1-micro-move-interrupted.md`。
- 2026-08-08：用户确认工作空间安全后完成 joint2 新零位/limit 的 5 秒 enable-hold regression。P0 Gate B/C 工具显式 enable、当前位置 hold、自动 disable；119 samples，max position jump `0.0003814697 rad`、max velocity `0.0073261261 rad/s`，最终位置等于 baseline，六轴 status 0、error_codes 空。状态转换附近的 `-12.467574 rad` 固定边界帧和 status=1/expected0 日志被 hardware guard fail closed；未发 trajectory/gripper/safe-home。driver/串口已清理，任务外既有 vision/TF 进程未触碰。证据：`Agent/evidence/P5/2026-08-08-joint2-enable-hold-regression.md`、`Agent/evidence/P5/gate-bc-20260808-202559.json`。
- 2026-08-08：完成 joint2 零位覆盖与 `+0.02 rad` software upper margin。只注册/disable joint2，写入前后各 10 帧均为 `-0.0001907349 rad`、status 0；未 enable、未 store parameters。hardware/两份 URDF/MuJoCo profile/asset/generated model 与 tests 已同步。focused 132、full suite `542 passed, 14 skipped`、fresh MuJoCo execute-loop 7、MuJoCo model/limit/headless 16、layering 18、compileall、四包 colcon build、MJCF check 和 diff check 均通过。标准 driver 实机复核 `CONNECTED_DISABLED`、六轴 status 0；现场和串口已清理。证据：`Agent/evidence/P5/2026-08-08-joint2-zero-margin.md`。
- 2026-08-08：校零后真实 disabled start-state plan-only 通过。真实 joint state `[-1.5482187271, -0.0001907349, -0.0123977661, 0.0017166138, 0.0295639038, 0.0829706192]` 直接通过 MoveIt bounds；真实 Gemini 2/YOLO/GraspNet 候选 pre-grasp `[0.053028, -0.418949, 0.227397] m` 规划返回 `SUCCESS(1)`、62 points、56.793 ms wall。规划后 2.048 秒最大 joint delta `0.0 rad`，hardware trajectory action server 为 0，未启动 executor/发送 goal；最终保护性 disable 后仍为 IDLE、六轴 status 0。现场、8081 和串口已清理。证据：`Agent/evidence/P5/2026-08-08-real-disabled-start-state-plan-only.md`。
- 2026-08-08：按用户确认对 joint2 做 disabled-only 零位校准。只注册 Damiao `0x02/0x12`，10 帧写入前 status=0、位置稳定；一次 `set_zero_position()` 后 joint2=`-0.0001907349 rad`、10/10 status=0。没有 enable、store parameters 或运动。标准 driver 随后成功进入 `CONNECTED_DISABLED`，六轴 status 全 0、error_codes 为空；退出后串口释放。该 zero 不跨断电保留，当前距上限也仅 0.00019 rad，后续仍需关注边界量化/回差。证据：`Agent/evidence/P5/2026-08-08-joint2-zero-calibration.md`。
- 2026-08-08：按用户确认将当前安装视觉 task-space 从 upstream `+X` 绕 base Z 旋转 `-90 deg` 到 `-Y`，未改 hand-eye/TCP/camera calibration。真实 Gemini 2/GraspNet 10 秒为 16/16 nonempty、38 candidates、7 valid filtered plans，解除此前负 Y 候选整体不可达问题。直接 raw start planning 仍因 joint2=`+0.003623962 rad` 越上限失败；纯软件归一 joint2=`0` 后 MoveIt plan-only 成功（106 points、48.853 ms wall），只证明几何链路。focused `71 passed`、layering `18 passed`、compileall 和两包 colcon build 通过；未 enable/执行，现场、8081 和串口已清理。证据：`Agent/evidence/P5/2026-08-08-y-negative-installation-workspace.md`。
- 2026-08-08：用户确认机械臂处于物理安全位置后，保持标准 driver soft-limit fail-closed，改用 disabled raw snapshot 做纯软件 start-state 诊断。真实 Gemini 2/YOLO/GraspNet 正常，12 秒 23 条 candidates、22 条非空；但当前 camera TF 朝向使目标位于 base 负 Y 外侧，12/12 filtered plans 均为 `no IK-reachable grasp candidates`，因此未调用 planning service。hardware trajectory action server 为 0，未执行；全部进程、8081 和串口已释放。证据：`Agent/evidence/P5/2026-08-08-real-disabled-plan-only-preflight.md`。
- 2026-08-08：实机 disabled joint-state + plan-only 预检被 hardware soft limit 正确拦截。`/dev/ttyACM0` 无原占用，底层 20 组只读反馈确认 joint1-6/gripper status 全为 0；joint2 稳定 `+0.008964539 rad`，超过 `[-3.14, 0]` 上限约 `0.51 deg`，不是单帧噪声。失败和复核退出均保持/执行保护性 disable，未启动 MoveIt、未发送轨迹，串口已释放。证据：`Agent/evidence/P5/2026-08-08-real-disabled-plan-only-preflight.md`。
- 2026-08-08：P5 第二检查点完成：真实 Gemini 2 + YOLO + local GraspNet 候选经当前 hand-eye/TCP TF、MoveIt IK/collision filter 形成 `base_link` 有效 plan；显式 velocity/acceleration scaling `0.1` 后 `/plan_kinematic_path` 返回 `SUCCESS(1)`、85 points、planning time `54.1 ms`。初次 Ruckig `-100` 已定位为手工 request 漏填 scaling，并非 calibration/IK 问题。MuJoCo 5 秒 251 samples 最大 joint delta `2.77e-17 rad`，trajectory action 0 clients，未启动 executor、未打开串口。现场相关进程和 8081 端口已清理。证据：`Agent/evidence/P5/2026-08-08-real-vision-mujoco-plan-only.md`。
- 2026-08-08：P5 只读 calibration audit 完成第一检查点：源码/安装/上游 hand-eye 文件一致、quaternion norm 正常，ROS 2 static TF runtime 成功发布当前 hand-eye 与 `grasp_tcp=-40 mm`，组合 TF 可查询。实时上游核对确认 `main@fb28dcdd...` 实际值就是当前值，README 中相差 `94.6 mm / 105.6 deg` 的另一组值为 stale documentation；TCP 仅与 finger STL 中心近似。MuJoCo motor/dynamics/contact config 与固定上游一致，但 fingertip force 等明确未实测。focused 正常顺序 `22 passed, 5 skipped`；未连接相机或机械臂。证据：`Agent/evidence/P5/2026-08-08-calibration-readonly-audit.md`。
- 2026-08-08：P4 7/7 关闭。官方代码/checkpoint 与 CUDA 12.8 toolchain 本地化，PointNet2 GPU smoke 通过；真实 Gemini 2 六帧为 `8–10` candidates/frame，best xyz 最大偏差 `18.08 mm`。localhost stale 为 HTTP 409/空结果，zero-depth 和 TF failure 均 fail closed。最终 ROS 2 probe 为 29 条、timestamp 全非零且严格递增、frame_id 正确、freshness 最大 `686.5 ms`、11 条非空且无 synchronization rejection。focused `22 passed, 1 skipped`，layering `18 passed`，全量 `541 passed, 14 skipped`，compileall、diff check 与隔离 colcon build 通过；测试进程已停止，未启动机械臂。证据：`Agent/evidence/P4/2026-08-08-graspnet-real-inference.md`。

- 2026-08-07：P1 upstream-only 收尾通过：active setup/launch 不再暴露 current selector/adapter/legacy wrapper；全量 `516 passed, 13 skipped`，MuJoCo/upstream 专项 `62 passed, 7 skipped`，layering 18 passed，compileall 与三包 colcon build 通过；health 为 8 joints/8 actuators、Viewer smoke 正常启动、独立 ROS domain 六轴轨迹 `error_code=0 / SUCCEEDED`。自动抓取序列仍按规划属于 P6 闭环，不作为 P1 完成声明。上游授权仍为 `PROVISIONAL`，只允许本地使用、不发布。
- 2026-08-07：完成 P2 Gemini 2 只读设备/SDK 预检；未打开机械臂串口，未调用任何 enable、trajectory、gripper 或 safe-home。设备 metadata、profiles、标定参数、depth scale 和两组 pipeline 行为已写入 P2 evidence；P2 checkbox 暂不勾选。
- 2026-08-07：完成 P2 SDK/ROS 实现检查点：修正 Ubuntu stream pair、profile fail-closed、depth scale、SDK metadata、双 CameraInfo 和 SDK timestamps；真实 driver `120/120` 同时出帧，ROS camera-only topics/CameraInfo/header 通过，完整测试 `523 passed, 13 skipped`，layering 18 passed，colcon 2 packages。P2 更新为 6/8；未做已知距离误差或 pixel-level alignment 最终验收。
- 2026-08-07：用户无法因固定歪装提供精确相机到目标距离，仅确认约 300 mm；采集 240 组 provisional paired samples，center ROI depth 恒定 median 310 mm、MAD 1 mm、valid 100%，plane fit tilt 4.75 deg、residual median/P95 0.30/0.83 mm。单一目标右边缘 RGB/depth peak offset 8 px，受 baseline occlusion 影响，不能据此判定标定错误或 alignment 通过；P2 保持 6/8。证据：`Agent/evidence/P2/2026-08-07-gemini2-approx-300mm-plane.md`。
- 2026-08-07：完成遮挡板移除前后差分：前后各 60 paired frames，主要 ROI 的 RGB/depth change-mask IoU 为 0.8042，depth change 位于 RGB change 内 92.77%，支持 gross correspondence；启发式边缘 median offset 为 +14 px，但受 13.81 mm sensor baseline 的 occlusion/depth shadow 影响，不能作为 calibration error。采集时一次 SDK timestamp warning 未在随后 180/180 连续单调帧复测中复现；P2 仍为 6/8，不触发重新标定。证据：`Agent/evidence/P2/2026-08-07-gemini2-obstruction-removal-differential.md`。
- 2026-08-07：用户继续清理左侧场景后复测 120/120 paired frames；全图 persistent valid depth 提升至 80.43%，center ROI valid 99.88% 且无 `<350 mm` 遮挡，下方中央桌面 ROI valid 99.27%、plane residual median/P95 0.54/1.66 mm。剩余近距离物只在画面底边，不进入候选作业 ROI；可继续 camera-only/P3 检查，但没有已知 ground truth，P2 仍为 6/8。证据追加至 `Agent/evidence/P2/2026-08-07-gemini2-obstruction-removal-differential.md`。
- 2026-08-07：按用户指定完成 Gemini 2 两分钟 short soak：120.029 秒内 `3594/3594` paired（约 29.94 FPS），无 partial/empty/shape mismatch、frame-index skip、timestamp 回退或新 SDK warning；center ROI 单帧 valid ratio min/median 为 91.48%/93.60%，device timestamp delta median/max 为 13074/13133 us。该结果只证明短时稳定，不替代 pixel-level 或已知距离验收；P2 仍为 6/8。证据：`Agent/evidence/P2/2026-08-07-gemini2-2min-short-soak.md`。
- 2026-08-07：用户确认同一 Gemini 2 原先已完成测试/calibration 且使用无问题，明确批准沿用既有结果并关闭最后两项。结合当前 serial/profile/SDK metadata/depth scale/ROS CameraInfo、gross alignment、约 300 mm 稳定性和 3594/3594 short-soak 证据，P2 以 8/8 关闭；本轮未补造 pixel-level ground truth 或多距离 bias/RMSE，作为 acceptance waiver 保留，active phase 转 P3。证据：`Agent/evidence/P2/2026-08-07-p2-closeout-accepted-calibration.md`。
- 2026-08-07：开始 P3 并完成第一轮 native preflight：新增 `vision_profile` routing、camera fail-closed empty detections、network stale payload fail closed 和 `max_plan_age_sec` executor gate；focused `13 passed`、全量 `528 passed, 13 skipped`、layering `18 passed`、colcon 三包 build 通过。native camera-only 与主 bringup `vision_profile:=ubuntu_native` 均启动并发布 RGB/depth/CameraInfo/detections/annotated/tf_static；非法 profile 复测 fail closed。raw Image consumer drop 仍阻塞 P3 timestamp/frame/freshness 最后一项。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：完成 upstream 默认 backend 切换：原始快照测试为 `219 passed, 1 skipped, 1 failed`，临时 test-only 断言兼容副本为 `220 passed, 1 skipped`；health/headless 通过；默认 `mujoco_moveit_sim.launch.py` 启动 upstream ROS node、`/clock`、joint states、trajectory action 和 gripper services；1 秒六关节轨迹返回 `error_code=0`。current adapter 仅在 `simulation_backend:=current` 时启动，非法 backend fail closed。退出时 MuJoCo process clean，但两条 backend 的 `move_group` 都超过 5 秒 SIGINT grace 后被 SIGTERM，记录为非阻塞 MoveIt cleanup 问题。证据：`Agent/evidence/P1/2026-08-07-upstream-default-backend.{md,json}`。
- 2026-08-06：P1 授权风险修复：新增 package-owned MJCF baseline/grasp scene，默认 mesh 只解析到本仓库 `rebotarm_bringup`；HJX `NOASSERTION` 来源退出默认运行路径。新模型 `nq=8`、`nv=8`、`nu=7`，1 秒 robot smoke 与 grasp scene 均 finite。
- 2026-08-06：P1 环境风险修复：直接依赖全部固定，NumPy 降至 1.26.4 并补 cffi 1.17.1；`pip check` 无 broken requirements，MuJoCo/NumPy/SciPy/cffi import 通过。
- 2026-08-06：P1 授权/环境修复完整回归：MuJoCo 专项 `36 passed`，仓库完整测试 `486 passed, 3 skipped`，required compileall、`rebotarm_simulation` colcon build、`pip check` 和 diff check 通过。
- 2026-08-06：P1 gap matrix 完成：新增 `docs/mujoco_gap_matrix.md` 与结构测试；只记录 current/upstream 能力差异，不复制 `reBotArm_develop_hjx` 实现或资产。
- 2026-08-06：P1 grasp scene keyframe 完成：scene reset 的 home `qpos/ctrl` 同步测试通过，生成与 1 秒 grasp benchmark 均 finite；`contact_without_lift` 保留为后续接触和执行轨迹标定边界。
- 2026-08-06：P1 规划重构：确认采用 `robotarm_ros2` MuJoCo upstream-first 迁移；新增固定快照、上游独立预检、双基线差异报告和选择性本地化验收项，当前 P1 总项扩展为 16 项。
- 2026-08-06：使用用户提供的 `/home/a/project/rebot_refer` 完成上游独立预检；health/headless 通过，MuJoCo 专项为 `219 passed, 1 skipped, 1 failed`。唯一失败为嵌套 mapping/tuple 的 `pytest.approx` 重放断言，数值最大差约 `7.05e-18`，保留为上游测试缺陷，不宣称全绿。
- 2026-08-07：完成第一个选择性本地化项 health check；新增 finite-state 检查器和 CLI，当前 MuJoCo 专项 `34 passed, 4 skipped`，全量 `489 passed, 4 skipped`。
- 2026-08-07：完成 P3 raw Image transport follow-up audit：`ros2 topic info -v` 确认 raw Image 为 `BEST_EFFORT`；12 秒 probe 中 CameraInfo/detections 约 15 Hz，而 color/depth raw Image 仅 3.25/7.08 Hz，说明 publisher 仍在推进但大 payload 在 consumer/DDS 侧丢帧。最新 CameraInfo/depth/detections freshness 约 0.37–0.39 s，color raw image 1.19 s；frame_id 和静态 TF 有效。未启动机械臂或硬件执行。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：补做 native loop 5 Hz 与 C++ rosbag cross-check；5 Hz 下 raw color/depth 仍仅 1.75/2.75 Hz，11 秒 rosbag2 记录 28/28 帧并报告 60 个 transport-layer lost messages，确认不只是 Python subscriber 限制，问题收敛到 DDS/RMW 或 publisher queue/backpressure。未改默认 QoS/配置。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：隔离测试将 image publisher history depth 临时从 5 提到 30，10.4 秒 C++ rosbag2 仍只收到 color/depth 20/28 并报告 62 个 transport-layer lost messages；增大 history 无效，实验已立即回滚，默认仍为 `qos_profile_sensor_data`。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：Fast DDS transport A/B 仅运行时设置 `FASTDDS_BUILTIN_TRANSPORTS=UDPv4`/`SHM`；相比默认约 28/28 帧和 60 lost，UDPv4 为 15/26 和 75 lost，SHM 为 16/25 和 73 lost，均未改善。未持久化环境或 DDS XML 配置，问题仍在 active `rmw_fastrtps_cpp` 大图 delivery。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：payload isolation 进一步确认：关闭 YOLO/annotated 后 raw-only 5 Hz 仍为 13/24 帧并丢 77，1 Hz 仍为 3/3 并丢 15；独立合成约 0.92 MB `sensor_msgs/Image` 1 Hz 发布 10 帧，rosbag 仅收到 4 并丢 4。问题最可能是 Fast DDS 大 sample 的 middleware/resource/fragmentation handling，不是 Gemini 2、calibration 或 detector。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：C++ publisher 对照改变了根因判断：`rclcpp` 下 1 KB 和 512 KB `BEST_EFFORT` 均 10/10；实际 RGB-size 921600 bytes 在 `BEST_EFFORT` 为 0/10、8 lost，但同尺寸改 `RELIABLE` 后 10/10、0 lost。C++ 本身不是修复点，决定因素是大 sample 的 QoS reliability；下一步应先参数化真实 native raw-image publisher QoS 并以 `RELIABLE` 复测，再决定是否迁移 C++。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：P3 最后一项关闭：vision node 保留 generic `best_effort` 默认并参数化/校验 `ros.image_reliability`，`camera_ubuntu.yaml` 显式使用 `reliable`。真实 Gemini 2 默认 15 Hz 的 11.6 秒 rosbag2 color/depth 为 175/175、0 loss；8 秒 RELIABLE consumer 为 120/120，timestamp 单调、freshness 约 0.42 s，frame_id/TF/CameraInfo/detections 正常。focused 12、layering 18、全量 529 passed/13 skipped、compileall 与 colcon 两包通过；P3 7/7 完成，转入 P4。证据：`Agent/evidence/P3/2026-08-07-ubuntu-native-preflight.md`。
- 2026-08-07：P4 环境与 service contract 子项通过：创建独立 `.venv-graspnet`，固定 CUDA 12.8 PyTorch、Open3D、NumPy/SciPy/OpenCV 等依赖；`torch.cuda.is_available=True`，设备为 NVIDIA GeForce RTX 5060 Laptop GPU，清除 ROS `PYTHONPATH` 后 `pip check` 通过，全部 import 成功。新增 setup/run/health-check 脚本、localhost `/health` 和 contract `1.0`；未配置模型时 health 为 `unconfigured` 且 inference fail closed。focused 8、layering 18、全量 `537 passed, 13 skipped`、compileall 与 diff check 通过。模型 root/checkpoint 未下载，真实候选验收待 provenance/license 审核后继续。证据：`Agent/evidence/P4/2026-08-07-graspnet-environment.md`。
- 2026-08-07：完成 URDF-to-MJCF/model 接口审查；确认 actuator、sensor、scene contract 不同，暂不整体迁移上游模型。
- 2026-08-07：用户确认完整上游本地快照方案；已提交 `docs/superpowers/specs/2026-08-07-upstream-full-localization-design.md`，等待用户审阅后再写实施计划。
- 2026-08-07：完成 upstream MuJoCo 完整快照本地化；provenance test `2 passed`、package diff clean，准备添加隔离 runner 和 A/B harness。
- 2026-08-07：用户确认方案 A 并完成 source swap：切换前的 `src/rebotarm_simulation` 已字节级归档到 `third_party/rebotarm_simulation_current_baseline/`，`ARCHIVE_MANIFEST.json` 对 20 个源/资源文件做 size/SHA-256 校验；上游模型、核心 runtime、ROS node、config、launch 已直接进入 active `src/rebotarm_simulation`。默认 `mujoco_moveit_sim.launch.py` 启动 `rebotarm_mujoco_node`，旧 current adapter 仅由 `simulation_backend:=current` 启用。
- 2026-08-07：source swap 验证完成：active headless 5-step、health（MuJoCo 3.3.0，8 joints/8 actuators）、upstream ROS action `error_code=0 / SUCCEEDED`、current fallback launch、package layering 18、全量测试 `516 passed, 13 skipped`、compileall、colcon build 和 `git diff --check` 均通过；未执行任何硬件命令。上游原始测试的 1 个 `pytest.approx` 嵌套 mapping/tuple 断言缺陷仍单独记录。
- 2026-08-07：完成隔离 runner 与 A/B harness；current/upstream health、model、1 秒 trajectory、gripper/contact 和 upstream test summary 已写入可复核报告。
- 2026-08-07：完成同一 command contract 复测；保留 actuator/controller contract 差异，未覆盖默认模型或上游快照。
- 2026-08-06：P0 现场已清理：停止 disabled driver/controller 与 ROS 2 daemon，串口无占用；清除源码 Python/pytest 和临时 channel override 缓存，保留构建、安装、日志和验收证据。
- 2026-08-06：P1 preflight 定位本机候选上游 `master@bcd584ce...`，嵌套 repo clean；MuJoCo 3.3.0 import 和 ROS package prefix 正常，但 license 文件缺失且 venv `pip check` 暴露两项依赖问题。
- 2026-08-06：P0 Gate B/C 真机报告 `gate-bc-20260806-211949.json` 为 PASSED：10 秒 hold、222 个 joint samples，最大位置跳变 `0.000381 rad < 0.03 rad`，最大绝对速度 `0.007326 rad/s < 0.05 rad/s`；enable/disable 均成功，最终六轴 status 0、`enabled=false`、控制循环停止。随后独立读取 `/rebotarm/arm_status` 再次确认安全失能。
- 2026-08-06：结合 Gate A、Gate B/C PASSED、此前失败报告的 stop/disable 安全 cleanup 和自动化部分使能回滚测试，P0 全部 7 项验收完成。
- 2026-08-06：重新上电后的 `gate-bc-20260806-211156.json` 仍因 joint6 瞬时 `-12.467574 rad` / `-23.435898 rad/s` 失败；用户确认机械臂没有移动。报告最终六轴状态全 0、`enabled=false`、`control_loop_active=false`，stop/disable cleanup 成功。
- 2026-08-06：针对 shared serial bus 反馈交错，增加整段反馈事务锁、enabled 状态主动反馈校验、闲置夹爪 loop 延迟启动和验收 joint-state stale gate；聚焦测试 21 项、完整测试集 `485 passed, 3 skipped`、compileall 和 `colcon build --packages-select rebotarmcontroller` 通过。构建期间未调用真机 enable。
- 2026-08-06：首次 Gate B 报告 `gate-bc-20260806-210112.json` 为 FAILED：夹爪状态未进入 1；cleanup 的 trajectory_stop/disable 均成功，最终六轴状态全 0、`enabled=false`、控制循环停止。
- 2026-08-06：将夹爪 enable 顺序修正为“失能态 MIT -> arm broadcast enable -> gripper 单电机 enable -> 状态确认”，并增强 arm/gripper 双重 disable 与峰值关节记录；构建后完整测试集 `483 passed, 3 skipped`，尚未再次调用真机 enable。
- 2026-08-06：Gate B/C 验收工具、纯状态判定、console entrypoint、确认门和 fail-safe cleanup 测试通过；构建及 `--help` 验证通过，完整测试集 `482 passed, 3 skipped`，准备阶段未打开串口或调用 enable。
- 2026-08-06：P0 Gate A 真机通过：启动日志为 `CONNECTED_DISABLED`，六轴状态码全 0，`enabled=false`、`control_loop_active=false`、`state_machine=IDLE`，joint states 平均 19.999 Hz 且全部数值有限；全程未调用 enable。
- 2026-08-06：motorbridge 只读扫描确认 joint1-6 和 gripper 的 ID/feedback ID 全部在线；逐电机反馈收集修复后完整测试集 `477 passed, 3 skipped`。
- 2026-08-06：Gate A 失能退出后进程和串口均已释放；ROS executor 在 Ctrl+C 时仍打印 context invalid/KeyboardInterrupt traceback，作为非阻塞清理项保留。
- 2026-08-06：安装精确匹配 `VID=2e88`、`PID=4603`、`serial=00000000050C` 的 udev rule；`/dev/ttyACM0` 已为 `root:plugdev`、`0660`，当前用户读写检查通过。
- 2026-08-06：首次 P0 Gate A 真机启动在打开 `/dev/ttyACM0` 时因权限拒绝退出；设备存在、无其他进程占用，且未到达 enable 流程。
- 2026-08-06：P0 hardware manager、action gate、分层和 launch cleanup 聚焦测试 36 项通过；连接不隐式使能、失能反馈刷新、主动反馈失败、软限位、hold 和回滚均有自动化覆盖。
- 2026-08-06：P0 软件门落地后的完整测试集 `475 passed, 3 skipped`。
- 2026-08-06：Agent 状态生成器、P0-P6 权重和实时字段测试通过；与分层测试合计 20 项通过。
- 2026-08-06：Agent 和分层 Python package `compileall` 通过。
- 2026-08-06：架构与主要 MuJoCo 单元测试 44 项通过。
- 2026-08-06：更新 docs 后，`tests/test_package_layering.py` 18 项通过。
- 2026-08-06：docs 相对链接、Markdown fence 和 `git diff --check` 通过。

- 2026-08-09：进入物理 TCP calibration 后完成 disabled-only/plan-only 预检。当前标定纸 ArUco 30/30，base center `[-4.443,-556.850,133.342] mm`，center std最大 `0.062 mm`、paper-normal `0.079 deg RMS`；视觉基准稳定。但纸面中心没有三维物理尖点，按 paper normal 构造的接触姿态对全部测试 roll 均 MoveIt `NO_IK_SOLUTION(-31)`，不得强行压纸。TCP core/node 已归位 `rebotarm_calibration`，Ubuntu-native ROS Image/CameraInfo、sensor QoS、frozen reference、known-reference gates 和 classic unknown-reference pivot solver 已实现；focused 31、layering 18、full `587 passed, 15 skipped`、compile/build/diff全通过。下一步等待用户固定一个刚性尖点/小球/单一方块角点，再做 disabled-only route 审查和至少 5 个多轴 pivot samples。全程未 enable/trajectory，最终 ROS/controller/tty clean。证据：`Agent/evidence/P5/2026-08-09-tcp-calibration-preflight.md`、`Agent/evidence/P5/2026-08-09-tcp-reference-preflight.json`。

- 2026-08-09：用户制作金属立杆+热熔胶固定约16 mm钢球，并确认不会摆动；camera-only 为60/60 circle detections，按“平行夹指轻力对称包络球心”条件接受，正式采样须以前后球心一致性/pivot residual排除热熔胶变形。紧接着 disabled-only controller connect 因六轴均无 feedback fail closed，joint1连续3次refresh失败且disable status均None；未connect/enable/trajectory，tty存在且已释放。等待用户确认执行器主电源/急停/总线在线后继续。证据追加：`Agent/evidence/P5/2026-08-09-tcp-calibration-preflight.md`。

- 2026-08-09：恢复主电源后TCP球夹具disabled-only预检通过：唯一真实publisher、status0/no error。球心粗定位 `base_link [-36.2,-552.7,188.4] mm`、球径约15.5 mm；当前位300姿态有81 valid IK，但最小关节变化仍 `1.995 rad/114.3 deg`。位置敏感性显示整套夹具向机器人移200 mm降至1.239 rad，移250 mm降至0.997 rad，故推荐保持高度/X不变向机器人移约250 mm后重做实位/碰撞审查。全程disabled/no goal，ROS/controller/tty clean。证据：`Agent/evidence/P5/2026-08-09-tcp-ball-{route-preflight,ik-search}.json`、`2026-08-09-tcp-fixture-position-search.json`。
- 2026-08-09：用户纠正物理球径为约10 mm，并确认之后保持不再移动。最新 camera-only 118 帧中 102 帧球轮廓一致；使用已知10 mm球径（反光球中心深度像素读到后方标定纸，未直接采用）得到球心 `base_link [-6.9,-314.4,214.3] mm`，轴向标准差约 `[0.52,5.26,1.60] mm`。控制器为 `CONNECTED_DISABLED`、enabled/control-loop 均 false、六轴 status 0、唯一真实 joint-state publisher。300姿态 IK 成功70，最佳最大关节变化 `1.073 rad/61.5 deg`；5个不同方向的 MoveIt plan-only 均 SUCCESS，规划段时长约9.49–14.42 s。未使能、未发送轨迹；夹具/标定纸未加入规划碰撞场景，不能据此授权实机接触。证据：`Agent/evidence/P5/2026-08-09-tcp-10mm-moved-route-preflight.json`。
- 2026-08-09：用户确认标定纸已移动并明确授权在安全范围内持续执行 TCP 低速接触采样，不要求每个采样点重复询问；该授权只覆盖本 TCP 标定批次，不扩展到 P6 的 approach/gripper/lift/retreat。plan-only 证据仍不等于接触安全证明：钢球立杆/底座及现场几何尚未完成完整碰撞场景验收。采样必须保持低速轻力、平行夹指对称包络同一个固定球心；任何可恢复失败都须保持 enabled hold 或受控回 baseline、确认后才 disable，禁止突然失能。TCP checkbox 继续未勾选，需待多轴 pivot 样本及 rank/condition/rotation-span/residual gates 通过。证据：`Agent/evidence/P5/2026-08-09-tcp-contact-authorization.md`。
- 2026-08-09：首次 TCP 低速接触验证安全失败：baseline→precontact `25 s`、precontact→nominal-contact `12 s` 均 action success、无 guard stop；低力闭合 `contact_detected=false`，`reached_position=0.0876619377 m`，未碰球，故不产生 pivot 样本。失败后保持健康 enabled hold，受控回 baseline 最大误差 `0.000381 rad`，确认后才 disable，最终六轴 status0/no error、IDLE/disabled。原始证据已归档为 `Agent/evidence/P5/2026-08-09-tcp-first-contact-sample.json`，摘要见同名 `.md`；TCP checkbox保持未勾选。注意：用户汇报低夹持力为 `0.10`，但原始 JSON `hold_force` 字段为 `0.05`，参数口径待核对。
- 2026-08-09：TCP 首次接触重试在 baseline→precontact 阶段被 tracking guard 安全取消，joint2 tracking error 持续 `0.0530177984 rad > 0.05 rad`、持续 `0.5805779 s`；夹爪保持打开，未接触钢球，故不形成 pivot 样本。取消后控制器健康，按规则保持 enabled hold；随后独立 `15 s` guarded return 回 baseline，最大误差 `0.000381 rad`，确认后才 disable，最终六轴 status0/no error、IDLE/disabled。禁止直接重试同一轨迹，必须先做 joint2 tracking 根因审查并生成新路线；TCP checkbox继续未勾选。证据：`Agent/evidence/P5/2026-08-09-tcp-first-contact-sample-retry.{md,json}`、`2026-08-09-tcp-retry-guarded-return.json`。
- 2026-08-09：分段 TCP 预接近 `4/4` action 成功且无 guard stop，但回基线段触发 `window_velocity_sustained`：joint2 `0.1036026011 rad/s > 0.1` 持续 `0.2508651 s`。取消后先保持健康 enabled hold，再以独立受控路线回 baseline，最大回位误差 `0.000763 rad`，确认后才 disable；最终六轴 status0/no error、IDLE/disabled，controller/MoveIt/tty均清理。无接触样本，真实运动暂停；禁止直接重试该分段路线，等待 joint2 轨迹/速度根因审查。证据：`Agent/evidence/P5/2026-08-09-tcp-segmented-precontact.{md,json}`。
- 2026-08-09：标定纸移除后新鲜采集 91 帧，球心像素 `[377.5,228.5]`，base 球心 `[-0.01364,-0.33370,0.20997] m`；fresh route 的 4×12 s forward + 15 s precontact→contact 全部成功且无 guard stop，实际到达 nominal contact pose。`GraspGripper(0.20/0.10)` 在接触位失败：`contact_detected=false`、`reached_position=0.0875864 m`，未闭合/未碰球；失败后35 s受控回baseline，最大误差`0.000763 rad`，确认后才disable，最终status0/no error。correct-float free-space GraspGripper 复现同一超时，确认服务路径不适用；`SetGripper` free-space target `0.006 m` 成功到 `0.008051 m`。后续仅采用定gap+actual-position方法，禁止盲目 force grasp；TCP checkbox保持未勾选。证据：`Agent/evidence/P5/2026-08-09-tcp-fresh-contact-run.{md,json}`、`2026-08-09-grasp_service_free_test.json`、`2026-08-09-setgripper_gap_free_test.json`。

- 2026-08-09：用户要求暂停 TCP 标定并保存现场。本轮不再发送运动、使能、夹爪或接触命令；最近一次运行已受控回 baseline（最大误差 `0.000763 rad`）后确认 disable，最后已知六轴 status 全 0、无 error、IDLE，随后停止 controller/MoveIt/RSP，`/dev/ttyACM0` 无占用。由于控制器已停止，ROS 状态 topic 无 publisher，仅作为进程/串口清理证据，不能冒充新的在线状态采样。GraspGripper 的 free-space A/B 失败与 SetGripper 的定 gap 成功已归档；非零 gap `max_effort` 不是严格力上限，暂停解除前必须先完成无物体限力/位置语义验证。TCP checkbox继续未勾选。封存记录：`Agent/evidence/P5/2026-08-09-tcp-pause-closeout.md`。

- 2026-08-09：用户决定取消实物 TCP classic pivot 标定，采用 active upstream MuJoCo `ee_site pos="-0.105 0 0"` 作为 nominal TCP；真实视觉配置、GraspNet profile、规划默认值和 launch 默认值已统一到 `tcp_offset_xyz=[-0.105,0,0]`。仿真执行 launch 保留 `0.0`，因为 upstream 模型内部已包含 `ee_site=-0.105 m`，避免重复施加。该决策不等于实物 TCP 通过，P5 TCP checkbox 保持未勾选；外部相机或其他硬件升级不列入当前规划，待实际加装时再处理。证据：`Agent/evidence/P5/2026-08-09-tcp-upstream-explicit-decision.md`。

- 2026-08-11：用户再次明确外部相机升级无需评估、无需写入规划，待未来实际加入时再处理。已从 `PROJECT_STATUS.md` 和 `EXECUTION_FLOW.md` 删除相关评估路线；TCP 决策与上游 nominal value 保留。当时记录的下一工作项为 MuJoCo geometry/dynamics/contact 核验，该只读审计已在同日后续完成，结论见本文件当前事实/阻塞与`2026-08-11-mujoco-geometry-dynamics-contact-readonly-audit.md`；仍不跳过未完成P5门进入P6。现场无控制/规划进程；本轮清理了仓库自身27个`__pycache__/.pytest_cache`（约`4.6 MB`），以桌面回收站方式保留可恢复性，未触碰虚拟环境、模型、build/install/log或验收证据。

- 2026-08-11：用户授权将 approach 重试的 pregrasp/approach 移动各设为 `45 s`、接近位保持 `20 s`、回 baseline `45 s`。fresh bottle confidence `0.8004082`、age `0.9302 s`；pregrasp `91` points、approach `53` points、return 均 `SUCCEEDED/error_code=0`，`guard_stop=null`，无速度尖峰；回 baseline 最大误差 `0.00152588 rad` 后才 disable，最终 status0/no error/runtime clean。仅关闭 approach staged hardware sub-gate，不扩展到夹爪/抬升/撤退/接触。证据：`Agent/evidence/P6/2026-08-11-approach-45s-roundtrip-pass.md`。
- 2026-08-11：用户要求识别当前桌面瓶子并按 `45/20/45 s` 执行一套完整抓取。只读 real-mode runtime 复核显示 bottle confidence `0.874–0.885`，中心 depth `364 mm`、bbox median `373 mm`（p10/p90 `363/382 mm`），TF 反投影到 `base_link` 约 `[-0.0281,-0.5444,0.1456] m`；局部深度统计未证明 depth 错误。当前 GraspNet best candidate confidence 仅 `0.2335`，candidate IK filter 持续 `error_code=-31`，`/grasp/filtered_plan` 为 `valid=false/no IK-reachable grasp candidates`，因此按 fail-closed 未 enable、未发 trajectory、未执行夹爪/lift/retreat/contact。该请求未形成完整抓取证据，当前阻塞收敛为 depth-to-grasp/GraspNet candidate/IK profile mismatch，需 fresh valid plan 后再执行。证据：`Agent/evidence/P6/2026-08-11-full-grasp-perception-gated.md`。
- 2026-08-11：用户移开瓶子后的障碍物后要求重新读取。8 个 bottle detection confidence `0.8759–0.8862`，中心 depth `364–365 mm`、中央 ROI median `367–372 mm`，live TF 反投影约 `[-0.02815,-0.54452,0.14598] m`；GraspNet raw candidates 已恢复到每帧 `1–9` 个，best raw score 采样最高约 `1.03`，但 filtered candidates 连续为 `0`，filtered plan 仍 `valid=false/no IK-reachable grasp candidates`，固定 `base_axis` grasp/pregrasp 持续 MoveIt `error_code=-31`。障碍物移开不是充分条件，当前不证明深度错误已解决，也不授权盲目完整抓取；未 enable、未发 trajectory、未执行夹爪/lift/retreat/contact，控制器只读为 disabled/status0。证据：`Agent/evidence/P6/2026-08-11-full-grasp-obstacle-cleared-reread.md`。
- 2026-08-11：用户更换瓶子后要求查看是否可行。新瓶子检测代表性 confidence `0.8969967`，中心 depth `395 mm`、中央 ROI median `401 mm`，TF 反投影到 `base_link` 约 `[-0.0280,-0.5729,0.1331] m`；10 秒窗口 GraspNet 有 `18` 个 non-empty 输出、单帧 `1–10` 个候选、best raw score 约 `1.23`，但 `17` 个 filtered plans 全部 `valid=false/no IK-reachable grasp candidates`，当前 `base_axis` grasp/pregrasp 持续 MoveIt `error_code=-31`。新瓶子改善了 raw perception 但未形成可执行计划，未 enable、未发 trajectory、未执行夹爪/lift/retreat/contact，控制器保持 disabled/status0。证据：`Agent/evidence/P6/2026-08-11-new-bottle-reread.md`。
- 2026-08-11：用户将新瓶子移近后要求重新查看。代表性 bottle confidence `0.9256485`，中心 depth `285 mm`、中央 ROI median `301 mm`（p10/p90 `284/346 mm`），TF 反投影到 `base_link` 约 `[0.00009,-0.45886,0.13810] m`；10 秒窗口 GraspNet 有 `19` 个 non-empty 输出、单帧多为 `10` 个候选、best raw score 约 `1.08`，但 `16` 个 filtered plans 全部 `valid=false/no IK-reachable grasp candidates`，当前 `base_axis` grasp/pregrasp 持续 MoveIt `error_code=-31`。瓶子位置已改变但 profile 仍无可执行计划，未 enable、未发 trajectory、未执行夹爪/lift/retreat/contact，控制器保持 disabled/status0。证据：`Agent/evidence/P6/2026-08-11-new-bottle-moved-closer-reread.md`。
- 2026-08-11：完成只读 IK 诊断扫描。`move_group`/KDL `/compute_ik` 在 `group=arm`、`ik_link=end_link`、`avoid_collisions=false` 下正常响应；fixed top-down quaternion 在代表性 `y=-0.596 m` 的9个z高度全部`NO_IK_SOLUTION(-31)`，旧z-rotation quaternion 9/9 `SUCCESS(1)`；fixed quaternion网格成功集中在`y=-0.48/-0.44 m`。按最新瓶子近似目标与nominal TCP/TCP offset、`0.08 m` grasp z-offset，pregrasp可解而grasp不可解；去掉z-offset或换旧z-rotation可解。结论收敛为姿态/TCP/z-offset组合问题，不是KDL/MoveIt IK backend整体故障。full real-mode复启还出现`joint1 feedback unavailable after 3 attempts`并导致`base_link`/`camera_depth_frame`断树，作为独立现场阻塞记录；未enable、未发trajectory。证据：`Agent/evidence/P6/2026-08-11-ik-diagnostic-sweep.md`。
- 2026-08-11：参数来源核对确认：P5 hand-eye只负责`end_link -> camera_depth_frame`相机外参；`tcp_offset_xyz=[-0.105,0,0]`是用户接受的upstream`ee_site` nominal TCP工程值，不是hand-eye或实物pivot结果；此前实机命令显式传入的q_y fixed top-down quaternion`[0,0.707106781,0,0.707106781]`属于P6 bottle software profile，覆盖launch原默认q_z`[0,0,-0.707106781,0.707106781]`。用户观察的瓶子竖直/夹爪平行与该姿态语义错配一致；未在确认真实末端坐标方向前重新部署或执行实机动作。证据：`Agent/evidence/P6/2026-08-11-grasp-orientation-provenance.md`。
- 2026-08-11：用户确认恢复旧 z-rotation quaternion `[0,0,-0.707106781,0.707106781]` 做 plan-only；MoveIt candidate IK filter 参数回读一致，对上一轮瓶子位置近似 candidate 输出 `valid=true` filtered plan，pregrasp/grasp 姿态均为旧四元数。该证据仅说明软件姿态与 IK 规划可行，不是实物末端方向或抓取结果验收；本轮未启动真实相机、串口、真机 controller、夹爪或 trajectory。证据：`Agent/evidence/P6/2026-08-11-old-z-rotation-plan-only.md`。
- 2026-08-11：用户将实机动作收缩为单次 `pregrasp → approach → close → 20 s hold → 45 s return baseline`，不做 lift/retreat/safe-home/outcome classification。第一次因夹爪开位目标 `0.090 m` 实际只到 `0.085 m` 安全拦截并回 baseline；第二次 fresh bottle plan（confidence `0.426`、旧 q_z、jaw `0.0813 m`）完成 pregrasp plan-only并以 `45 s` 轨迹发送，未形成可追溯的 approach/close/return action result，因此不宣称抓取成功。最终受控回 baseline，joint 最大误差约 `0.00267 rad`，controller `disabled/IDLE/status0/no error`。证据：`Agent/evidence/P6/2026-08-11-single-grasp-return-fail-safe.md`。

2026-08-11：用户要求切换为 upstream hand-eye calibration / 上游手眼标定参数，以复核瓶子夹取位置超过且向左偏移的问题。已将 `src/rebotarm_vision/config/handeye.yaml` 对齐 `/home/a/project/rebot_refer` commit `152f8f14ceec5cc8fc61664ff63915187a412800`：`end_link -> camera_depth_frame` 平移 `[-0.085,0.010,0.045] m`、四元数 `[-0.523092811,0.578134825,-0.405717914,0.476997914]`。保持 nominal TCP `[-0.105,0,0] m` 和旧 z-rotation quaternion `[0,0,-0.707106781,0.707106781]` 不变，未把 hand-eye 与 TCP 混用。ROS domain `125` fresh real-mode runtime 只做 plan-only：静态 TF 回读一致，controller transient-local 状态为 `enabled=false/control_loop_active=false/IDLE/status0/no error`；live bottle 有间歇性 `valid=true` plan，代表性 confidence `0.4998`、jaw `0.0684 m`、pregrasp `[-0.0031,-0.5344,0.1790] m`、grasp `[-0.0031,-0.5944,0.2090] m`，旧 q；MoveIt `execute=false` 的 pregrasp/grasp 分别成功规划 `73/76` points。该结果仅说明上游 TF 下有软件可执行计划，不证明实物末端对瓶子已对齐，也未 enable、未发 trajectory、未动夹爪。证据：`Agent/evidence/P6/2026-08-11-upstream-handeye-bottle-plan-only.md`。下一步只能在用户明确执行后，重新获取 fresh valid plan，再进行单次受保护抓取；当前运行需先清理。

2026-08-11：用户明确授权执行一次上游 hand-eye bottle grasp。fresh plan confidence `0.73182195`、jaw `0.07044034 m`、旧 z-rotation；MoveIt plan-only pregrasp/grasp 成功，轨迹最大关节变化 `2.271795/0.288131 rad`。实机受保护执行按 `45 s pregrasp → 45 s approach → close → 20 s hold → 45 s return baseline` 完成；三段 action 均 `SUCCEEDED/error_code=0`、`guard_stop=null`。夹爪打开实际 `0.08297 m`，闭合请求 `0.05844 m` 实际 `0.06055 m`，仅记录位置服务结果，不推断夹持/滑落。回 baseline 最大误差 `0.000381 rad` 后确认 disable，最终 `IDLE/disabled/status0/no error`；未 lift、未 retreat、未接触标定。runtime、8081、串口、旧 sim controller均已清理。该结果证明上游参数下动作链可受保护完成，不证明瓶子被成功拾取。证据：`Agent/evidence/P6/2026-08-11-upstream-handeye-single-grasp-return.{md,json}`。

2026-08-11：用户因未观察到上一轮动作要求立即重试。fresh perception 在 30 秒内及随后 60 秒观察均为 `valid=false/no IK-reachable grasp candidates`，未直接复用 stale joint trajectory。按用户“刚刚的动作”要求，复用上一轮已验证的 Cartesian bottle pose，并在当前 start state 重新做 MoveIt `execute=false` 规划；pregrasp/grasp `83/87` points 成功，关节变化 `2.272753/0.294408 rad`。随后受保护执行 `45 s pregrasp → 45 s approach → close → 20 s hold → 45 s return baseline`，三段 action 均 `SUCCEEDED/error_code=0`、`guard_stop=null`；夹爪开位实际 `0.08290 m`，闭合请求 `0.05844 m` 实际 `0.06056 m`，不做夹持/滑落分类。回 baseline 最大误差 `0.000381 rad` 后 disable，最终 `IDLE/disabled/status0/no error`，未 lift/retreat/contact；runtime、8081、tty、旧 sim controller clean。该次结果证明复用已验证位姿的受保护动作链可完成，不证明瓶子拾取成功。证据：`Agent/evidence/P6/2026-08-11-upstream-handeye-single-grasp-return-repeat.{md,json}`。

2026-08-12：用户重新安装夹爪后确认机械闭合，并要求将其校准为零位。只读 controller 预检为 `CONNECTED_DISABLED`、六轴 status 全 0、无 error；夹爪 feedback 为 `position=0.0`、`velocity=0.0`、`torque=0.0`、`status_code=0`，符合项目 closed-endpoint zero convention。发现原 `/rebotarm/set_zero` 只支持 arm motor，未盲目调用空参数；在 `rebotarmcontroller.HardwareManager.set_zero` 增加显式 `joint_name=gripper` 分支：要求 arm disabled，停止 gripper loop，验证 gripper status 0，再调用 gripper motor `set_zero_position()`，不触碰六轴。`rebotarmcontroller` symlink build通过，focused hardware safety tests `21 passed`。实际调用 `/rebotarm/set_zero` `{joint_name: gripper}` 返回 `success=True`；校准后 gripper仍 position `0.0`/status0，arm disabled/IDLE/status0/no error，无机械臂运动。证据：`Agent/evidence/P6/2026-08-12-gripper-zero-calibration.{md,json}`。该结果只校准当前夹爪电机 encoder zero，不等价于实际指宽测量或抓取成功。

2026-08-12：用户确认将 unified failure matrix / 统一失败矩阵从当前 P6 范围剔除，不再作为待办或验收项；历史 evidence 保留。无力反馈条件下，空抓/滑落/未抬起/成功的非显式自动分类同样不纳入当前范围，仅记录实际动作、反馈与安全门结果。当前下一步保持 staged hardware acceptance / 分级实机验收。

2026-08-12：为一次新的瓶子夹取请求完成只读现场与入口审计。默认 ROS domain 无节点或 trajectory action，8081 无 listener，`/dev/ttyACM0` 无占用；`tools/yolo26s-seg.pt` 与安装的 package-share 链接 SHA-256 一致，active hand-eye 仍为 upstream `end_link -> camera_depth_frame` 参数。当前 `rebotarm_visual_grasp_executor` 的 `/rebotarm/visual_grasp/execute` 固定序列包含 `move_to_pregrasp → approach_grasp → close_gripper → lift`（可选 safe retreat），且 PoseExecutionNode 不提供本轮所需的45秒分段重定时或受保护回baseline。因此不得将该 Trigger 服务作为“pregrasp 45s → approach 45s → close → hold 20s → return 45s、无lift/retreat”的单次夹取命令；该路线必须使用专用受保护 runner，在 fresh valid plan、start feedback、controller status 和 joint-delta 审查通过且用户再次明确执行后才可调用。本轮未启动 runtime、未 enable、未发送 trajectory 或夹爪命令。

2026-08-12：按用户要求为重复 hand-eye/瓶子夹取验证新增 `tools/p6_single_bottle_grasp_runner.py`。该 ROS-only tool 每轮严格等待重新收到的 `valid=true`、`bottle`、`base_link` filtered plan，依据实时关节反馈分别 MoveIt plan-only，再经 guard 执行 `45 s pregrasp → 45 s approach → SetGripper close → 20 s healthy enabled hold → 45 s return baseline`；无 lift、retreat、safe-home 或无力反馈抓取分类。route gate 默认 pregrasp `<=2.5 rad`、approach `<=1.2 rad`，开夹需实际至少 `0.082 m`，各末端/回baseline误差均要求 `<=0.02 rad`；失败时遵循 stop→healthy enabled hold→45 s guarded return→确认后disable。显式 `--confirm REAL_SINGLE_BOTTLE_GRASP` 才会初始化真实运行，支持 `--runs N`，每轮存储 plan、action、samples、gripper 和 guard JSON。已完成 `py_compile`、拒绝错误确认 smoke、launch `--show-args` 核对；未启动 runtime 或硬件。

2026-08-12：首次按该 P6 入口启动时，未出现 filtered plan 的运行时根因已确认：GraspNet HTTP service 虽监听 `8081`，但 `/health` 为 `backend_configured=false`、`model_root_or_checkpoint_unset`；launch 参数误用了不受支持的 `graspnet_source_mode=local`（应为 `local_service`）；同时 `rebotarm_vision_node` 与 `rebotarm_graspnet_baseline_node` 未存活，安装的 ROS console entrypoint 使用 `/usr/bin/python3`，若未按 `tools/run_ubuntu_vision.sh` 等价方式将 `.venv-vision` site-packages 加入 `PYTHONPATH`，将缺少 ultralytics/pyorbbecsdk/torch。故现有 `/grasp/filtered_plan` publisher 虽存在但没有输入；这不是 MoveIt 规划耗时或机械臂动作。修复顺序为先带 `GRASPNET_MODEL_ROOT`、`GRASPNET_CHECKPOINT_PATH`、`GRASPNET_API_ROOT` 重启唯一 service 并检查 health，再以 `local_service` 和 vision `PYTHONPATH` 启动全链路；在 fresh `valid=true` 前不得调用 runner。

2026-08-12：本轮实际单瓶 trial `Agent/evidence/P6/2026-08-12-235203-single-bottle-grasp.json` 已进入真实 pregrasp，但 `joint4` effort guard 连续触发：pregrasp 中 `6.4444447 > 5.95` 持续 `0.5540 s`，action 被 canceled；stop 后 controller 当时六轴仍为 status `1`、无 error、enabled hold，runner 的45秒受控回baseline再次在 `joint4` 以 `6.4297924 > 5.95` 持续 `0.5442 s` 被取消，故按安全规则保持 healthy enabled hold，未夹取、未 approach、未 lift/retreat。随后 controller 日志记录 `joint4 status_code=12, expected 1` 共7957次（23:53:33至launch停止），该 SDK status code 当前仓库未提供语义映射，不能断言具体电机故障类别。不得重试、enable或复位；须先完成断电/机械干涉检查与只读 status 复核。运行时现已停止，online status 不可得。

2026-08-12：用户现场补充 Joint4 为 DMD4310，故障期间出现发热、使能脱开、红灯闪烁。达妙 DM-J4310 manual 的 ERR 表定义红灯闪烁为 fault，`C` 为 motor winding over-temperature；本项目 status 为十进制 `12`，即十六进制 `0xC`，结合发热现象强烈指向绕组过温保护，但仍须以原始 feedback frame 或达妙调试助手确认，不能自动 reset。停止当前 P6 真机重试，先断开执行器电源、自然冷却并检查 Joint4 机械负载、线缆和安装散热。

2026-08-13：只读因果复核将本次 DMD4310 Joint4 过温根因收敛为高静态负载姿态与失败后的带载保持，而非轨迹速度。45秒 pregrasp 由 Joint4 `+0.00515` 走至 `-1.64831 rad`（距软下限 `-1.87 rad` 约`0.222 rad`）；中途22.49秒反馈 torque为`-2.024 N.m`，终点速度近零但 torque升至`-6.444 N.m`、实际相对目标落后`0.01349 rad`。guard 在最后`0.554 s`超`5.95 N.m`取消；45秒 return 在同负载初态立即再触发。受控回位失败后按安全策略留下 enabled hold，日志中的`status_code=12`于 recovery停止约34.3秒后出现，期间应持续为该姿态供电保持。慢速45秒只降低动态惯性，不降低重力/摩擦产生的静态电流，反而延长热积累。尚无温度、母线电压、原始ERR frame或机械摩擦量测，故不能区分模型重力低估、减速器/线缆机械阻滞、散热/环境或电机本体退化；禁止将 PI/vlim、手眼标定或抓取模型直接归为主因。

2026-08-13：用户明确要求重新校验夹爪零位。仅启动 `driver_only.launch.py`（50 Hz、无 enable/trajectory），前置采样为 arm `enabled=false`、control loop false、六轴 status全`0`且无error，Joint4此时也是status`0`；gripper feedback为 position/velocity/torque=`0/0/0`、status`0`。调用 `/rebotarm/set_zero` `{joint_name: gripper}` 返回 `success=true`；复核反馈仍为`0/0/0/status0`、机械臂仍 disabled/IDLE、无活动 trajectory goal。随后停止本次临时 controller 并释放 `/dev/ttyACM0`；未触发 arm enable、复位、轨迹或夹爪开闭。

2026-08-13：针对用户所述夹爪传动齿轮断裂，已只读审计实际 P6 runner。每次 trial 无上电自动 homing；zero 始终是执行 `/set_zero {joint_name:gripper}` 时的当前机械位置，故只有人工先将夹爪完全闭合再设零才可解释为闭合零位。发生 Joint4 保护的 `2026-08-12-235203` trial，在 enable 后、任何 arm pregrasp trajectory 之前执行 `SetGripper(position=0.085m,max_effort=0)`；映射到 motor target `-4.722 rad`（software open soft limit `-4.9 rad`），即假设90mm满行程的94.4%开度。然后才运行45秒 arm pregrasp；该 trial 在pregrasp失败，`gripper_close=null`，从未下发闭合或20秒夹持。实现没有给该开位命令明确速度上限：500Hz MIT position loop，以`kp=5,kd=1`并将合成电机侧命令力矩裁到`±1.5 N.m`；传入的`max_effort=0`虽转为`0.4`，但非零开位时不作为前馈力矩使用。最初闭合到开位的大误差会使该`±1.5 N.m`裁剪生效，且初始开位过程未被 runner raw samples覆盖；只有开位成功后样本可见反馈 torque为约`-0.05至-0.26 N.m`、速度峰值`0.5055 rad/s`（约9.1mm/s换算）且仍在开位收敛。因此不能证明断齿由`1.5 N.m`导致，但其发生窗口是唯一的夹爪传动动作；应停止复测并做无驱动机械检查、齿轮/齿条啮合、实际行程/zero偏差、终止挡块与断面检查。后续测试须先改为受限速度、受限力矩、限时且记录起动全程的夹爪专用 characterization，不能复用85mm全开命令。

2026-08-13：按用户要求实现夹爪大位移受限 profile，但未执行真实夹爪动作。`HardwareManager` 的正常 `SetGripper` 位置模式现在从测得 motor angle 开始，以电机侧`0.20 rad/s`斜坡推进（当前90mm/5rad映射约`3.6mm/s`），并将位置环合成命令力矩硬裁到`0.15 N.m`（先前为隐式`±1.5 N.m`）；timeout按大位移估算延长，完成判定对最终 goal 而非斜坡中间点。MIT closing/holding 路径未改，本次限制只覆盖大位移 `SetGripper` normal position mode。`py_compile`、hardware-safety focused `21 passed`、package layering `18 passed`，mock profile 确认10ms tick目标仅推进`0.0020 rad`且effective torque `-0.0100N.m`。未启动controller或电机；损伤传动的机械行程和可靠低载阈值仍需实体检查后才可授权动作。

2026-08-13：用户报告 Joint4 与夹爪相关故障均已清除。当前仅完成新的只读 runtime 预检：ROS domain 126 无项目节点或 trajectory action，8081 无 listener，`/dev/ttyACM0` 存在且无进程占用；这些只证明可从干净现场启动，不能替代上电后的 status/温升/机械啮合检查。受保护 runner 现要求显式 `--gripper-open-m`（必须由实际机械行程确认）并将开夹 `max_effort` 默认/上限设为 `0.15 N.m`；正常 `SetGripper` 大位移实现同时限制为电机侧 `0.20 rad/s`。静态验证：`py_compile`、hardware safety + layering 共 `39 passed`、`git diff --check` 通过；未启动 controller、未 enable 或发送真实动作。下一步是 operator 启动 real hardware plan-only 链路，确认 fresh `valid=true`、上电 status、joint delta 与实际安全开度后，才可手动执行一次 `45 s pregrasp → 45 s approach → close → 20 s hold → 45 s return`（无 lift/retreat）。

2026-08-13：用户报告夹爪在预夹取阶段到达85mm后、后续接近/夹取阶段仍持续向外施力并断齿。只读代码追踪确认正常 `SetGripper` 路径存在残留位置控制缺陷：`wait_gripper_target()` 在反馈接近 `_gripper_goal_angle` 时只返回 `True`，没有将 `_gripper_active=False` 或 `_gripper_mode="idle"`；因此 `_gripper_tick()` 会持续以原85mm开位 goal 的 position mode 发送命令，直到下一次 `SetGripper` 覆盖目标或 controller 停止。这违反“到位后不再向外施力”的预期。最新三份 runner JSON (`00:57:05`、`00:57:32`、`01:04:11`) 中，两份没有 trial，另一份在 joint-delta gate 拦截，均未 enable、夹爪或发送 trajectory；当前 runtime/serial/ROS graph 也为空，故无法把用户所述动作归因于该三次 runner 调用，仍需在下一次启动时核对是否有 visual executor、手动 service 或遗留 controller 命令路径。暂停一切夹爪复测；修复应使到位时显式下发零刚度/零前馈的 neutral MIT command，并原子切为 idle，随后增加单元测试和仅夹爪低行程验证。

2026-08-13：按用户要求完成夹爪持续开夹问题的上游追溯与代码修复。`/home/a/project/rebot_refer@fb28dcdd` 的 `HardwareManager.set_gripper_target()` / `wait_gripper_target()` 与当前仓库原始导入版本一致：在到达目标时仅返回成功，`_gripper_active` 仍为true、`_gripper_mode`仍为`position`，而500Hz `_gripper_tick()` 继续发MIT position command；upstream `git blame` 将此路径追到初始 `4b60e2f`，所以是上游已有缺陷而非本轮限速限力改动。当前 `HardwareManager` 新增 `_release_gripper_position_target()`：normal position到位后，在 gripper lock 内先下发当前角度、`kp=0/kd=0/tau=0` 的 neutral MIT command，再清除goal并切换`idle/inactive`；tick与同步等待路径都执行该释放，显式`grasp_closing/grasp_holding`不受影响。新增两个mock回归测试，验证同步服务和无人等待的tick都会中性脱力且后续tick不再发position command。`py_compile`、focused+layering `41 passed`、全量 `615 passed, 15 skipped`、controller compileall 与diff check通过；build/install为symlink且已解析到修改后的source。未启动controller、enable或任何电机动作。真实验证仍须在修复/更换齿轮后以低行程、无物体、单独夹爪动作进行，禁止直接重试85mm或完整抓取。

2026-08-13：用户确认当前夹爪机械状态下的开合行程口径：闭合 encoder zero / 编码器零位为 `0.000 m`，最大可达且应视作当前最大安全开位为 `0.085 m`（85 mm）。该85mm是用户在齿轮当前状态下给出的机械上限，不是本轮controller在线反馈测量；controller当时未运行。后续任何 `SetGripper` 或 P6 runner 开夹参数不得超过 `0.085 m`，并仍须保持到位neutral/idle修复和低速低力矩限制。

2026-08-13：将用户确认的当前夹爪最大安全开位正式写入真机命令门：CAD/feedback mapping仍保留`0.090 m`，但 `HardwareManager.set_gripper_target()` 的真实下发位置被硬裁到 `_G_VERIFIED_OPEN_LIMIT_M=0.085 m`，因此旧UI/工具即使请求90mm也只会产生85mm目标；P6 runner 参数校验同步拒绝`>0.085 m`。新增mock regression验证90mm请求被裁为85mm角度。focused+layering `42 passed`、全量`616 passed, 15 skipped`、py_compile/diff check通过，已从install symlink实际导入并回读`0.085 m`。未启动controller或电机。

2026-08-13：用户依据实际夹爪长度确认 canonical TCP / 规范工具点应为 `end_link -> grasp_tcp = [-0.04, 0, 0] m`，不再采用上游 MuJoCo 的 `ee_site=-0.105 m` 作为实机 TCP。已将本地真机视觉抓取 defaults/configs、MuJoCo `ee_site`、MJCF generator 和离线感知入口统一为 `-0.04 m`；sim execution 继续传 `[0,0,0]` 外加偏移，以避免模型内部 `ee_site` 的重复施加。该配置变更未启动 MuJoCo/真机、未使能、未发送 trajectory 或夹爪命令；必须在下一次真实动作前重新做 fresh plan-only 与净空审查。

2026-08-13：实时瓶子抓取诊断确认 GraspNet service 配置后可产生 fresh `bottle` plan，MoveIt plan-only 也通过；两次动作均在受保护 runner 的开夹 `SetGripper(0.080 m, max_effort=0.15)` 阶段超时，未进入 pregrasp/approach/close。失败时夹爪反馈约`0.04048 m`、status0；禁用后连续反馈位置保持不变，当前无 runner、无 active trajectory、arm disabled/IDLE、六轴status0。该证据不能证明零位错误：需要在 controller disabled 状态下将当前物理指宽与40.5mm反馈对照，或人工完全闭合后验证feedback是否为0，禁止仅凭中间位置重新set-zero。另发现普通 `set_gripper_position()` 的 timeout 返回false时不会自身neutral/idle；本次runner会在异常后stop+disable使现场停止，但该超时释放缺口必须修复后才能继续夹爪复测。

2026-08-13：用户确认已手动将夹爪置于机械闭合零位，并明确授权重新校准。前置为arm disabled/IDLE、六轴status0、gripper status0；调用`/rebotarm/set_zero {joint_name: gripper}`返回`success=true`/`set_zero complete`，但随后的持续实时feedback仍为`position=0.0404818 m`（而非0）、velocity约`-0.00733`、torque约`-0.7350`、status0。故本次不能记录为zero calibration通过；当前实现只依据`motor_handle_set_zero_position()`未报错返回成功，未刷新并验证夹爪post-zero feedback。未使能、未发trajectory或开合命令；不得在未解释该反馈不变前继续抓取。

2026-08-13：用户确认此前反馈40.5mm时夹爪实际上已在机械闭合零位，并指出前两次开夹超过其机械最大位置。修复controller：夹爪zero现在保存当前raw motor angle为software closed origin，所有指宽/开夹目标相对该origin计算，85mm硬上限也随origin平移；普通position开夹超时时立刻neutral MIT并切idle。重启为driver_only（无enable/trajectory）后，pre-zero与post-zero realtime feedback均为`position=0.000 m`、status0（post-zero torque约0.00244）；因此本次零位校验通过。新增origin和timeout-neutral mock回归，hardware safety+layering `45 passed`；全量为`618 passed,15 skipped,1 failed`，唯一失败仍是既有snapshot许可证测试期望`PROVISIONAL`而工作区声明为`AUTHORIZED_BY_UPSTREAM_OWNER`，与本修复无关。controller仍为disabled/IDLE，下一步只在用户明确授权后先以小行程无负载开夹验证，不得直接重试80mm或完整夹取。

2026-08-13：用户授权两次低行程无负载夹爪开合验证，约定每轮`0→10mm→0`、服务`max_effort=0.05 N.m`、任一段异常立即停止。preflight为zero`0.000m`、arm disabled/IDLE、六轴status0；enable后六轴/gripper均status1且无error。第一轮开夹`SetGripper(0.010m,0.05N.m)`经约6秒等待返回`success=false/reached_position=0.0m`，实时feedback仍`0.0m`，无开夹位移；故不执行close或第二轮，立即disable。post-check为arm disabled/IDLE、六轴status0、gripper`0.0m/status0`、无active trajectory。该结果说明当前`0.05N.m`不足以克服夹爪静摩擦/传动阻力或position command未建立，尚不能区分；禁止直接提高到80mm。下一步须由用户明确选择在小行程下提高到不超过当前0.15N.m软件上限的分级值（建议先0.10N.m），或先做无驱动机械传动检查。

2026-08-13：用户授权继续两次10mm无负载开合，以`0.10N.m`分级验证。preflight仍为zero约`0.0m`、arm disabled/IDLE/status0；enable成功。第一段`SetGripper(0.010m,0.10N.m)`仍超时且仅到`0.00000687m`（约0.0069mm），没有有效开夹位移；gripper status1期间无error。按约定未执行close和剩余轮次，立即disable；post-check arm disabled/IDLE、六轴/gripper status0。故0.05与0.10N.m都不能驱动最小10mm行程，不能把问题继续归因为zero；在得到用户明确许可前不得尝试0.15N.m或更大位移，建议优先检查传动卡滞/电机模式与实际MIT命令，再决定是否做受限0.15N.m诊断。

2026-08-13：用户明确授权最高`0.50N.m`的受限递减夹爪诊断，并说明历史断齿位置不在本次前部小行程范围。实现显式`gripper_position_torque_cap_nm` controller参数：默认`0.15N.m`，仅driver_only本次启动传`0.50`才可放宽，范围硬限`[0.05,0.50]`；其余启动路径不继承。新增测试后hardware safety+layering`46 passed`。先driver_only下set-zero，zero feedback为0。首段`SetGripper(0.010m,0.50N.m)`实际到`0.00785m`（实时约0.00788m），说明命令可驱动，但仍因距10mm到位阈值约2.15mm超时返回false；六轴和gripper仍status1/no error。按fail-closed未继续0.4/0.3/...档或下发close，立即disable。随后停止高cap controller并重启为默认`0.15N.m`，回读controller disabled/IDLE、六轴status0、参数确为0.15。重启后reported gripper position为0而无本轮close命令；因未做物理指宽复核，不得据此断言夹爪已机械闭合，下一次动作前应由操作者确认实际指宽并在机械闭合位再次set-zero。

2026-08-13：用户指出夹爪零位应记录而非每次controller重启重新校准。确认旧实现并不自动set-zero，而是把软件`_gripper_zero_angle`默认初始化为0且仅存内存，故重启后的0mm是默认解释，不能证明机械闭合。修复为持久零位记录：显式`/rebotarm/set_zero {joint_name:gripper}`仅在operator确认机械闭合后，将raw closed angle原子写入`~/.local/state/rebotarm/gripper_zero.json`（0600）；controller启动只读该记录、不发校准/开合命令；无有效记录则拒绝SetGripper/GraspGripper，且`/rebotarm/gripper/state.position`发布NaN而非假0mm。当前重启后的driver_only确为disabled/IDLE、六轴status0、gripper status0/position NaN，记录文件尚不存在，符合“未显式校准即未知”的fail-closed状态。hardware safety+layering`48 passed`；全量`621 passed,15 skipped,1 failed`，唯一失败仍是既有snapshot许可证期望与已授权声明不一致。下一步需operator将夹爪物理闭合后显式set_zero一次以创建记录；其后重启会加载记录，不重新校准。

2026-08-13：用户再次确认夹爪已机械闭合后，执行一次无运动的`/rebotarm/set_zero {joint_name: gripper}`。服务返回成功，并将当前raw closed angle `-0.00019073486328125 rad`持久写入`/home/a/.local/state/rebotarm/gripper_zero.json`（schema v1、权限0600）；随后反馈为`position=0.0 m`、status0。全过程arm保持disabled/IDLE，未enable、未发trajectory、未下发开合命令。后续controller重启只加载该记录，不会自动重新校准或移动夹爪。

2026-08-13：用户授权一次无负载小行程到位复测。复用现场已运行的`driver_only` controller（未重启/覆盖），预检持久zero记录有效、夹爪`0.0m/status0`、六轴含Joint4均status0，normal position torque cap为`0.15N.m`。仅执行`enable → SetGripper(0.010m, 0.15N.m) → feedback → disable`，无规划、arm trajectory或闭夹；enable后六轴均status1/no error。SetGripper返回`success=false`、`reached_position=0.0000137329m`，终态反馈约`0.000006866m`（约0.014mm），未到达10mm；trap受保护disable返回success，post-check夹爪与Joint4均status0。该结果证明在当前默认低力矩下夹爪仍不能驱动最小开度，不能归因为零位，禁止在未做机械传动/电机输出诊断或取得新的明确授权前提高力矩、增加位移或执行完整抓取。

2026-08-13：用户要求将普通position开夹电机侧力矩默认设为`0.40N.m`、可配置最大值设为`1.20N.m`。已同步`HardwareManager`构造校验、ROS controller参数、`driver_only` launch默认值及P6 runner的默认/CLI校验；grasp closing/holding的独立限制未改。focused hardware safety+package layering `48 passed`，controller/bringup symlink build通过，launch `--show-args`回读默认`0.40`，runner help回读允许`0.05..1.20 N.m`，diff check通过。现场既有controller仍以启动时的`0.15N.m`运行；本轮未重启、未使能或下发夹爪命令，新的默认值须在下一次显式重启controller后才生效。

2026-08-13：用户授权关闭/重启controller后立即复测。发现此前controller已停止，故没有强杀或覆盖其他进程；以`driver_only`新启动，回读normal position torque cap=`0.40N.m`、持久zero记录已加载、夹爪`0m/status0`、六轴含Joint4均status0。仅执行`enable → SetGripper(0.010m,0.40N.m) → feedback → disable`，无规划、arm trajectory或闭夹。夹爪实际达到约`0.00788m`（7.88mm，显著高于0.15N.m时的0.014mm），但服务仍返回`success=false/reached_position=0.00787594m`，未到达10mm；保护disable success，post-check六轴、Joint4与夹爪均status0。当前controller保持运行但disabled。该结果表明提高cap改善了运动、却仍存在约2.12mm行程不足/到位失败；不可据此进入大位移或完整抓取，应先判定机械限位/传动阻力或将到位容差与编码器映射做分离诊断。

2026-08-13：用户指出普通开合应采用上游方式、仅剔除持续保持位置的缺陷。已将normal position开夹从本地target ramp恢复为upstream-compatible direct MIT goal：`set_gripper_target()`立即将目标角写入MIT position loop，`wait_gripper_target()`恢复上游3秒默认等待语义；保留本地85mm硬上限、持久闭合zero、position effort默认0.40N.m/硬上限1.20N.m，以及到位和timeout均发送`kp=kd=tau=0` neutral并切idle的修复。新增回归验证直接目标而非ramp；hardware safety+layering`49 passed`、controller/bringup build和diff check通过。现有controller是修改前启动，仍disabled；移除ramp会令起始误差立即进入0.40N.m cap，在当前已有齿轮损伤风险下未擅自重启或实测，需用户明确授权后才执行新方式的10mm无负载单次验证。

2026-08-13：用户明确授权direct MIT目标10mm测试。关闭旧disabled controller并启动新controller加载改动后，回读cap=`0.40N.m`、六轴status0；但出现关键feedback连续性异常：旧controller disable前最后夹爪反馈为`7.8828mm`，其后没有任何close或set-zero命令，重启新controller却报告`0.0mm/status0`。代码审计确认本地`connect()/init_gripper()`仅添加motor、请求反馈并保持disabled，不调用`set_zero_position()`；故不能由本轮启动解释为软件主动归零，也不能单凭新反馈断言机械已闭合（可能是无力后机械回弹，也可能是DMD反馈坐标在controller重建时重置/未保留）。为避免将未知物理位置误当0mm后直接以0.40N.m开夹，本轮已暂停实际direct-target测试，controller保持运行但disabled；需operator目视/量具确认当前指宽并明确报告机械是否闭合，再决定重新set-zero或从当前真实开度开始受限测试。

2026-08-13：用户现场确认夹爪当前实际未闭合。进一步追踪当前launch、安装entrypoint、controller及third-party Python SDK：启动只执行`RobotArm`/`add_damiao_motor`、`request_feedback()`和`poll_feedback_once()`，没有调用`set_zero_position()`；`ensure_mode(MIT)`也只在显式`/rebotarm/enable`内执行。当前`/rebotarm/gripper/cmd`无publisher，当前ROS日志也无set-zero service事件。因此“重启后显示0mm”不是本项目在启动中向电机写零位，而是反馈状态重建后返回0（可能是SDK cached/default state或DMD disabled feedback坐标语义）；同时本地持久zero record把旧`raw_closed_angle≈-0.00019rad`加载为有效闭合origin，`gripper_position_m()`随即将新raw0解释/clip成0mm，形成危险的false zero。结论：当前持久raw-angle零位方案不能跨controller重建作为位置连续性保证；在无绝对编码器或homing/limit-switch测量前，启动后必须将jaw opening标为unknown并拒绝运动，直至operator按实际机械状态明确reconcile/设零。未改代码、未使能或发送电机命令。

2026-08-13：按用户要求实现启动缓慢回零，但保持默认关闭，避免任意controller重启自动驱动已有损伤的传动。controller重建后会主动使旧raw zero record失效并发布unknown；仅在启动参数`gripper_startup_rehome_enabled:=true`时运行。该流程只使能gripper motor（不使能六轴/arm loop/trajectory），以`0.15N.m`、最大`0.10rad/s`的阻尼限速恒力矩向闭合方向运动，要求反馈先出现至少`0.02rad`闭合方向运动、再持续低速停滞`0.40s`才disable并调用`set_zero_position()`记录新zero；无运动、反馈丢失或15秒timeout均neutral+disable且保持unknown，不写zero。参数限制：force`0.05..0.25N.m`、speed`0.01..0.10rad/s`、timeout`0.5..60s`。新增mock回归覆盖controller重建使旧zero失效、motion+stall成功设零、静态feedback拒绝设零；hardware safety+layering`52 passed`，controller/bringup build、compileall与diff check通过。本轮未重启当前controller、未enable或实际运行startup rehome。

2026-08-13：用户随后明确授权重启并运行startup rehome。三次尝试均停在rehome预检：DMD4310在controller constructor阶段持续返回`gripper status_code=1, expected 0`，故fail-closed流程未发送任何closing MIT力矩、未写`set_zero_position()`；每次controller最终仍以`CONNECTED_DISABLED`启动。外部ROS复核在constructor结束后显示夹爪`position=NaN/status0`、六轴含Joint4均status0，说明这是启动期DMD状态帧稳定性/时序问题，不是零位恢复成功。已将重试扩大为仅预检异常的最多3次（无运动才允许retry；真实无位移/timeout永不retry），但仍失败。最新已构建未运行的源代码尚有一个待完成修复：将startup rehome从node constructor同步调用移到ROS executor开始spin后的one-shot timer，避免在状态缓存未稳定时抢跑；用户要求新开会话，故本轮停止在此，未继续改动或动作。当前`driver_only` controller仍运行并占用`/dev/ttyACM0`，但夹爪和六轴均disabled/status0；新会话应先只读确认并由用户决定保留或停止该进程。

2026-08-13：本轮完成startup rehome时序修复：`reBotArmController`不再在constructor同步调用回零，而是在ROS executor开始spin后延迟`1.5 s`创建one-shot timer；回调再次确认参数仍为enabled。新增`StartupRehomePreflightError`，仅当运动前等待gripper disabled status时持续看到`status=1`才允许最多3次有限重试；无位移、超时、反馈异常和运动后的异常不重试。成功后刷新验证夹爪`0.0 m/status0`与六轴全部status0，post-check失败会清除zero记录并保持unknown。新增controller callback与hardware regression tests；focused+layering`56 passed`，全量`629 passed,15 skipped,1 failed`，唯一失败仍为既有snapshot license test把`AUTHORIZED_BY_UPSTREAM_OWNER`期望成`PROVISIONAL`。`rebotarmcontroller`/`rebotarm_bringup` symlink build、compileall和`git diff --check`通过。未启动本轮controller、未enable、未发送MIT/trajectory；只读确认已有PID`146456`的旧controller自`04:12:25`运行，当前`enabled=false/control_loop_active=false/IDLE`、六轴status0、夹爪`NaN/status0`，并继续占用`/dev/ttyACM0`，因此本轮没有真实回零验收证据。下一步等待用户明确授权后重启controller，再按受限startup rehome命令做一次真实测试。

2026-08-13：用户随后要求停止旧`driver_only` controller。先对精确 launch/controller PID发送`SIGINT`，进程未立即退出；确认目标仍为同一组旧PID后发送`SIGTERM`，随后`driver_only.launch.py`、`reBotArmController`均退出且`/dev/ttyACM0`无占用。未发送enable、trajectory或夹爪命令；新timer修复仍未获得真实运行时回零证据。

2026-08-13：用户授权执行“启动回零→开到10mm→回到零位”的受限夹爪测试。按新timer版本启动`driver_only.launch.py`并使用startup rehome参数`0.15N.m/0.10rad/s/15s`；ROS spin后回零仍最终失败，日志为`gripper status_code=1, expected 0`，外部状态为夹爪`position=NaN/status0`、六轴含Joint4均status0。按fail-closed未执行enable、10mm开合或回零动作；随后SIGINT停止launch/controller，确认无相关进程且`/dev/ttyACM0`无占用。该次获得了真实timer运行证据，但未获得startup rehome成功证据，不能进入10mm测试或宣称夹爪验收通过。

2026-08-13：针对用户指出“unknown是代码自写”完成只读审计。确认`position=NaN`完全由本地应用层产生：`connect()`在读取持久zero record后仍无条件将`_gripper_zero_calibrated=False`，`gripper_position_m()`随后返回NaN，ROS publisher直接发布；这不是DMD原始位置或原始status。另确认`status_code=1`在本地SDK/上游RobotArm语义中表示enabled，而不是故障；startup rehome在`_disable_all_motors()`之后等待status0仍失败，说明当前确定性阻塞点是disable后的DMD状态/反馈时序，不能仅归因于constructor未spin。主机安装`motorbridge 0.4.6`，上游变更记录显示v0.4.7已将dm-serial timeout从1ms提高到10ms，v0.4.8又强化fresh feedback；这使旧SDK缓存/串口时序成为高优先级软件嫌疑，但尚无本轮原始总线帧证据。当前未改代码、未启动硬件；下一步应先升级/复核motorbridge ABI与串口反馈时序，并增加逐阶段原始状态诊断，再决定是否重试startup rehome。

2026-08-13：用户指出复测没有明确给电机使能。未改代码，先停止卡住的startup rehome controller；用独立`motorbridge 0.4.6` gripper-only handle（仅注册DMD4310 gripper motor id`0x07`/feedback`0x17`，不注册六轴）实测`disable→status0`、`enable→status1`、`disable→status0`，全程未发MIT torque或位置命令。随后从确认的status0重新启动现有startup rehome，约`15s`后仍以`gripper status_code=1, expected 0`失败；六轴status全0，未执行10mm开合。停止后fresh raw feedback为`status0, pos=-0.434309959 rad, vel=-0.007326126, torq=-0.007326126`；相对持久raw zero`-0.00019073486328125 rad`换算约`7.814 mm`，确认机械仍非0mm。该轮证明了夹爪单独enable/disable链路可用，但没有证明startup callback逐阶段一定已调用enable；startup rehome仍未建立新zero，不能继续正常10mm位置测试。

2026-08-13：按用户要求恢复上游夹爪控制路径，移除本地startup rehome timer、NaN/unknown零位持久化和position ramp/neutral自定义分支；`set_gripper_target()`恢复直接MIT目标，`wait_gripper_target()`恢复上游等待语义，`_gripper_tick()`恢复上游position/grasp命令，`init_gripper()`恢复初始化即启动gripper feedback loop。项目既有显式`/rebotarm/enable`门、85mm机械上限和0.40/1.20N.m约束保留，未把上游`ctrl.enable_all()`放回constructor，避免未经授权自动使能六轴。离线针对性测试`42 passed`，构建和compileall通过；全量测试`615 passed,15 skipped,1 failed`，唯一失败仍为既有snapshot许可证状态不一致。未启动真机、未enable、未发送trajectory。

2026-08-13：用户确认夹爪已机械回零并授权一次10mm无负载位置测试。source工作区overlay后启动driver_only，预检`enabled=false/IDLE`、六轴status全0、夹爪约`0.000003m/status0`。显式`/rebotarm/enable`成功，反馈为`enabled=true/control_loop_active=true`、六轴与夹爪status全1、无error。发送`SetGripper(0.010m,0.40N.m)`返回`success=true/reached_position=0.007886m`，随后反馈到`0.009438m`；按恢复的upstream 0.12rad到位容差判定成功且继续position hold。发送`SetGripper(0.000m,0.40N.m)`返回`success=true/reached_position=0.001857m`，随后反馈为`0.000m`。最后显式disable成功，post-check为`enabled=false/IDLE/control_loop=false`、六轴status全0、夹爪`0.000m/status0`；controller仍运行并占用`/dev/ttyACM0`，未发送arm trajectory。

2026-08-13：10mm位置测试结束后按用户要求向driver_only launch发送SIGINT；launch与reBotArmController均退出，`/dev/ttyACM0`无占用，未再发送任何电机命令。

2026-08-13：Current-source correction / 当前源码更正（由 Claude 的只读审计提出、随后以当前 source 复核）。本项不改写 2026-08-11/12 的历史 evidence；历史中把 `-0.105 m`、persistent zero / 持久零位或 neutral / 中性脱力描述成“当前运行逻辑”的文字，均须以本项为准。当前 active vision / 真机视觉配置和默认 launch 的 `tcp_offset_xyz` 为 `[-0.04, 0, 0] m`：`camera.yaml`、`camera_ubuntu.yaml`、`flat_graspnet.yaml`、`grasp_pose_policy.yaml`、`visual_grasp_system.launch.py`、`visual_grasp_perception_preview.launch.py` 和 `mujoco_offline_perception.launch.py` 已一致。当前 active local MuJoCo `robot.xml` 与 `urdf_to_mjcf.py` 的 `ee_site` 同样为 `-0.04 m`；`real_perception_sim_execution.launch.py` 传 `[0,0,0]` 仅为避免本地模型重复施加偏移。`-0.105 m` 目前只保留在固定 third-party upstream MuJoCo snapshot，不能再称为 active model 或 current runtime TCP。该审计只确认当前配置，不提供 physical TCP measurement / 物理 TCP 测量证据。

2026-08-13：同一当前源码审计确认，`src/rebotarmcontroller/` 已无 `gripper_zero`、`_gripper_zero_angle`、`local/state`、startup rehome / 启动回零或 `NaN unknown / 未知` 的实现引用；`~/.local/state/rebotarm/gripper_zero.json` 虽仍在磁盘上，但为不被读取的 legacy file / 遗留文件，本轮未删除。normal gripper position / 普通夹爪位置路径已恢复 upstream continuous position hold / 上游持续位置保持：`wait_gripper_target()` 到位后不清除 active position goal，`_gripper_tick()` 仍以 MIT `Kp=5`、`Kd=1` 持续发送，直至新目标覆盖或 controller 停止。此前“到位/timeout neutral + idle”的本地修复已被回退；不得把历史修复记录误作当前保护。

2026-08-13：夹爪 torque limit / 力矩限幅的当前源码口径亦已更正。ROS 参数、launch 与 runner 确实把 `gripper_position_torque_cap_nm` 的默认/允许值写为 `0.40/0.05..1.20 N.m`，但该值仅裁剪 `_gripper_target_effort`；在 normal non-zero position target / 非零普通位置目标中，`_gripper_tick()` 令 `tau_ff=0` 且调用 `_gripper_safe_mit()` 时未传该 cap。后者使用默认 `_G_TAU_MAX=1.5 N.m` 裁剪合成 `position term + feed-forward torque / 位置项加前馈力矩`。因此不得把 `0.40/1.20 N.m` 称为当前非零普通位置目标的 effective motor-side hard cap / 有效电机侧硬上限。`p6_single_bottle_grasp_runner.py` 的 open 参数默认 `0.40`、close helper 默认 `0.15` 也不能证明对此路径形成有效非零目标力矩上限。这是需在任何后续夹爪动作前处理的 source safety gap / 源码安全缺口；本轮仅记录事实，未改控制代码、未启动硬件或发送命令。

## 下一次交接

1. P1 已完成并冻结为 upstream-only 本地仿真基线；上游原始重放断言是测试缺陷，不能把未修改上游测试报告写成全通过；
2. 上游根 LICENSE 未补齐，当前只允许本地使用和本地 Git 保存，不发布、不推送含上游资产的分支；
3. P2-P4 已完成；P5 历史关闭证据保留，但当前 active source 的 vision TCP 与 local MuJoCo `ee_site` 均为 `[-0.04,0,0] m`，`-0.105 m` 仅属固定 upstream snapshot，且本仓库尚无新的 physical TCP measurement / 物理 TCP 测量证据。用户已取消实物 TCP classic pivot；Contact calibration已从规划剔除。P5仍为`6/6`完成。P6 的下一步不是自动动作：先处理已记录的夹爪非零位置目标有效力矩上限和持续 position hold 缺口，再由用户单独授权任何实机测试；
4. HJX 只允许行为级观察，不复制实现或资产；
5. P5 的重新标定决策以实机夹取表现和可量化残差为依据，不能仅因进入阶段而重算 intrinsics/hand-eye；
6. 每完成一个验收项，更新 `PROJECT_STATUS.md` 并运行 `update_state.py`；
7. 不触碰工作区中与当前任务无关的已有 RViz 修改。

2026-08-14：修复 normal `SetGripper` position mode 的持续 position hold / 位置保持缺陷，并让 position torque cap / 位置力矩上限真正生效。缺陷一（用户指定）：`wait_gripper_target()` 到位只返回 `True`，`_gripper_active`/`_gripper_mode` 仍为 `True`/`position`，500 Hz `_gripper_tick()` 继续以 `Kp=5/Kd=1` 下发 MIT position command，timeout 返回 `False` 时同样不释放；`/home/a/project/rebot_refer` 同一函数逐行一致，确认为 upstream 已有缺陷。修复新增 `_release_gripper_position_target(expected_goal, require_arrived=)`：在 `_gripper_lock` 内守卫 `mode=="position"` 且 goal 未被新命令替换，先下发一次 neutral MIT（当前角、`kp=0/kd=0/tau=0`、`tau_limit=0.05`），再原子切 `_gripper_active=False`/`_gripper_mode="idle"`；`wait_gripper_target()` 在入口记录 `owned_goal`，到位与 timeout 两条路径都释放，避免 stale waiter 关闭更新的命令；`_gripper_tick()` 在 position 且已在容差内时释放并 return，覆盖无人等待情形。`grasp_closing`/`grasp_holding` 的 mode 不是 `position`，守卫不成立，行为不变。缺陷二（本轮经用户批准一并收口）：`_gripper_tick()` position 分支调用 `_gripper_safe_mit()` 时未传 `tau_limit`，回落到 `_G_TAU_MAX=1.5 N.m`，因此 `0.40/1.20 N.m` 的 cap 此前只在目标角约等于 0（闭到零位）时作为 feed-forward 使用；现显式传 `tau_limit=effort`，非零开夹/闭合目标的电机侧合成力矩上限由 1.5 降为配置值（默认 0.40）。未改六轴控制、`enable/disable`、`set_zero`、85mm 硬上限、`init_gripper` 的上游 loop 启动，未恢复 startup rehome / NaN unknown / persistent zero / target ramp。测试改写两个原本断言错误行为的用例为「neutral + idle 且后续 tick 不再发 position command」，新增 timeout 释放、`tau_limit` 生效（`pos_term + tau_safe == -0.40`）和 grasp 模式不被误关闭三个用例。验证：focused hardware safety `27 passed`、focused+layering `45 passed`、全量 `618 passed, 15 skipped, 1 failed`（唯一失败为既有 `test_upstream_snapshot_provenance` 期望 `PROVISIONAL` 而 manifest 为 `AUTHORIZED_BY_UPSTREAM_OWNER`，改动前已单独复现，与本修复无关）、`colcon build --symlink-install --packages-select rebotarmcontroller`、controller 与分层 compileall、runner `py_compile`、`git diff --check` 通过，并从 overlay 实测 import 回读 release helper、`tau_limit=effort` 与两条释放路径。本轮未启动 controller、未 enable、未发送 trajectory 或任何夹爪命令；只读现场为无项目进程、`/dev/ttyACM0` 无占用、8081 无 listener。真实验证仍须用户单独授权，且只允许低行程（建议 `0 → 10 mm → 0`）、无负载、单独夹爪动作，禁止 80/85mm 开爪或完整抓取。注意：降低有效力矩上限后，此前在 1.5 N.m 下勉强达到的行程可能变得不足，10mm 复测若 `success=false` 属预期范围内的新基线，不得据此提高 cap。另记录 `/home/a/.local/state/rebotarm/gripper_zero.json` 仍存在但当前 source 已无任何读取代码，为回退后的孤立无效文件。

2026-08-14：用户授权修复后的一次低行程无负载夹爪验证（`0 → 10 mm → 0`、`gripper_position_torque_cap_nm=0.40`、driver_only、不使能六轴轨迹、不做抓取），但在只读预检阶段 fail closed，未 enable、未下发任何夹爪或 trajectory 命令。启动前现场为无项目进程、`/dev/ttyACM0` 无占用、overlay 与修复后 source 一致。`driver_only.launch.py` 启动为 `CONNECTED_DISABLED`，回读 `gripper_position_torque_cap_nm=0.4`，`/rebotarm/arm_status` 为 `mode=mit / enabled=false / control_loop_active=false / IDLE`、六轴 `per_joint_status_code` 全 `0`、`error_codes` 为空。阻塞原因：`/rebotarm/gripper/state` 报 `position=0.0668562526702881 m`（约 `66.9 mm`）、`velocity=-0.0073261260986328125`、`torque=-0.017093658447265625`、`status_code=0`，15 秒内 10/10 连续样本三个数值完全不变。这与本次授权前提「当前夹爪处于机械闭合零位、`0 → 10 mm → 0` 属低行程」不成立：若 `66.9 mm` 为真实指宽，则 `SetGripper(0.010 m)` 是约 `57 mm` 的大幅闭合行程，不是低行程；若为 false zero / 虚假零位（`MEMORY.md` 2026-08-13 已记录 controller 重建后 raw feedback 坐标不保证连续、持久零位方案已回退且当前 source 无任何读取代码），则实际物理位置未知。两种解释都不允许在有断齿史的传动上直接 enable 并下发位置命令，故按 fail-closed 停止并在未 enable 的情况下 SIGINT 停止 controller；`reBotArmController` 与 launch 均退出，`/dev/ttyACM0` 无占用。本轮未产生修复后的真机验证证据，neutral+idle 释放与 `tau_limit=effort` 仍只有软件测试证据。解除条件：operator 目视/量具确认当前实际指宽，并说明 `66.9 mm` 是真实开度还是反馈坐标失真；若为真实开度，需先决定是人工归位后重新 `set_zero`，还是从当前开度出发做受限小幅闭合，且新行程须重新审查后单独授权。

2026-08-14：修复后的低行程无负载夹爪验证由 codex 在 operator 手动机械回零后执行并通过，两个缺陷均取得真机证据。`0 → 10 mm` 到 `7.86–8.02 mm`、`10 → 0 mm` 回到 `1.83–1.93 mm`，两段服务均 `success=true`；到位后位置稳定、反馈扭矩仅约 `-0.0024 ~ -0.0073 N.m`，无持续施力迹象。该 residual torque 量级即 neutral MIT（`kp=0/kd=0/tau=0`）的预期特征，证明 neutral+idle 释放在真机生效；对比 2026-08-13 修复前同一命令在服务返回后反馈仍从 `0.007886` 继续爬到 `0.009438 m`。离线换算显示三段均在力矩限幅饱和处停住而非到达目标：角度误差 `0.11889/0.11000/0.10722 rad`，对应 `kp*err = 0.594/0.550/0.536 N.m`，全部超过当时的 `0.40 N.m` cap；`0 → 10 mm` 的 `7.86 mm` 距 `_G_ARRIVE_TOL=0.12 rad`（约 `2.16 mm`）仅差 `0.02 mm`，因此 `success=true` 属容差擦边通过，不代表到位。
2026-08-14：用户据此确认断齿已机械修复、传动阻力偏高结论成立，并决定将 move-phase / 移动期力矩恢复为 `1.5 N.m`。口径变更已明确记录：该值超过 2026-08-13 设定的可配置最大 `1.20 N.m`，正好等于 `_G_TAU_MAX`（`_gripper_safe_mit` 绝对钳位），即把移动期有效力矩恢复到修复前行为；`0.40/1.20` 由本轮取代，仅保留在 Agent 历史中。安全前提是防"到位后继续朝外顶"依靠 `_release_gripper_position_target()` 的 neutral+idle 释放，而不是低移动期 cap，该释放已有上述真机证据。落地范围：`_G_LARGE_MOVE_MAX_TAU_NM=1.5`、`_G_LARGE_MOVE_MAX_TAU_CAP_NM=1.5`（构造校验区间随之为 `[0.05, 1.5]` 并改为引用常量）、`rebotarm_controller.py` 参数默认 `1.5`、`driver_only.launch.py` 默认 `1.5`、runner `--gripper-open-max-effort` 默认 `1.5`/上限 `1.5`。**闭合力矩未随之提高**：runner 闭合阶段原先硬编码 `0.15` 且不受 CLI 控制，现提为显式 `--gripper-close-max-effort`（默认仍 `0.15`），因为闭合是夹持力问题而非行程问题，提高闭合力需单独授权。`grasp_closing`/`grasp_holding` 的 `_G_GRASP_*` 常量未改。测试同步：原 `0.40` 断言改为显式传入 cap 的用例，新增默认 cap 达到 `_G_TAU_MAX` 的用例、构造函数越界拒绝的参数化用例（直接调 `HardwareManager(...)`，校验在任何硬件初始化前执行，不开串口）和硬上限接受用例；`make_manager` 增加 cap 参数。验证：focused+layering `49 passed`、全量 `622 passed, 15 skipped, 1 failed`（唯一失败仍为既有 `test_upstream_snapshot_provenance` 的 `PROVISIONAL` 期望，与本轮无关）、`colcon build --symlink-install` 两包、compileall、runner `py_compile`、`git diff --check`、launch `--show-args` 回读 `1.5`、runner help 回读两个 effort 参数、overlay import 回读常量与释放路径均通过。本轮未启动 controller、未 enable、未发送任何夹爪或 trajectory 命令；`1.5 N.m` 移动期力矩尚无真机验证证据，下一次真机动作仍须单独授权，并建议先重复 `0 → 10 mm → 0` 确认在新 cap 下能否消除约 2 mm 稳态误差。

2026-08-14：用户授权在恢复 `1.5 N.m` move-phase / 移动期力矩后，仅以 `driver_only` 重跑一次无负载 `0 → 10 mm → 0`。启动前无项目进程、`/dev/ttyACM0` 无占用；不传 override 的 launch 回读 `gripper_position_torque_cap_nm=1.5`。disabled preflight 为 `IDLE`、`enabled=false`、`control_loop_active=false`、六轴 status 全 `0`；夹爪 10 个样本稳定为 `1.8299446 mm/status0`，符合当前约 `2.16 mm` 到位容差且未 set_zero。enable 后六轴与夹爪 status 为 `1`，无 arm trajectory。开夹 `SetGripper(0.010, 1.5)` 返回 `success=true/reached=7.961775 mm`；随后约12秒的9个无新命令样本全为 `8.023573 mm`，torque `-0.00244..-0.01221 N.m`，未继续朝10mm爬升，故 open-side neutral+idle release / 开夹侧中性脱力释放有真机支持。该 reached 与0.40N.m时`7.86..8.02 mm`相当，未消除约2mm误差。闭合 `SetGripper(0.000, 1.5)` 返回 `success=true/reached=2.077137 mm`，但返回后的首个样本已变为 `0.745028 mm`；随后5个样本稳定在该值、torque `-0.00244..-0.00733 N.m`。由于用户规定返回后任何继续朝目标爬升即停止，本轮立即disable，未重试或扩大行程；post-check 为arm六轴与gripper status全`0`、`IDLE/disabled`，然后SIGINT关闭controller并确认串口释放。结论：`1.5 N.m`没有消除开夹的约2mm残差；闭合返回后的1.33mm位移使本轮不能作为完整neutral+idle通过证据，须区分释放逻辑失效与服务在位置反馈/线程竞争下过早判定到位，下一步仅可先做软件级时序/状态锁诊断，禁止直接重复真机测试或提高力矩。

2026-08-14：用户在机械闭合零位后明确授权仅夹爪大行程无负载 `0 → 50 mm → 0`。启动前无项目进程、tty空闲，默认 cap 回读`1.5 N.m`；disabled preflight `IDLE/disabled`、六轴status全0，夹爪10个样本均`0.0mm/status0`。enable后只下发`SetGripper(0.050,1.5)`，无六轴trajectory：服务返回`success=true/reached=48.000692 mm`；随后约12秒9个无新命令样本全部`48.083090 mm`，torque `-0.00244..-0.00733 N.m`，未继续向50mm爬升，故大行程开夹侧 neutral+idle release / 中性脱力释放通过观测。回零`SetGripper(0.000,1.5)`返回`success=true/reached=1.864277 mm`，但首个返回后样本变为`0.669497 mm`（继续闭合约1.195mm），随后4个样本位置稳定、torque近零；按本轮硬停止门立即disable，未重试。post-check arm六轴和gripper均status0、`IDLE/disabled`，SIGINT停止controller，tty已释放。结论：50mm开夹的残差仅约1.9mm且释放正常；闭合服务返回后仍有约1.2mm位移，与10mm轮次同型，因此整轮不关闭闭合侧释放/到位时序验收。下一步只可做软件级的等待条件、反馈刷新和control-thread时序诊断；不得基于此重复大行程、提高力矩或扩大行程。

2026-08-14：`1.5 N.m` 移动期力矩的真机复测结果推翻"约2mm残余误差源于力矩不足"的结论。10mm 开夹到 `7.962mm`（残余 `2.038mm`）、50mm 开夹到 `48.001mm`（残余 `1.999mm`）：行程差5倍而残余几乎相同，且 `kp*err` 为 `0.555–0.566 N.m`，远低于 `1.5` 上限，未饱和。对照 `_G_ARRIVE_TOL=0.12 rad`（约 `2.160mm`），三次（含 `0.40 N.m` 那次的 `2.140mm`）都停在容差内侧极小余量处（`0.020/0.122/0.161mm`）。因此约2mm残余是 `_G_ARRIVE_TOL` 判据的直接体现：刚进入容差即判到位，随后 neutral 释放脱力停住，不是力矩不足。用户决定保留 `1.5` 并做成分档参数；该值现在的作用只是给移动期更大余量，未解决任何已观测问题。开夹侧 neutral+idle 在 10mm 与 50mm 两个量级均确认无返回后继续位移。
2026-08-14：闭合侧发现并修复 tick 竞态。三次闭合（10→0、10→0、50→0）均在服务返回后出现一次继续位移 `1.33/1.20mm`，开夹侧无此现象。根因：`_gripper_tick()` 原实现在锁内采样状态、释放锁、再发命令，因此一个已通过到位判定的 tick 仍可能在 `wait_gripper_target()` 完成释放之后发出最后一条带力矩的命令；闭合到零位是唯一暴露方向，因为 `abs(target) < 1e-6` 使 `effort` 成为 feed-forward torque（`_G_DEFAULT_FORCE=0.40` 朝闭合方向推），开夹时 `tau_ff=0` 故无残留推力。按用户选择的方案 B 修复：判定与发送在同一次持锁内完成。由于 `_gripper_lock` 是 plain `Lock` 而非 `RLock`，新增 `_release_gripper_position_target_locked()` 供 tick 复用，公共 wrapper 保持不变；释放顺序改为先清 `_gripper_active/_gripper_mode` 再发 neutral，使 tick 一旦看到 `active=False` 即不再产生命令、neutral 成为最后一条。`_gripper_safe_mit` 调用因此进入锁内，持锁时间包含串口 IO，属该方案的已知代价。
2026-08-14：确认夹取夹不住的根因是控制模式错误，非力矩数值。runner 闭合阶段原走 `SetGripper` position mode，`close_position = min(0.08, max(0.006, jaw_width - 0.012))` 对瓶子约 `0.058m` 为非零目标，故 `tau_ff=0`，且到位即 neutral+idle 脱力，物理上不产生持续夹持力；提高 `max_effort` 无效。修复前残留的 position hold 反而提供了少量握持，neutral 修复使"夹不住"完全暴露。已按用户选择的路径1将 runner 闭合阶段改为 `GraspGripper` 力控路径（`_G_GRASP_CLOSE_KP=0.0` 纯力矩闭合 → 停滞后 `grasp_holding` 以 `kp=5` + `hold_force` 持续保持），新增分档 CLI 参数 `--grasp-close-force`（默认 `0.4`，上限 `1.0`）、`--grasp-hold-force`（默认 `0.4`，上限 `1.5`）、`--grasp-close-timeout-sec`（默认 `2.0`）。
2026-08-14：明确无力反馈下的接触检测语义。`grasp_gripper()` 判据只有 `elapsed >= min_time`、`closure_m >= min_closure`（默认 `0.006m`）和 `abs(vel) <= velocity_threshold`（默认 `0.04`），全部来自位置/速度反馈，`_gripper_torque` 未参与判定；`st.torq` 是 DMD4310 电流估算，语义未标定（既有记录已注明 `finger_force_limit_n=20` 未经 load cell 实测）。因此 `contact_detected=true` 的真实含义是 stall detection / 堵转检测，无法区分夹到物体、夹到机械限位、传动卡滞和力矩不足；2026-08-09 TCP 标定期间的 `contact_detected=false/reached_position=0.0875864` 应重新理解为传动阻力异常下堵转判据无法区分卡滞与接触，而非"服务路径不适用"。runner 已将该字段更名记录为 `stall_detected` 并附 `interpretation` 说明，仅作观测量，不用于分类夹持/滑落/拾取成功，与项目既有"无力反馈不做自动结果分类"边界一致。
2026-08-14：修复 `grasp_holding` 无退出路径的缺口。上游成功进入 `grasp_holding` 后没有任何自动退出，会以 500Hz 持续加载电机直到下一条命令或 controller 停止，与 2026-08-12 Joint4 过温的连续负载机理同类。新增 `_G_GRASP_HOLD_TIMEOUT_DEFAULT_SEC=30.0`、`_G_GRASP_HOLD_TIMEOUT_MAX_SEC=120.0` 与构造参数 `grasp_hold_timeout_sec`（校验区间 `[0.1, 120]`），ROS 参数与 `driver_only.launch.py` 默认均为 `30.0`；`grasp_gripper()` 进入 hold 时设置 deadline，tick 每次检查并在到期时 neutral 脱力切 idle 并记录 `grasp_hold_release_reason`。同时发现并修复 `grasp_gripper()` close-timeout 路径原先只切 idle 而从不下发 neutral，最后一条闭合力矩命令会保持生效，属同一缺陷类；现统一走 `_release_grasp_hold_locked()`。新增公共 `release_grasp_hold(reason)` 供外部主动松开。`GraspGripper.srv` 未改动（10 个包依赖 `rebotarm_msgs`，避免 ABI 变更与重建级联），hold 上限走 controller 参数。runner 另增 `--controller-grasp-hold-timeout-sec` 并在启动前校验 `--hold-sec` 必须小于该值，否则夹持会在 hold 期间被释放而 JSON 仍显示正常；并在 hold 结束、回 baseline 之前显式开夹松开，避免依赖 timeout 在 45 秒回程中途到期。
2026-08-14：验证（全部纯软件，未启动 controller、未 enable、未发送任何夹爪或 trajectory 命令）：focused+layering `55 passed`（新增 tick 到位不再发力矩命令、hold timeout 释放、无 deadline 不释放、主动 release、position mode 不被 grasp 释放误伤、hold timeout 越界拒绝等用例），全量 `628 passed, 15 skipped, 1 failed`（唯一失败仍为既有 `test_upstream_snapshot_provenance` 的 `PROVISIONAL` 期望），`colcon build --symlink-install` 两包、compileall、runner `py_compile`、`git diff --check`、launch `--show-args` 回读 `grasp_hold_timeout_sec=30.0`、runner `--hold-sec 40` 对 `30s` controller timeout 的组合被正确拒绝、错误 `--confirm` 仍拒绝、overlay import 回读单一持锁 tick 与两条 neutral 路径均通过。`GraspGripper` 力控闭合、hold timeout 释放和闭合竞态修复尚无真机证据；下一次真机动作须单独授权，建议先做无物体的 `GraspGripper` 低力闭合以确认堵转判据能否触发，再做带瓶子的分档 hold force 试验。

2026-08-14：用户授权首次 `GraspGripper` 无物体、低力、单独夹爪真机验证，并要求未满足任一阶段即停止。启动前无项目进程/tty占用；default `driver_only` 回读`gripper_position_torque_cap_nm=1.5`、`grasp_hold_timeout_sec=30.0`，disabled `IDLE`/六轴status全0，夹爪10样本稳定`0.003433mm/status0`。enable后仅 `SetGripper(0.030,1.5)`，返回`success=true/reached=27.867937mm`，无六轴trajectory。指定的`GraspGripper(close_force=0.4, hold_force=0.4, close_timeout=2.0s, min_time=0.08s, velocity_threshold=0.04, min_closure=0.006m)` 返回`success=false`、`contact_detected=false`、`contact_position=0.0`、`reached_position=8.840698mm`、`hold_force=0.4`、message=`grasp close timeout before stall detected`。因此该2秒窗口内未满足"累计闭合≥6mm且速度≤0.04"的 stall detection / 堵转检测条件；未进入`grasp_holding`，不应声称堵转判据、30秒hold timeout或SetGripper接管已经真机验证。按用户异常即停立即disable，post-check六轴与夹爪均status0（夹爪约`8.744568mm`）、SIGINT停止controller且tty释放；未重试、未提高力矩、未接触瓶子。下一步需用户另行决定是否做纯软件诊断（收集close-loop velocity/position时序或复核2秒/0.04阈值），或授权一条新的受限真机测试；不得自动扩大范围。

2026-08-14：用户确认首次低力闭合无异常声响，并明确授权一次相同范围的二次无物体 `GraspGripper` 测试；未提高force、未延长2秒timeout、未扩大30mm开度。预检默认参数仍为move cap`1.5N.m`/hold timeout`30.0s`，disabled `IDLE`、六轴status全0，夹爪10样本稳定`0.408554mm/status0`；30mm开位返回`27.854204mm`。闭合前高频feedback显示稳定开位`27.936602mm`、torque近零；同一`0.4/0.4N.m`、2秒、0.08秒、`0.04rad/s`、6mm参数返回再次为`success=false/contact_detected=false/contact_position=0/reached=8.929962mm/hold_force=0.4/message=grasp close timeout before stall detected`。这与首次的`8.840698mm`可重复，说明在当前低力与阈值下无物体闭合没有进入holding，不能开启30秒hold timeout或5秒SetGripper接管验证；该结果也不代表已测得物理接触。按异常即停立即disable，post-check六轴及夹爪status全0（夹爪`8.895630mm`），SIGINT关闭controller、tty已释放。下一步建议仅做软件级诊断：记录/模拟close-loop中的raw position、velocity量化和判据时间序列，判断2秒窗口、`0.04rad/s`阈值与当前电机反馈是否可达；未经新的明确授权，不重复真机测试、不改force、不接瓶子。

2026-08-14：用户授权在现有硬上限内将无物体 `GraspGripper` 的 `close_force` 提高至 `1.0N.m`（`hold_force=0.4N.m` 保持不变）。本轮在任何 enable 或指令前被零位 preflight / 预检拦截：`driver_only` 回读 move cap`1.5N.m`、hold timeout`30.0s`，arm为`IDLE/disabled`、六轴status全`0`；但夹爪10个连续样本稳定为`position=8.895630mm`、`status0`、torque约`-0.0073..-0.0122N.m`，不符合operator已机械闭合至0位的前提。故未调用`/rebotarm/enable`、`SetGripper`或`GraspGripper`，直接SIGINT停止controller；后验无相关进程且`/dev/ttyACM0`无占用。此轮没有`close_force=1.0N.m`的真机动作或hold证据；须先由operator重新机械归零并在新启动反馈确为0附近后，才能重新授权该受限测试。

2026-08-14：operator重新机械回零后，完成一次无物体、仅`driver_only`的`GraspGripper(close_force=1.0N.m, hold_force=0.4N.m)`真机验证。预检夹爪10样本稳定`0.003433mm/status0`、move cap`1.5N.m`、hold timeout`30.0s`、arm disabled/IDLE且六轴status全0；enable后仅开夹至`30mm`，`SetGripper`成功、`reached=27.895403mm`。首次grasp成功：`success=true/contact_detected=true/contact_position=0.0/reached_position=0.0/hold_force=0.4/message=closing stalled; holding (bounded to 30 s)`。hold反馈在约30秒到期前为torque约`-0.1197..-0.1245N.m`，随后于下一秒降为`-0.00244..-0.00733N.m`且position保持`0.0m`，为自动neutral release / 自动中性脱力的真机证据。执行偏差须明确保留：原计划应在首次hold约5秒时以`SetGripper(30mm)`验证命令接管；本轮先完成35秒timeout观测，之后才发送第二次grasp。第二次grasp从`0mm`开始，无法满足`min_closure_distance_m=0.006`的累积闭合条件，故按2秒timeout返回`success=false/contact_detected=false/reached=0.0`；随后`SetGripper(30mm)`成功、`reached=27.874804mm`，但这不是hold期间的接管证据。按异常即停已disable；post-check arm disabled/IDLE、六轴与夹爪status全0（夹爪约`27.895403mm`），SIGINT停止controller且tty释放。未经新授权，不应重试或提高力矩。

2026-08-14：完成用户要求的无物体 `GraspGripper` 完整真机验证，范围仅`driver_only`、无六轴trajectory。预检从上一轮开位`27.895403mm/status0`开始，arm disabled/IDLE、六轴status全0，move cap`1.5N.m`、hold timeout`30.0s`。第一条路径：`GraspGripper(close_force=1.0N.m, hold_force=0.4N.m, close_timeout=2.0s, min_time=0.08s, velocity_threshold=0.04rad/s, min_closure=0.006m)`返回`success=true/contact_detected=true/contact_position=0/reached=0/hold_force=0.4`并进入hold；保持期torque约`-0.1197..-0.1245N.m`，约30秒deadline后降到`-0.00244..-0.00733N.m`且position稳定`0m`，自动neutral通过。第二条路径先开至`27.895403mm`再以同参数成功进入hold；首次接管动作虽在active hold期间成功，但复核controller日志发现真实间隔约`21.5s`，不满足约5秒验收，故不把它计作精确定时证据。随后在新controller中从`27.895403mm/status0`重新执行同参数grasp并成功进入hold，使用同一执行链计时：grasp服务返回时间`1786647099578568916ns`，`SetGripper(0.030m,1.5N.m)`发起时间`1786647104583412539ns`，间隔`5.004844s`；接管前torque约`-0.1197..-0.1245N.m`，SetGripper返回`success=true/reached=27.881670mm`，反馈到`27.922869mm`后稳定为`27.895403mm`且torque降至`-0.00244..-0.00733N.m`，证明active hold可由正常position命令覆盖并在到位后neutral。最终disable成功，arm disabled/IDLE、六轴及gripper status全0，夹爪约`27.895403mm`；SIGINT停止controller，项目进程为空且`/dev/ttyACM0`释放。结论仅限无物体条件：stall detection可在机械闭合限位触发、30秒自动释放与精确5秒接管均有真机证据；`contact_detected=true`仍只能解释为堵转，不能分类为真实物体接触。

2026-08-14：用户重新安装夹爪并确认其处于机械闭合零位，授权只读核对位置，若非零则在disabled状态手动`set_zero`。本轮未能读取夹爪位置，也未执行校准：启动前无项目进程且`/dev/ttyACM0`空闲、HDSC CDC设备正常枚举；但两次`driver_only`连接都在创建ROS topic/service之前被整臂feedback safety gate拦截，确定性报错`arm feedback refresh failed: joint1 feedback unavailable after 3 attempts`，joint1至joint6的disable诊断均为`status_code=None`。因此当前故障是六轴供电/CAN反馈不可用，不是已测得的夹爪位置非零。为避免在整臂状态未知时绕过controller安全门直接写电机零位，本轮未enable、未调用`/rebotarm/set_zero`、未发送夹爪开合或六轴trajectory；失败进程均已退出，`/dev/ttyACM0`释放。解除条件是operator恢复并确认六轴控制电源、急停/使能链路与CAN连接，再重新启动`driver_only`；只有六轴及夹爪均能返回disabled `status_code=0`后，才读取连续夹爪样本并按机械零位授权调用`SetZero{joint_name: gripper}`。

2026-08-14：完成带瓶子的 `GraspGripper` 分档真机验证，范围仅`driver_only + enable + SetGripper + GraspGripper`，无六轴trajectory/pregrasp/approach/lift/retreat。operator先将夹爪两端手动调至刚好接触瓶子，只读20样本记录瓶宽反馈稳定为`50.369654mm/status0`；移瓶并机械闭合后10样本均`0.0mm/status0`，故无需set_zero。开至80mm命令返回`reached=77.856485mm`、反馈`77.904551mm`。A档`close_force=0.4N.m/hold_force=0.4N.m/timeout=4.0s/min_time=0.08s/velocity_threshold=0.04rad/s/min_closure=6mm`返回`success=true/contact_detected=true/contact_position=47.980093mm/reached_position=47.980093mm/message=closing stalled; holding (bounded to 30 s)`；10秒hold反馈约从`47.581821mm`至`47.574955mm`，漂移`-0.00687mm`，torque区间`0.27106..0.28571N.m`。operator主观判断为“能握住一点但握不紧，不足以达到夹取效果”；因此A档证明0.4N.m在4秒timeout内可以触发stall，支持此前2秒窗口过短是无物体低力测试失败的重要原因，但0.4档对本瓶夹持不足。松开至约77.96mm后执行B档，参数仅将`close_force`改为`1.0N.m`、hold仍`0.4N.m`：返回`success=true/contact_detected=true/contact_position=34.789427mm/reached_position=34.789427mm/message=closing stalled; holding (bounded to 30 s)`；10秒1840样本位置稳定`34.947355mm`、漂移`0.0mm`，torque区间`0.42247..0.43712N.m`。operator判断“能握住，些许形变，不影响”，未报告异响或卡顿；该主观结论满足本次瓶子的夹取效果，但`contact_detected=true`仍只表示stall而非自动接触分类。注意B档stall位置比手动刚接触瓶宽小约`15.58mm`，只可记录为瓶体压缩、滑移或接触几何变化的合并可能，不自动分类。收尾`SetGripper(80mm)`返回`reached=77.911417mm`，反馈约`77.904551mm`且torque近零；disable成功，arm六轴与gripper status全0、IDLE，SIGINT停止controller且`/dev/ttyACM0`释放。本轮未改控制代码。

2026-08-14：尝试验证同瓶A档`close_force=0.4N.m`在`close_timeout=5.0s`与25秒hold窗口下能否达到平衡，但本轮因观测启动延迟不计通过或失败。预检机械闭合反馈10样本均`0.0mm/status0`，move cap`1.5N.m`、hold timeout`30.0s`，开80mm返回`reached=77.918283mm`、反馈`77.904551mm`；operator确认同瓶放好且初始无形变。grasp全部返回字段为`success=true/contact_detected=true/contact_position=45.638597mm/reached_position=45.638597mm/hold_force=0.4/message=closing stalled; holding (bounded to 30 s)`，证明5秒timeout顺利触发stall。错误在于grasp服务返回后到25秒采样启动存在约15秒调度间隔，导致相对采样的`t=15s`已撞上controller 30秒hold timeout。采样点：t0=`45.638597mm/0.388278N.m`、t5=`45.638597mm/0.383394N.m`、t10=`45.638597mm/0.383394N.m`、t15=`45.727861mm/-0.007326N.m`、t20=`45.727861mm/-0.007326N.m`；全采样24.175秒/4836点，min/max position=`45.638597/45.734728mm`，末值`45.727861mm`，表面`+0.089264mm`回推发生在自动neutral之后，不是active hold内的弹性平衡证据。有效active-hold片段仅约前15秒，期间位置在反馈分辨率内不变、torque约`0.383..0.393N.m`，但不足以回答25秒收敛；自动释放后也不能做operator轻推握持判断。已松开到`77.904551mm`、disable，全轴与gripper status0，停止controller并释放tty。若继续，必须重新满足启动零位前提，并把subscriber建立、grasp调用及25秒采样放入同一执行链，确保从grasp返回起立即计时；未经operator重新放瓶确认不重试。

2026-08-14：按operator要求重测上述A档，仍因本地采样脚本边界错误而不计通过或失败。预检为机械零位`0.0mm/status0`、六轴全status0、move cap`1.5N.m`、hold timeout`30.0s`；开至`77.904551mm`后operator确认同瓶在位且初始无形变。grasp返回`success=true/contact_detected=true/contact_position=46.098650mm/reached_position=46.098650mm/hold_force=0.4/message=closing stalled; holding (bounded to 30 s)`，5秒timeout再次顺利触发stall。为避免上一轮延迟，本轮先建立subscriber再调用grasp，但采样器错误地以`torque>0.10且position<70mm`作为hold起点，将closing phase的`69.980541mm/0.124542N.m`纳入计时；因此输出t0不是service-return后的hold起点。后续可确认的趋势为约`45.762194mm/0.295483N.m`（误计t5和t10）到`45.707262mm/0.280830N.m`（误计t15、t20、t25），位置后段稳定，但从controller记录的hold建立时间到采样结束仅约23秒，不满足用户要求的完整25秒active-hold窗口；随后已自动neutral，未做轻推。不能用本轮确认0.4N.m足够或1.0N.m必要。收尾已开到`77.904551mm`、disable，全轴和gripper status0，停止controller并释放tty。下一次若继续，采样必须先缓存topic，再以grasp服务返回的ROS/monotonic时间戳筛选其后严格25秒，不能再用位置或torque阈值猜测hold起点；仍需operator重新移瓶、机械闭合归零和重新放瓶确认。

2026-08-14：operator 确认瓶子实际接触点约 `50 mm`，据此修正两项此前判断。（一）A 档 `close_force=0.4` 不是假堵转：停在 `47.9801 mm` 即压入瓶壁 `2.02 mm`（瓶宽 4%），确为真实接触；但 10 秒仍以 `-0.00687 mm` 继续闭合、未达平衡，且保持力矩 `0.2711~0.2857 N.m` 低于命令的 `0.4`，属"接触但压不实"，与 operator"能轻微握住、握不紧"一致。B 档 `close_force=1.0` 停在 `34.7894 mm` 即压入 `15.21 mm`（瓶宽 30%），10 秒向外弹 `+0.158 mm` 为弹性回推，是真实夹紧特征。该 30% 压缩量说明 B 档参数依赖瓶体变形吃掉行程，不得沿用于更硬或更脆的目标。（二）此前"瓶子只需走 10mm、2 秒够用"的推断错误：从 `80 mm` 开位到 `50 mm` 接触点需走 `30 mm`，按实测 `9.51 mm/s` 需约 `3.15 s`，故 `2.0 s` 默认对真实抓取确实不足，但原因是空行程时间而非闭合力矩不足；B 档能在 2 秒内触发是因为更大力矩闭合更快。
2026-08-14：完成两项纯软件收口（用户指示"1、4 立刻做"）。（1）`--grasp-close-timeout-sec` 默认由 `2.0` 提高到 `4.0`，help 明确要求覆盖到物体的完整空行程而非仅接触段，并在代码注释记录 `9.51 mm/s` 与 `3.15 s` 的实测依据；上限仍 `10.0`。（4）明确 `hold_force` 语义：`grasp_holding` 下发 `kp*(hold_angle-pos)+kd*(-vel)+hold_force` 且 `hold_angle` 冻结在堵转位置，位置项主导，故实际握持力由 `close_force` 造成的压入深度决定，`hold_force` 只是前馈项。证据是两档命令值同为 `0.4` 而实测保持力矩相差约 1.5 倍（`0.271~0.286` vs `0.423~0.437`）。已在 `hardware_manager.py` 的 `_G_GRASP_HOLD_KP/KD` 处加入含两档实测数据的说明注释，并改写 runner `--grasp-hold-force` 的 help 明确它不是握持力主控参数。未改任何控制逻辑或默认力矩值。验证：focused+layering `55 passed`、全量 `628 passed, 15 skipped, 1 failed`（唯一失败仍为既有 snapshot license 期望）、`colcon build --symlink-install --packages-select rebotarmcontroller`、compileall、runner `py_compile`、`git diff --check`、runner help 回读两项新文案均通过。未启动 controller、未 enable、未发送任何命令。
2026-08-14：当前可用分档记为 `close_force=1.0`（仅限此类软瓶，压缩量约 30%），`hold_force=0.4` 不作为握持力结论。`contact_detected` 仍只代表 stall / 堵转，不构成"已抓住"的自动判定，是否握住由 operator 目视与轻推判断。待验证项（第3项，提示词已交付、等待用户决定何时执行）：A 档 `close_force=0.4` 在 `close_timeout_sec=5.0` 与 30 秒观测窗口下能否压到平衡并握住；若能，则应改用 `0.4` 而非 `1.0`，对传动与目标都更安全。当前没有证据表明必须使用 `1.0`，A 档此前的不足可能只是闭合时间与观测窗口过短。

2026-08-14：A 档 `close_force=0.4` 的两次补测确认 `close_timeout_sec` 提高有效，但暴露一致性问题。`5.0 s` timeout 下两次均顺利触发堵转（`contact_position=45.6386 mm` 与 `46.09865 mm`），证明此前 `2.0 s` 失败确为空行程时间不足而非力矩不足，`--grasp-close-timeout-sec` 默认改为 `4.0` 的依据成立。但三轮 `0.4 N.m`（含最初 `47.9801 mm` 那次）压入深度为 `2.02 / 4.36 / 3.90 mm`，极差 `2.34 mm`；保持力矩相应为 `0.271~0.286 / 0.383~0.388 / 0.281~0.296 N.m`，即命令值 `0.4` 的 `70% / 96% / 72%`。该离散度与 `hold_force` 非主控、握持力由 `kp*压入深度` 主导的结论一致：`0.4 N.m` 落在瓶壁刚开始变形的敏感区，堵转点对初始条件敏感。A#3 在 23 秒观测内仍以约 `2.39 um/s` 继续闭合（`45.7622 -> 45.7073 mm`），未出现 B 档 `+0.158 mm` 那样的向外弹性平衡特征；A#2 看似位置恒定但有效窗口仅约 15 秒且力矩为三轮最高，不足以证明达到平衡。两轮均未取得 operator 轻推判断。
2026-08-14：用户决定采用 B 档 `close_force=1.0`、不再重测 A 档。**依据记为可重复性而非力矩下限**：A 档并非推不动（三轮都能触发堵转），而是压入深度与握持力离散度过大（极差 `2.34 mm`、力矩在命令值 `70%~96%` 间波动），抓取需要可重复而非单次勉强握住；B 档压入 `15.21 mm` 时位置项接近饱和，力矩收窄到 `0.423~0.437 N.m`，一致性显著更好。此结论修正此前"没有证据表明必须使用 1.0"的表述——证据即 A 档三轮离散度。**已知代价：B 档对该瓶压缩约 30%，依赖瓶体变形吃掉行程，换更硬或更脆的目标必须重新分档测试，不得直接沿用。** `hold_force=0.4` 继续不作为握持力结论。`contact_detected=true` 仍只代表 stall / 堵转，是否握住由 operator 目视与轻推判断，不写入自动结论。当前 P6 单瓶抓取的夹爪参数口径：`--grasp-close-force 1.0`、`--grasp-hold-force 0.4`、`--grasp-close-timeout-sec 4.0`（或更长以覆盖实际空行程）。
2026-08-14：`grasp_hold_timeout_sec=30.0` 的自动 neutral 释放现有两组独立真机证据：无物体机械限位那次约 30 秒后 torque 由 `-0.120~-0.125` 降至 `-0.0024~-0.0073 N.m`；A#2 带瓶那次因 grasp 返回到采样启动间约 15 秒调度间隔而在采样第 15 秒处撞上 timeout，torque 由 `0.3834` 掉到 `-0.0073 N.m`、位置保持。两次释放后的向外位移（`+0.089 mm` 等）发生在脱力之后，不得记为保持期弹性平衡证据。`active hold` 被普通位置命令接管亦有真机证据（hold 返回后 `5.0048 s` 发送 `SetGripper(30 mm)` 成功开到约 `27.9 mm`）。

2026-08-14：补充 `close_force=1.0` 成立的目标前提（operator 确认）。当前目标是**空塑料瓶**，瓶壁可弹性变形，因此约 30% 压缩量属可接受，且正是这种变形吃掉行程才让夹爪进入可靠堵转区间——B 档一致性好的物理前提就是目标可压缩。判断新目标是否算同类，看的是"瓶壁能否变形吸收约 15 mm 行程"，不是外形是否像瓶子。明确排除并须重新分档的场景：（一）**满瓶或装液体的瓶子**，液体不可压缩，`1.0 N.m` 会把力直接反弹到传动，等效于夹刚体；（二）玻璃瓶、金属罐等硬壳目标；（三）易碎或薄壁易压瘪的目标（压缩 30% 会破坏）。这些场景在有断齿史的传动上不得直接沿用 `1.0`。

2026-08-14：审阅 codex 提交的完整六轴单瓶执行命令，三终端流程与视觉 launch 参数正确（`use_hardware:=true`、`execution_mode:=plan_only`、`start_visual_grasp_executor:=false`、`start_sim_trajectory_controller:=false` 保持真机/仿真互斥，`tcp_offset_xyz:=[-0.04,0,0]` 与旧 z-rotation quaternion 与当前 source 一致），runner 的 B 档参数（`--grasp-close-force 1.0`、`--grasp-hold-force 0.4`、`--grasp-close-timeout-sec 4.0`、`--controller-grasp-hold-timeout-sec 30.0`、`--gripper-open-max-effort 1.5`、`hold-sec 20 < 30`）也正确；codex 关于"不要单独启动 driver_only"的判断成立。据此完成三项修复：（一）**launch 参数缺口**：真机 controller 实际由 `visual_grasp_system.launch.py → interactive_system.launch.py` 启动，而后者的 `reBotArmController` 参数块从未传 `gripper_position_torque_cap_nm` 与 `grasp_hold_timeout_sec`，此前只靠 node 默认值恰好等于期望值（`1.5`/`30.0`），属偶然正确且无法从命令行审计或覆盖。现两个 launch 均显式声明并透传，`--show-args` 可回读。（二）**`GraspGripper` 失败不再仅靠人工观察**：codex 指出 `success=false` 时 runner 不会立即失败；现改为 `success` 或 `stall_detected` 任一为假即 `raise`，走既有 stop→healthy enabled hold→受控回 baseline 路径，避免在未真正闭合的情况下白保持 20 秒。注释明确 `stall_detected=true` 不证明握住，但其缺失确证闭合未完成。（三）**回程前的松开加门**：`hold` 结束后的显式开夹现校验 `reached >= gripper_open_m - 0.006`，否则拒绝启动 45 秒回程，防止带夹持力回位。另核实 `_G_ARRIVE_TOL=0.12 rad`（`2.160 mm`）导致所有 `SetGripper` 都停在目标外约 `2.0~2.13 mm`：`80 mm → 77.90 mm` 误差 `2.100 mm`、容差余量仅 `+0.060 mm`，`30 mm → 27.868 mm` 余量仅 `+0.028 mm`；runner 开夹门限为 `target - 3.0 mm`，`77.90` 以 `+0.90 mm` 通过，故当前 `--gripper-open-m 0.080` 可用，但该余量很薄，若日后开夹门限收紧或容差调整需重新核算。验证：focused+layering `55 passed`、全量 `628 passed, 15 skipped, 1 failed`（既有 snapshot license）、`rebotarm_bringup` build、launch compileall、runner `py_compile`、`git diff --check`、`visual_grasp_system.launch.py --show-args` 回读两个新参数均通过。本轮未启动 controller、未 enable、未发送任何命令；完整六轴链路仍待用户授权后由 codex 执行。

2026-08-14：回答 operator 关于底座方向与手眼标定的问题：**底座安装方向不影响 hand-eye 标定有效性**。标定的是 `end_link -> camera_depth_frame`，属末端局部外参；joint1 角度或底座朝向改变时整条运动链会正确传播该固定关系。故 codex"若相机安装位置或角度与上游不同"的条件句在 operator 已确认相机安装与上游一致的前提下不成立。但另一事实成立且更重要：当前 `handeye.yaml` 是上游值 `translation [-0.085,0.010,0.045]`，而 2026-08-09 本机 SUBPIX 重标定并部署过的值为 `translation [-0.065156962,0.010759370,0.052694746]`、`rotation xyzw [-0.580583738,0.571763761,-0.412803362,0.406942392]`（`Agent/evidence/P5/2026-08-09-handeye-subpix-deployment.md`），两组差 translation 约 `21.296 mm`、rotation 约 `10.446 deg`。2026-08-11 为排查偏移临时切换到上游值后未换回。**operator 明确表示当时标定过程不规范、无法保证可用，因此本轮不换回，也不把任何一组称为更可信；事实记录为：两组都缺少当前可信残差证据**（上游组在本机无残差证据；本机组虽有 `2.495 mm/0.359 deg` holdout 数据但过程规范性被 operator 否定）。该项搁置，不作为本轮修复内容。
2026-08-14：核实 codex 路径规划排查结论，三项主要判断成立。（一）**MoveIt 路径被丢弃**——runner 以 `execute=False` 取得 `planned_trajectory` 后只读最后一个 waypoint，再用 `build_quintic_command` 从当前位置到该终点重新生成关节空间五次曲线，避障路径、中间姿态与 IK branch 过渡全部丢失，故 RViz 看似合理而真机末端走出异常 Cartesian path。（二）**`candidate_pose_policy` 默认 `preserve_candidate_pose`**（`visual_grasp_system.launch.py:611`），该策略保留 GraspNet 每帧姿态，命令行传入的 `fixed_grasp_orientation_xyzw` 不构成约束，导致姿态逐帧变化、IK 成功率低、腕部姿态与 IK 分支不稳定；历史上成功的 plan-only 用的是 `base_axis`。（三）**30 秒失败是可预测的 guard 触发而非解算失败**——joint2 位移 `1.679 rad` 的五次曲线峰值为 `1.875*1.679/30 = 0.104937 rad/s`，超过 `window_sustained_rad_s=0.10`，实测正是在 `0.10256 rad/s` 取消；45 秒对应峰值 `0.069958` 故可通过。另确认大量 MoveIt WARN（`planning volume was not specified`、`planner_id field`）伴随 RRTConnect 正常规划，不是低成功率主因；主因在 candidate IK filter 的 `-31`、`joint6 delta too large`、`jaw_width too large` 及 `still processing previous candidates; dropping this frame`（每帧 20 候选 × 双姿态 × pregrasp/grasp IK + 碰撞检查，处理不完即丢帧）。
2026-08-14：按 operator 指示实施第 2、3 项修复（第 1 项标定搁置）。（3）新增 `rebotarm_motion.paired_trajectory_protocol.build_retimed_path_command()`：保留 planner 几何路径（按序遍历全部 waypoint），仅替换时间参数化——以关节空间 max-norm 弧长为进度量、对进度施加 quintic profile，因此起终点速度为零且慢速斜坡不变，末端 waypoint 精确落在 planner 终点而非插值值。runner 的 `_request_plan()` 改为返回完整 waypoint 序列，pregrasp/approach 两段改用该函数并把实时 baseline/实际 pregrasp 位置前置为路径起点；回 baseline 段保持 `build_quintic_command`（目标是已知 baseline，无 planner 路径）。同时新增 `_peak_command_velocity()`/`_assert_command_speed()` 与 `--max-command-velocity-rad-s`（默认 `0.09`，硬上限 `0.10`），在发送前拒绝自身命令峰值超限的 leg 并给出所需最短时长，把原本的真机中途 abort 变成预检错误；三段均记录实际峰值到 JSON。（2）`candidate_pose_policy` 属启动命令参数，不改代码默认值，执行时须显式传 `candidate_pose_policy:=base_axis` 才能让 `fixed_grasp_orientation_xyzw` 生效。验证：新增 4 个 re-timer 用例（保留绕行中间姿态、起终点零速、30s 超限/45s 通过、退化输入拒绝），`tests/test_paired_trajectory_protocol.py` `6 passed 1 skipped`，全量 `632 passed, 15 skipped, 1 failed`（既有 snapshot license），`rebotarm_motion`/`rebotarm_bringup` build、compileall、runner `py_compile`、`git diff --check` 通过；实测 gate 对 30s 路径给出 `peak=0.104937 -> REJECTED, lengthen to at least 35.0 s`，45s `peak=0.069958 -> accepted`，与真机观测一致。未启动 controller、未 enable、未发送任何命令；MoveIt 路径执行与 `base_axis` 策略仍无真机证据。

2026-08-14：定位并修复 approach 段规划起点错误（由本轮 path-preserving retimer 暴露的自身 bug）。两次真机运行（`real_bottle_grasp_20260814_055323.json`、`_055509.json`）均在 approach 的命令峰值预检被拒：`0.144797` 与 `0.191473 rad/s`，提示需 `72.4 s`/`95.7 s`；因此 pregrasp 走完后 approach 一步未动即中止，受控回 baseline 后 disable，operator 观察到的"只靠近和返回"即此现象，闭合从未发生。**根因**：`_request_plan()` 两次调用都在 pregrasp leg 执行之前，而 MoveIt 以当前反馈为起点规划，故 `grasp_path` 是一条从 **baseline** 出发的完整路径；此前只取终点时不可见，改为执行完整路径后该路径被真实执行，等效于 `pregrasp -> baseline -> grasp`。弧长验证精确吻合：`pregrasp->baseline 2.0756 rad` 加 `baseline->grasp 2.5194 rad` 合计 `4.5950 rad`，45 s quintic 峰值 `1.875*4.5950/45 = 0.191456`，与实测 `0.191473` 一致；而正确的 `pregrasp->grasp` 仅 `0.4438 rad`、45 s 峰值 `0.018492`，本应轻松通过。**速度门限未误报，它拦下的是一条真实错误路径**——若无该门限，机械臂会先退回起点再冲向瓶子。修复：approach 的 `_request_plan()` 移到 pregrasp 到位、取得 `pre_actual` 之后，使 MoveIt 起点为真实 pregrasp 位置；原位置改为 `approach-preview` 仅用于运动前的 joint-delta 预筛（不执行），re-plan 后再按 `--max-approach-delta-rad` 复核一次并记录 `approach_executed_max_delta_rad`。已在代码注释写明该 re-plan **不需要新的感知帧**：Cartesian `grasp_pose` 仍来自同一个已接受的 plan，只重算关节空间路线，故 `--max-plan-age-sec` 不适用于这一步。保留两段结构（operator 决定）：已确认瓶子**不在 MoveIt 碰撞场景**（无任何 `CollisionObject` 发布），故 pregrasp 在规划层面从不提供避障，其价值是运动中途的人工检查点；合并为一段会失去该检查点，且 `baseline->grasp 2.5194 rad` 在 45 s 下峰值 `0.104974` 超限、需 75 s 以上，总时长并无优势。验证：`py_compile`、全量 `632 passed, 15 skipped, 1 failed`（既有 snapshot license）、`git diff --check` 通过。未启动 controller、未 enable、未发送命令；修复后的 approach 仍无真机证据。另记录该轮 plan confidence 仅 `0.0665580`（历史成功那次为 `0.73182195`），下次执行前建议先观察 `/grasp/filtered_plan` 等待较高 confidence。

2026-08-14：**P6 单瓶抓取任务按 operator 决定暂停，等待更换齿条后重新标定。** 暂停原因与关键新事实如下。
（一）**夹爪 angle→jaw 映射常量错误，是本轮开夹超程与断齿的直接机理。** 代码 `_G_MAX_DIST_M=0.09 / _G_ANGLE_OPEN=-5.0` 隐含 `18.00 mm/rad`。两点只读测量（arm disabled、未 enable、无负载手动摆放）：机械零位 `position=0.0`；手动摆到自称 85 mm 处 `position=0.027352935791015623`，即编码器角度 `-1.519608 rad`。据此真实系数远大于 18：若限位为 85 mm 则 `55.94 mm/rad`，若为 103 mm 则 `67.8 mm/rad`，均与代码相差 3.1~3.8 倍。后果链：请求 80 mm 时代码算出目标角 `-4.4444 rad`，而 80 mm 实际只需约 `-1.43 rad`（85mm 假设下），故驱动过量约 3 倍，必然顶死机械限位后持续施力；反馈按 18 mm/rad 换算读回偏小值（如实际顶死时读 `35.682 mm`），软件误判"还差 44 mm"继续推进。**`_G_VERIFIED_OPEN_LIMIT_M=0.085` 这道保护因使用同一错误映射而完全失效。** 此前把断齿仅归因于 position hold 不释放是不完整的；两者叠加。
（二）**operator 实测最大限位实际约 `103 mm`，不是 CAD 的 90 mm、也不是此前确认的 85 mm，且断齿发生在齿条末端即最大限位处。** 断齿位置与"驱动过量顶死后继续施力"完全吻合，属机理性损坏而非偶发。由于限位处齿已断，该位置行程不可信，无法在当前硬件上完成标定。
（三）**此前三个候选系数全部作废，不得用于改代码**：`42.88 mm/rad`（由顶死反算，且受传动受力变形影响）、`55.94 mm/rad`（两点测量但基于 85 mm 前提）、`67.8 mm/rad`（按 103 mm 重算）。三者互相矛盾且共同前提已被推翻。换齿条后必须从头重新标定，并至少取零位、中间点、接近限位点三个以上无负载测量点以判定映射是否线性（齿条+连杆可能非线性，若非线性则需分段或查表而非单一比例常数）。
（四）**受影响的既有结论需要重新评估，不得沿用**：所有小行程测试的物理位移约为记录值的 3 倍（命令 10 mm 实际约 30 mm；`reached≈7.9 mm` 实际约 24 mm）；`close_force=1.0 / hold_force=0.4` 这组瓶子参数是在错误映射下标定的，真实压入深度约为记录 `15.21 mm` 的 3 倍，所谓"30% 压缩"实际可能接近压扁，必须在映射修正后重新分档；`_G_ARRIVE_TOL=0.12 rad` 对应的实际毫米容差也随系数变化（不再是 2.16 mm）。
（五）本轮已完成且不受映射问题影响的软件修复保留有效：normal position 到位/超时 neutral+idle 释放、闭合竞态方案 B（判定与发送同一持锁）、`grasp_holding` 时长上限与 neutral 释放、`grasp_gripper` close-timeout 路径 neutral 补齐、`build_retimed_path_command` 保留 planner 路径的重定时、approach 规划起点修正（re-plan 移到 pregrasp 到位之后）、命令峰值预检。这些的真机证据中涉及夹爪位移数值的部分需按新映射重新解释，但控制流行为（到位脱力、时长上限生效、被位置命令接管、路径峰值预检拦截）本身仍成立。
（六）现场已清理：controller 与 launch 均退出，`/dev/ttyACM0` 无占用，GraspNet service 已停止，无残留项目进程。本轮未修改任何映射常量或安全限位。
**恢复条件**：更换齿条 → 实测新的真实最大行程 → 多点无负载标定 angle→jaw 映射并判定线性性 → 修正映射常量与 `_G_VERIFIED_OPEN_LIMIT_M` → 低行程无负载验证开夹到位精度 → 重新分档 `close_force` → 才可恢复完整六轴抓取。

2026-08-14：operator 要求将 Web 普通夹爪位置命令的 `max_effort` 从 `0.3 N.m` 调到 `1.0 N.m`。本轮仅短暂修改 dashboard 运行时参数，未发送夹爪或六轴运动；持久化前复核到当前只完成闭合零位重设，尚未满足上一条记录要求的多点 `angle→jaw` 行程映射重标定，旧映射曾造成开夹目标过量并在机械末端断齿。为避免 `1.0 N.m` 在错误位置映射下更用力顶限位，运行时参数已恢复为 `0.3 N.m`，`/api/config` 回读确认 `max_effort=0.3`、limit=`1.5`；未修改源代码默认值。解除条件仍是完成新齿条的多点无负载行程标定并修正映射/软件限位，之后才能分级提高 Web position move 力矩。

2026-08-14：operator 明确说明当前正以 Web 测试并再次授权直接写入 `1.0 N.m`，用于自行判断是否需要行程标定。按该显式授权，将 dashboard 参数 `web_gripper_max_effort` 的源码默认值由 `0.3` 改为 `1.0`，并将当前运行节点参数同步设为 `1.0`；`/api/config` 回读为 `max_effort=1.0`、`max_effort_limit=1.5`。本轮只修改参数，未自动发送夹爪 Goal、未重启controller、未改变机械臂enable状态。已明确提醒避开机械最大开度，遇到异响、卡滞或反馈位置停止变化立即停止；该授权不消除未完成多点 `angle→jaw` 标定的已知风险。验证：`colcon build --symlink-install --packages-select rebotarm_dashboard`通过，dashboard+layering focused tests `48 passed`，compileall与diff-check通过。

2026-08-14：operator 明确指定当前物理姿态为安全位并要求记录、比较网页 Safe Home。enabled/IDLE 下连续25帧（80ms间隔）七电机反馈完全稳定、status均为1：六轴中位数 rad 为 `[-1.629091263, 0.012779236, -0.000190735, 0.008964539, 0.096323013, 0.029182434]`，夹爪 `0.0m`。网页按钮只调用 `/rebotarm/safe_home`，active SDK `ArmEndPos.safe_home()` 当前硬编码 `_q_target[:]=0.0`，因此既有网页安全位是六轴全零；当前位与它最大差异为joint1 `-1.629091263rad/-93.3401deg`，joint5另差`+0.096323013rad/+5.5189deg`，不能视为近似同一姿态。完整记录保存于 `Agent/evidence/P6/2026-08-14-operator-safe-pose.json`。本轮只记录和比较，未调用safe_home、未发送trajectory或夹爪命令，也未把网页目标改为该姿态。

2026-08-14：operator 再次调整姿态并明确要求以当前位置覆盖上一份安全位记录。最终采用 enabled/IDLE、全电机status1下连续40帧（100ms间隔）的中位数，覆盖同一 evidence JSON：六轴 rad `[-1.629091263, 0.012397766, -0.000572205, 0.053597450, -0.031280994, 0.008201599]`，夹爪 `0.0m`。Joint5/Joint6 在采样期仍有 `0.012589455rad/0.019836426rad`（约`0.7213deg/1.1366deg`）摆幅，故记录值明确标为中位数而非完全静止精确点。上一组安全位数值由本组取代；网页 `/safe_home` 目标仍未修改，仍为SDK硬编码六轴全零。本轮未发送任何运动命令。

2026-08-14：为满足 operator“必须确定执行的是规划路径”的要求，进一步收紧 `build_retimed_path_command()`：固定 50 ms 重采样之外，把每个 MoveIt source waypoint 按 joint-space max-norm 弧长对应的 quintic 时间显式插入下发 `JointTrajectory`，避免控制器在未命中采样时刻的拐点处线性插值切角；command 同时保存 `source_waypoints`、`source_waypoint_indices` 与覆盖这些字段的 SHA-256，`validate_command()` 在 action goal 前逐点验证原 waypoint 原样存在且顺序不逆。新增篡改中间拐点即 fail-closed 的测试。验证：paired protocol `7 passed, 1 skipped`，visual wiring+layering `91 passed`，全量 `633 passed, 15 skipped, 1 failed`（唯一失败仍为既有 snapshot license status `PROVISIONAL` 与 manifest `AUTHORIZED_BY_UPSTREAM_OWNER` 不一致），相关 compileall、`git diff --check`、overlay import 回读均通过。未启动 controller、未 enable、未发送机械臂或夹爪命令；现仅 localhost GraspNet service 保持 ready。完整抓取仍受夹爪 physical closed 但 Web/反馈约 `37.55 mm` 的零位/行程映射不可信阻塞，在多点 angle-to-jaw 标定和重新分档 close force 前，不得把 `--gripper-open-m 0.080` 或旧 `--grasp-close-force 1.0` 当作当前安全值。

2026-08-14：operator 指出上一套命令无法启动并确认夹爪位于机械闭合位。现场复核确认上一回复 terminal 3 的 `${VAR:?}` 四个未赋值变量会按设计直接让 shell 退出，故不能称为可直接运行命令；不是 ROS launch 损坏。使用同一环境和完整参数完成 25 秒只读启动：controller `CONNECTED_DISABLED`，MoveIt、Gemini 2/YOLO、localhost GraspNet client、candidate IK filter、PoseExecution 全部启动，期间产生 `valid` 候选；未 enable、未发送 trajectory 或 gripper command，SIGINT 后所有 ROS 项目进程退出、tty释放，8081 service继续 ready。随后 `driver_only` disabled-only 回读机械闭合夹爪 `position=0.0 m`、`status_code=0`，arm `IDLE/enabled=false/control_loop_active=false`、六轴status全0、无error，说明当前闭合零点反馈已恢复为0；这只解除 offset/零位读数异常，不证明 angle-to-jaw 行程比例已标定。启动日志：`/home/a/.ros/log/2026-08-14-16-01-07-751266-a-JIGUANG-Series-50126`。

2026-08-14：operator 多次运行完整 runner 均在 enable 前退出，报告 `real_bottle_grasp_20260814_155718..160905.json` 统一为 `pregrasp plan failed: error_code=99999`、services/legs 为空。MoveIt 同时刻日志给出确定根因：`CheckStartStateCollision` 检出 `link2-link5` 接触并中止 pipeline，不是 runner 未启动、freshness、夹爪或 action/service 缺失。当前 disabled 实时六轴为 `[-1.629091263, 0.012016296, -0.000953674, 0.028419495, 0.084115028, 0.011634827]`，夹爪闭合 `0.0 m/status0`。`/check_state_validity` 量得当前模型重叠约 `0.859 mm`；最后记录的 operator safe pose 在当前 collision model 中也仍无效。附近 sweep 显示只将 joint3 从 `-0.000953674` 缓慢减到 `-0.100953674 rad` 可到 collision-free target，最大变化 `0.1 rad`，20 s quintic 理论峰值约 `0.009375 rad/s`；该段属于从当前模型重叠姿态受控脱离，不能由 MoveIt 自身规划，须单独 guarded direct recovery 后再让完整 runner 从新 collision-free baseline 规划。未在本轮自动发送该恢复动作。

2026-08-14：operator 明确授权将 P6 指令速度预检放宽至 `0.12 rad/s`。`tools/p6_single_bottle_grasp_runner.py` 的默认值和可传上限均由 `0.09/0.10` 改为 `0.12`；`rebotarm_motion.HardwareAcceptanceGuardConfig.window_sustained_rad_s` 同步为 `0.12`，raw、瞬时、tracking 与 effort 门保持不变。边界仍为严格 `>`：`0.120000` 允许，`0.120001` 在 P6 参数校验中拒绝。验证：hardware-acceptance-guard `11 passed`、paired trajectory `7 passed, 1 skipped`、layering `18 passed`、motion compileall、runner py_compile、diff check、runner help 与无需 ROS runtime 初始化的参数边界测试通过。Codex 未启动/中断 runner、未 enable 或发送任何真机/夹爪命令；当时已有 operator 启动的 runner 显式传 `--max-command-velocity-rad-s 0.09`，不受源码变更影响。

2026-08-14：operator 明确授权 P6 单瓶抓取仅将 Joint2 的持续速度门放宽到 `0.20 rad/s`。P6 runner 新增 `--joint2-max-command-velocity-rad-s`（默认/上限 `0.20`），逐关节预检和 JSON 逐关节峰值记录；Joint1/3/4/5/6 继续使用 `--max-command-velocity-rad-s`（默认/上限 `0.12`）。`PairedTrajectoryNode` 现接受可选的 guard config，P6 仅对 Joint2 注入 raw/window sustained `0.20` 覆盖，告警与瞬时、tracking、effort 门及其他工具默认值均不变。验证：guard `12 passed`、paired trajectory `7 passed, 1 skipped`、layering `18 passed`、motion compileall、P5/P6 runner py_compile、diff check，及无 ROS runtime 的 Joint2 `0.20` 参数/预检边界测试通过；临时报告已删除。Codex 未启动或中断 runner、未 enable 或发送任何真机/夹爪命令；当时 operator 已运行的显式 `0.09` runner 不受本次源码修改影响。
2026-08-14：operator 要求 P6 的**发送前命令预检**与网页执行保持一致。P6 默认/硬上限现为：单段最大 joint delta `1.5 rad`（pregrasp、approach）、预测命令速度 `1.5 rad/s`（全轴，包括 Joint2）；原 `--joint2-max-command-velocity-rad-s` 改为纯命令预检参数。为避免将网页预检值错误注入 `src` 的 `HardwareAcceptanceGuardConfig`（该配置即时反馈门仍为 `0.30 rad/s`），新增独立 `--joint2-runtime-sustained-velocity-rad-s`，默认保留 `0.20 rad/s`。此次对齐不改变 `src` 的 `raw_velocity_corroborated_immediate`（raw `>0.30` 且 window `>0.10`）或绝对 `0.40 rad/s` 停止门，故不能掩盖真实反馈超速；18:14 的 Joint4/5/6 `.315` 级中止仍需单独修复返回与热保护策略。
2026-08-14：operator 随后明确要求移除 `src` 的持续速度停止门，以与网页遥操作一致。已删除 `raw_velocity_sustained` 与 `window_velocity_sustained` 的阈值、计时和 P6 Joint2 覆盖参数；P5/P6 因此都不再因持续 raw/window velocity 停止。保留 `raw_velocity_corroborated_immediate`（raw `>0.30` 且 window `>0.10`）、raw absolute `>0.40`、window immediate `>0.20`、tracking 与 effort 门；这些并非持续速度门。operator 随后要求 P6 不采用网页的单次最大位移限制，故已删除 pregrasp/approach 的 delta 拒绝与相关 CLI 参数，仅保留网页口径的 `1.5 rad/s` 预测命令速度预检；JSON 仍记录观测到的 delta 以供事后审计。
2026-08-14：operator 明确要求移除同一 local `HardwareAcceptanceGuard` 的即时速度反馈门。已删除 `raw_velocity_corroborated_immediate`（旧 raw `>0.30` + window `>0.10`）、raw absolute（旧 `>0.40`）与 window immediate（旧 `>0.20`）的配置和 stop 分支；raw/window velocity 仍记录为 warning telemetry，但不会取消 P5/P6 trajectory。tracking error、effort 和 controller-state 门保持；本次未操作硬件。

2026-08-14：operator 要求与 upstream 的控制层对齐，并去除本地附加的运行安全门。已删除本地 `rebotarm_motion.hardware_acceptance_guard`、P5/P6 对其 raw/window velocity、tracking、effort 取消接线与测试；同时删除 P6 预测命令速度预检及其 CLI 参数，P5 hand-eye 采集复用的本地 tracking/raw-velocity/effort 数值阈值也一并删除。P6 实际执行链保留 controller/action 的既有轨迹格式与关节限位校验，以及 controller/motor 失能或报错、关节反馈缺失/非有限/陈旧等执行健康中断；这些不是本地数值阈值安全门。验证：P5/P6 `py_compile`、motion compileall、98 passed/1 skipped focused tests 和 diff check 通过；全量为 629 passed、15 skipped、1 failed，唯一失败是既有 `UPSTREAM_PROVENANCE.json` license status 断言（期望 `PROVISIONAL`、实际 `AUTHORIZED_BY_UPSTREAM_OWNER`）。未启动 controller、未 enable、未发送真实臂或夹爪命令。

2026-08-14：operator 要求将当前实际姿态记录为网页 `/rebotarm/safe_home` 的安全位。只读采集 40 个 `/rebotarm/joint_states` 样本，六轴中位数且每轴 span 均为 0：`[-1.549363136291504, 0.01659393310546875, -0.02002716064453125, -0.00858306884765625, 0.10395240783691406, 0.00133514404296875] rad`。ArmStatus 为 disabled/IDLE、六轴 status0/no error；MoveIt `check_state_validity(group=arm)` 为 valid、0 contacts。证据：`Agent/evidence/P6/2026-08-14-web-safe-home-record.json`。controller 原先把空默认 `[]` 推断为 ROS 2 `BYTE_ARRAY`，故运行中 `ros2 param set` double-array 被拒绝、参数没有改变；修复为 `_WEB_SAFE_HOME_JOINT_POSITIONS` 明确浮点默认，重建 `rebotarmcontroller` 后，**下一次 controller 启动**将作为网页 safe_home target。当前运行节点仍需 operator 重启才加载该参数；本轮未调用 `/safe_home`、未 enable、未发送轨迹或夹爪命令。验证：controller build、safe-home/P0/layering focused `63 passed`；全量 `630 passed,15 skipped,1 failed`，唯一既有失败仍为 upstream snapshot license status 断言。

2026-08-14：operator 根据异常 P6 bottle trial 的 candidate confidence `0.1714014` 要求设置下限 `0.6`。P6 runner 的 `_valid_bottle_plan()` 现要求 `candidate.confidence >= 0.6`；低置信度 plan 不写入 latest plan，runner 在 enable、夹爪 open 或 trajectory action 前等待，至 `--plan-timeout-sec` 后 fail closed。阈值仅作用于 `tools/p6_single_bottle_grasp_runner.py`，不改变网页或 candidate filter 全局输出。边界验证 `0.5999` 拒绝、`0.6000` 接受；runner compile、focused `98 passed,1 skipped`、diff check通过；全量 `630 passed,15 skipped,1 failed`（唯一既有 upstream provenance license status 断言）。未启动controller、未 enable、未发送动作。

2026-08-31：修复 Ubuntu Gemini 2 点云路径的 QoS 不兼容。原视觉节点在 Ubuntu profile 下把 depth Image 发布为 `RELIABLE`，但 CameraInfo 使用 `qos_profile_sensor_data/BEST_EFFORT`，Jazzy `depth_image_proc point_cloud_xyz_node` 因 `RELIABILITY_QOS_POLICY` 不兼容收到 0 个 CameraInfo/0 个同步对。现 color/depth CameraInfo 与对应 Image 共用 `image_qos`。实机重启后 `/camera/depth/camera_info` 为 `RELIABLE`，`/camera/depth/points` 成功输出 `PointCloud2`（640x480、point_step 16、frame `camera_depth_frame`）。focused `13 passed`、compileall、vision symlink build通过；未启动 MoveIt/controller/串口或发送动作。证据：`Agent/evidence/P6/2026-08-31-gemini2-pointcloud-qos.md`。

2026-08-31：新增只读 Open3D 查看器 `tools/view_graspnet_scene_cloud.py`，直接复用 GraspNet backend 的 `build_scene_cloud()`，显示 `sample_cloud()` 之前、同一 `0.05-1.5 m` 深度门后的完整 XYZ+RGB 场景。Gemini 2 实测每次约 `119,820-123,648` 点、z=`0.249-1.499 m`、RGB/depth skew约 `20.8 ms`，窗口成功创建；focused `18 passed,1 skipped`、py_compile/help/diff check通过。未启动 HTTP GraspNet、MoveIt/controller/串口或动作。证据：`Agent/evidence/P6/2026-08-31-graspnet-full-scene-viewer.md`。

2026-08-31：按 operator 指定先实现抓取候选低置信度修复第一项：GraspNet network input / 网络输入改为最高置信度 YOLO detection 的目标点云，完整 workspace-filtered scene cloud / 工作区过滤场景点云继续只供 model-free collision detection / 无模型碰撞检测；不改候选投影后筛、MoveIt IK/排序、P6 `0.6` 置信度门、jaw-width 门或执行门。新增 `build_detection_cloud()`，保留原图像像素坐标与相机内参，优先 polygon、否则使用 clamp 后 bbox；场景或目标框内无有效深度时 fail closed。单帧只读 Gemini 2 实测 bottle confidence `0.925`、bbox `[257,49,397,462]`，完整场景 `120596` 点、目标输入 `26716` 点（`22.2%`），z=`0.202-0.658m`、同步差 `20.4ms`。focused `20 passed,1 skipped`、layering `18 passed`、compile/diff-check通过；全量 `634 passed,15 skipped,1 failed`，唯一失败仍为既有 upstream provenance license status 断言。未启动 controller/MoveIt execution/串口或发送动作；为不打断 operator 现有进程，PID 68625 的 GraspNet service 未重启，须下次重启服务后才加载新 wrapper。证据：`Agent/evidence/P6/2026-08-31-graspnet-yolo-object-cloud.md`。

2026-08-31：operator 随后要求关闭旧服务并测试修改效果。PID `68625` 已 SIGINT 退出且 8081 释放；首次替换实例因未继承模型环境变量而 health=`model_root_or_checkpoint_unset`，立即停止，随后显式使用 `.local-models/graspnet-baseline`、`.local-models/checkpoints/checkpoint-rs.tar`、`cuda:0` 启动 PID `82647`，health=`ready/backend_configured=true`。修改后直接 HTTP 10 帧全部非空、每帧 10 candidates，top raw score `0.755371-1.180294`/median `0.951344`；ROS `/grasp/graspnet_candidates` 首个 10 帧同样全部 10 valid，top `0.690181-1.384870`/median `1.049310`。但按 P6 的 score `>=0.6` + jaw width `<=0.082m` 组合门复测，第二组只有 `3/10` 帧存在 eligible candidate（各 `4/8/10` 个，eligible top `1.069803-1.276361`），其余 `7/10` 仍被 jaw-width 门拦截；说明第一项已明显消除 full-scene confidence dilution，但完整 MoveIt 输入仍受框内背景/宽度稳定性阻塞，下一项应做 object/background depth separation，而不能宣称抓取链已闭环。临时 ROS client 已停止；最终仅 Gemini2/YOLO PID `61883` 与新 GraspNet PID `82647` 保持运行，未启动 MoveIt/controller/串口或动作。证据同上。

2026-09-01：上述效果测试后的最终交接复核中，修改后 GraspNet PID `82647` 仍监听 `127.0.0.1:8081` 且 health=`ready/backend_configured=true`，但原 Gemini2/YOLO vision launch PID `61883` 已不在进程列表或 ROS graph；Codex 未擅自重启相机链。前述 HTTP/ROS 20 帧测试在 vision 尚运行时已完成，结果仍有效；当前运行态只有 GraspNet 服务，不应误报为完整感知链正在运行。

2026-09-01：按 operator“继续下一步”实现 YOLO detection 内 foreground/background depth separation / 前景背景深度分离。目标 mask 优先 polygon、否则 clamp bbox；取框中央 `35% x 35%` 有效深度中位数作为 anchor，少于 `32` 个 seed pixels 时 fail closed，只保留 `[anchor-0.03m, anchor+0.08m]`，完整 scene cloud 仍仅供 collision detection；未改投影后筛、MoveIt、P6 score `0.6`、jaw width `0.082m` 或执行门。focused `22 passed,1 skipped`、layering `18 passed`、compile/diff-check通过；全量 `636 passed,15 skipped,1 failed`，唯一失败仍为既有 provenance 断言。实时视觉已重启，但当前画面无 bottle，YOLO 最高目标为 chair；单帧点云级验证显示 bbox cloud `27056` points/z `0.328-1.499m` 收敛为 foreground `3048` points（`11.3%`）/z `0.635-0.744m`，证明深度带实际生效。新服务 PID `101694` ready；chair 10 帧 generic regression 为 9 非空、top score `0.117384-0.431481`/median `0.271045`、0 帧通过 bottle-specific score+width gate，不能冒充瓶子验收。当前 vision PID `100980` 与 service 保持运行，等待 operator 把瓶子放回画面后按同一 10 帧口径复测 raw score、jaw width 与组合门；未启动 MoveIt/controller/串口或动作。证据：`Agent/evidence/P6/2026-09-01-graspnet-foreground-depth-separation.md`。

2026-09-01：operator 将瓶子放回后完成第二项同瓶位闭环诊断与修复。bbox+foreground-depth 首轮 20 帧为 `19/20` 非空、top score median `0.472673`、score `>=0.6` + jaw `<=0.082m` 组合门 `0/20`。确定根因之一是 `yolo26m-seg` mask 全链路丢失：Ultralytics converter 未复制 `result.masks.xy`，ROS `Detection2D` 因而 `has_mask=false`，HTTP contract/local client 也只传 bbox，使 backend 的 polygon 路径不可达。现已接通 `result.masks.xy -> Detection2D.mask_polygon_xy -> optional HTTP mask.polygon_xy -> backend mask_polygon_xy`；非法 converter polygon 降级为 no-mask，畸形 HTTP polygon contract 拒绝，bbox-only producer 兼容。重建/重启后 bottle `has_mask=true`（样本264 vertices）；单帧 `25828`-point bbox/z `0.293-0.917m` 经 polygon 成 `21964`/z `0.293-0.500m`，再经 depth band 成 `21594`/z `0.293-0.390m`。生产 ROS 路径 20 帧全部非空，top score median 升至 `0.997143`，组合门改善到 `4/20`（20%），eligible top median `0.763318`、jaw median `0.076272m`；但 `16/20` 仍无组合门候选，高分候选 jaw 多为 `0.085-0.093m`，故修复有效但完整 MoveIt 输入仍未稳定闭环。focused `42 passed,1 skipped`、vision symlink build/compile/diff-check通过；全量 `638 passed,15 skipped,1 failed`，唯一既有 provenance 失败。首次相机重启出现一次 `usbEnumerator openUsbDevice failed`，清理失败 launch 后单次重试成功。最终 vision PID `104848`、GraspNet PID `104538` ready，临时 client 已停，无 MoveIt/controller/串口或动作。证据同上。

2026-09-01：纯感知实测确认 production `max_grasps: 10` 在 jaw-width 过滤前截断会误丢当前瓶子场景的合格候选。临时只启动 GraspNet ROS client 并设 `max_grasps:=100`，30 帧全部非空、28 帧有超过10个投影后候选；按 P6 `score>=0.6` + `jaw_width<=0.082m` 统计，Top-10 仅 `7/30` 帧有合格候选，而完整 Top-100 为 `14/30`，其中 `7/30` 帧属于 Top-10 全失败但第11名以后存在合格候选。代表性恢复项包括 rank17 `0.821545/0.081336m`、rank19 `0.980400/0.080483m`、rank12 `0.929990/0.071274m`。结论是排序/截断顺序已构成当前 false negative，不应放宽物理 jaw 门；下一实现应在投影后先做 jaw-width prefilter，再截 Top-10。临时 client 已停止，恢复到原 vision + localhost service、无 MoveIt/controller/action 的运行态。证据：`Agent/evidence/P6/2026-09-01-graspnet-top10-before-jaw-width-audit.md`。

2026-09-01：已修复 GraspNet Top-10 与 jaw-width 的筛选顺序。`max_jaw_width_m` 作为可选、正有限 HTTP contract 字段由 ROS node 传至 localhost backend；Ubuntu 配置固定 `0.082m`，两个 visual-grasp launch 复用同一 `candidate_max_jaw_width_m` 同时驱动 GraspNet 预过滤和下游 IK 门，避免阈值漂移。backend 当前顺序为 collision/NMS/score sort -> YOLO projection -> jaw-width filter -> Top-10。单测证明高分超宽候选不会再占据低分可抓候选的输出槽。修改服务重启后生产参数回读 `max_grasps=10/max_jaw_width_m=0.082`，同瓶位30帧全部非空、发布候选宽度违规0、组合门可用 `13/30`，相较修复前 Top-10 `7/30` 接近翻倍，并与 Top-100 audit 的 `14/30` 基本一致。focused `117 passed,1 skipped`、layering18、两包symlink build、compile/diff-check通过；全量 `640 passed,15 skipped,1 failed`，唯一既有 provenance 断言失败。临时 ROS client 已停，vision保持运行，修改后 GraspNet PID `112166` ready；无 MoveIt/controller/action。证据：`Agent/evidence/P6/2026-09-01-graspnet-jaw-width-before-top10-fix.md`。

2026-09-01：按 operator 要求独立复测 jaw-width-before-Top-10 修改效果。生产参数回读仍为 `max_grasps=10/max_jaw_width_m=0.082`；30帧中 `29/30` 非空且非空帧全部为 bottle，候选数中位10，所有发布候选 jaw-width 违规为0。`15/30` 帧至少有一个 `score>=0.6` 候选，共45个高分候选；top score `0.168620-1.129269`、median `0.636949`，top width `0.054450-0.081900m`、median `0.078830m`。两次修复后独立结果为 `13/30` 与 `15/30`，均显著高于修复前 Top-10 `7/30`；但本次仍有1个空帧，说明随机生成稳定性尚未完全闭环。临时 ROS client 已停止，恢复 vision + localhost GraspNet、无 MoveIt/controller/action。证据同 `Agent/evidence/P6/2026-09-01-graspnet-jaw-width-before-top10-fix.md`。

2026-09-01：已为 Ubuntu localhost GraspNet 增加逐帧结构化阶段计数日志，事件名 `graspnet_stage_counts`，记录 `scene_points/object_points/raw/after_collision/after_nms/after_score_sort/after_projection/after_jaw_width/published/empty_reason/published_top_score` 及请求时间戳、frame、class和生效的Top-N/jaw门。ThreadingHTTPServer 下使用service infer lock确保GPU推理与对应计数日志不串帧。空结果会标记首个清空阶段，也区分 no detection/empty scene/empty object。真实 bottle 日志已验证：一帧 `149->147->147->147->53->10`、top score `0.921357`；另一个空帧 `2->1->1->1->0->0` 明确标记 `jaw_width_filter_empty`。观察样本 raw在`2-352`间大幅波动，collision仅少量损失，NMS与projection在该样本未减少，jaw-width常去掉较大比例；这是初步观测而非固定分布benchmark。focused `25 passed,1 skipped`、layering18、compile/diff-check通过；全量`641 passed,15 skipped,1 failed`，唯一既有provenance断言失败。临时client已停，vision保持运行，修改后service PID `116443` ready，无MoveIt/controller/action。证据：`Agent/evidence/P6/2026-09-01-graspnet-stage-count-logging.md`。

2026-09-01：固定输入与固定采样对照已把剩余 GraspNet 波动定位到 `sample_cloud()` 的随机 `np.random.choice`。实验1把同一 RGB/depth/intrinsics/bbox/263-vertex mask 原样HTTP重放30次，仅刷新transport time，结果published count仍`2-10`、top score `0.105284-0.885151`、仅`6/30`达到0.6，排除实时相机/mask变化为主因。实验2在第二固定帧（scene `137676`、object `22242`、network input `20000`）中，固定同一 sampled point set 重复模型10次得到唯一signature：raw49、collision/NMS/projection44、jaw11、published10、score `0.2386668175` 全部完全一致，证明固定点集下模型/CUDA确定；只改为每次重新sample则10次signature全不同，raw `6-23`、jaw `4-11`、published `4-10`、score `0.237238-0.860415`。结论：直接波动源是随机点云采样。固定seed只能复现一个可能很差的样本，本实验固定样本score仅0.238667，故下一修复应是稳定且geometry-aware的采样覆盖，而非简单锁seed；multi-sample ensemble可提高召回但有延迟代价。临时direct backend已退出，localhost service PID `120168`恢复ready，vision保持运行，无MoveIt/controller/action。证据：`Agent/evidence/P6/2026-09-01-graspnet-fixed-input-sampling-diagnosis.md`。

2026-09-01：实测 deterministic geometry-aware sampling 的延迟成本。当前瓶子帧 scene `136332`、object `21330`、输入`20000`，keep ratio `93.7647%`。200次CPU microbenchmark：production random choice median/p95 `0.460/0.478ms`；确定性voxel-stratified 1mm `1.532/1.591ms`、2mm `1.399/1.445ms`、3mm `1.326/1.383ms`，相对random median净增仅`0.865-1.072ms`，重复输出byte-for-byte deterministic。相同输入12次当前localhost HTTP GraspNet latency min/median/p95/max=`202.982/221.269/231.362/238.006ms`，故deterministic voxel sampling新增约`0.39%-0.48%`，不到0.5%，延迟不是实际阻塞。该结果仅证明prototype速度，不代表1/2/3mm质量已验收；下一步应以候选稳定性、score gate、jaw分布和空间覆盖选default。未benchmark full FPS，因为当前保留93.8%点，FPS复杂度不划算。生产代码未改，service PID `120168`与vision保持运行，无MoveIt/controller/action。证据：`Agent/evidence/P6/2026-09-01-graspnet-deterministic-sampling-latency.md`。

2026-09-01：已将 production `sample_cloud()` 改为 deterministic geometry-aware sampling：固定2mm体素逐层覆盖，选中点再按原始index的SplitMix64稳定哈希排列，避免axis-sorted点序触发GraspNet内部采样偏置。重要修正：朴素axis-sorted体素输入虽只增加约1ms selector开销，却使raw约`1016-1024`、完整推理`415.6-513.9ms`且score仅`0.239-0.421`，因此被拒绝；hash排列恢复raw `75-106`、完整推理`194.5-208.3ms`。8个独立瓶帧A/B中，random 5次/帧为score门`13/40`（32.5%）、median`0.471260`，2mm+hash为`5/8`（62.5%）、median`0.647587`，3mm为`4/8`，故选2mm。生产服务同一输入10次得到唯一signature、均10 candidates、score固定`0.7543995380`；但实时变化30帧仍仅`5/30`达到0.6、median`0.417203`，说明相同点云的随机波动已消除，剩余低分主要随实时cloud/raw model response变化，不能宣称置信度问题完全关闭。focused `29 passed,1 skipped`、layering18、全量`645 passed,15 skipped,1 failed`（唯一既有provenance断言）。首次视觉重启遇USB反复reset，在确认无占用后对明确Gemini2 `2bc5:0670/AY6V16300BP`单次`usbreset`恢复。最终vision+修改后service ready，临时client已停，无MoveIt/controller/action。证据：`Agent/evidence/P6/2026-09-01-graspnet-deterministic-sampling-fix.md`。

2026-09-01：在无硬件 `plan_only` 下完成修复后整体 MoveIt 候选链实测。20秒内收到48个 GraspNet frame（47非空、约2.4Hz），candidate filter输出18个plan，其中12 valid、6个为`no IK-reachable grasp candidates`；processed/raw `37.5%`、valid/processed `66.7%`，raw到filtered median/p95/max约`926/1281/1380ms`。等待`18.605s`获得fresh P6合格瓶子plan（confidence `0.887913`、jaw `0.073890m`），MoveIt分别对pregrasp/grasp plan-only成功：`102.592ms/62 points/6.025s`与`137.009ms/71 points/6.935s`。1187个joint-state samples最大变化`0.0rad`，无执行。链路已功能贯通；剩余是实时高分frame稀疏以及Top-10 IK/collision约0.9-1.4s导致busy-drop的吞吐问题，不是完全断链。临时MoveIt/client/filter/sim controller均已停止，原vision与PID `131430` localhost GraspNet保持ready。证据：`Agent/evidence/P6/2026-09-01-deterministic-graspnet-moveit-candidate-chain.md`。

2026-09-01：按operator确认将P6 runner-local `MIN_CANDIDATE_CONFIDENCE`从`0.6`降为`0.4`，不改GraspNet、jaw门、MoveIt IK/collision或controller。TDD边界为`0.3999`拒绝、`0.4000`接受；旧实现RED、修改后GREEN，相关focused `26 passed,1 skipped`、py_compile/diff-check通过；最终全量`646 passed,15 skipped,1 failed`，唯一失败仍为既有provenance license status断言。无硬件plan-only同场景A/B两个30秒窗口合计146 raw frames：raw score门`>=0.4`为`39/146=26.7%`，旧`>=0.6`为`12/146=8.2%`；candidate filter输出37 plans、31 valid，最终P6合格plan新门为`4/37=10.8%`、旧门为`3/37=8.1%`，新增1条confidence `0.416523`，相对plan数+33.3%但绝对仅+2.7个百分点。filter只处理`37/146=25.3%` raw frames，故降门明显扩大raw候选池，却未绕过IK/joint6/busy-drop主瓶颈。两轮3532 joint samples最大变化`0rad`，无执行；临时链已停止，原vision和PID`131430` GraspNet ready。证据：`Agent/evidence/P6/2026-09-01-p6-confidence-threshold-0.4.md`。

2026-09-01：0.4阈值下再次独立运行60秒无硬件候选链：127 raw frames全部非空，37/127（29.1%）含score>=0.4；candidate filter仅输出38 plans（processed/raw 29.9%），31 valid（81.6%），最终P6合格4/38（10.5% filtered、12.9% valid、3.15% raw），eligible confidence min/median/max `0.407330/0.593772/0.773313`。第一条raw过门仅`0.175s`，第一条任意valid filtered plan为`0.945s`，但第一条P6合格plan需`23.045s`（confidence `0.472715`、jaw `0.081440m`）；随后MoveIt pregrasp/grasp分别`100.843/124.999ms`成功，从采样开始到两条完整plan ready共`23.278s`。3560 joint samples变化`0rad`，无执行；临时链已停止，原vision和PID`131430` GraspNet ready。结论：首路径等待主要耗在candidate filter/IK可用性，不在最终OMPL（合计约226ms）。证据：`Agent/evidence/P6/2026-09-01-p6-candidate-first-path-repeat.md`。

2026-09-01：candidate IK filter 的 busy-frame 直接丢弃已改为 latest-wins / 最新帧优先单槽调度：一个 active worker 串行处理，忙时只保留一个 pending，后到帧替换旧 pending；异常帧完成后仍继续最新 pending，不形成无界积压。confidence、jaw finite/range 与 geometry/workspace gate 已移到 MoveIt IK/collision 前，production/preview filter threshold 显式为 `0.4`，jaw range保持`0.006..0.085m`；新增 cumulative queue 与逐帧stage/latency日志。60秒无硬件plan-only实测150 raw、149 filter results，coverage `99.3%`，较上一轮`40/122=32.8%`显著提高；14帧包含score>=0.4候选，3条valid且P6 eligible，首条eligible `4.849s`，较上一轮`14.899s`减少约`10.05s/67.5%`。raw-to-filter median/p95/max为`6.629/243.698/896.715ms`；3597 joint samples最大变化`0rad`。日志实际出现忙时合并（代表性`received115/started113/completed112/coalesced2/busy=true`、processing `989.3ms`、pending age `292.4ms`），证明不再因单帧IK耗时直接丢掉其后所有帧。`149`输出包含低分帧的快速fail-closed invalid结果，不等于149条IK-valid；剩余瓶颈是高分候选稀疏与geometry/IK可达性。focused `84 passed`并补充candidate-gate `4 passed`、layering18、两包build/compile/diff通过；全量`654 passed,15 skipped,1 failed`，唯一仍为既有snapshot license manifest断言。本次仅sim action server，无controller/tty/trajectory，临时链已清理，原vision与localhost GraspNet保留。证据：`Agent/evidence/P6/2026-09-01-candidate-filter-latest-wins.md`。

2026-09-01：按operator要求再次运行修改后的完整候选链。60秒software-only plan-only窗口收到164 raw（159 non-empty、1378 candidates），26帧含confidence>=0.4且通过raw class/jaw门；filter输出153，IK/collision valid与P6 eligible均为8。最终通过率为`8/164=4.88% raw`、`8/153=5.23% filtered`、`8/26=30.77% score-qualified`，较上一轮post-fix `3/150=2.0% raw`明显回升；首条eligible `4.748s`，与上一轮`4.849s`一致且明显快于pre-fix `14.899s`。eligible confidence min/median/max=`0.442624/0.663637/0.845689`，jaw=`0.060320/0.076944/0.083428m`；其余145个plan均为`no IK-reachable grasp candidates`。raw到filter median/p95/max=`5.895/322.608/1178.499ms`。164到153的11帧差与测量窗口周围queue `coalesced`增加11一致；最重一帧10个precheck/20个geometry variants耗时约1175.8ms，latest-wins按设计合并过载而非无界积压。另取fresh plan（confidence0.695348、jaw0.076742m）执行最终MoveIt `execute=false`：pregrasp/grasp均成功，`53/66` points，请求`102.512/102.058ms`；两个窗口合计观察2180 joint samples最大变化`0rad`。临时链均清理，只有原vision/TF与健康8081服务保留，tty空闲。证据：`Agent/evidence/P6/2026-09-01-candidate-filter-latest-wins-repeat.md`。

2026-09-02：operator 确认 replacement-rack angle-to-jaw calibration / 替换齿条角度到开度标定与当前 `0.080m` 安全最大开度，并确认目标仍为可压缩空塑料瓶；下一次操作范围由旧 runner 的夹紧/松开/回baseline扩展为一次真实 `pregrasp -> approach -> force grasp -> lift -> safe retreat -> safe_home`。该请求授权 operator 获取并自行运行命令，不授权 Codex 自动 enable 或发送动作。完整链使用 `VisualGraspExecutorNode`，保留每段 MoveIt `execute=false` trajectory precheck / 轨迹预检、禁用自动重试；旧45/45/20/45秒 runner 不用于本次抬瓶。为避免携瓶回安全位途中30秒 hold timeout 自动脱力，controller hold timeout 设为120秒；成功到 safe_home 后须先人工托住瓶子、显式开夹，再 disable。任何 recoverable failure / 可恢复失败先请求 visual-grasp stop 与 trajectory stop并保持 enabled hold，不得在离开baseline时直接disable。

2026-09-02：对上述完整视觉执行链做失败语义复核：`VisualGraspExecutorNode` 在 `auto_retry_enabled=false` 时，任一 stage failure 仅调用 motion-execution stop 与 controller `trajectory_stop`；低层 `stop_active_motion()` 停止发送、把六轴目标更新为当前位置并保持 enabled hold，不调用 `safe_home` 或 `disable`。因此这条链当前只在**成功路径**通过 `safe_home_after_grasp=true` 自动执行 lift/safe-retreat/safe_home；失败路径尚未接入 `rebotarm_motion.real_failure_recovery.recover_real_failure()` 已有的“healthy hold -> controlled baseline return -> verify -> disable”流程。夹爪 `grasp_holding` 独立保持，120秒 timeout 到期只发 neutral 脱力，不自动开夹，也不失能六轴。另因 launch 显式 `shutdown_safe_home=false`，直接 Ctrl-C 会走 controller shutdown 并失能/断连，不会先回安全位；异常时应先调用 stop 服务而不是用 Ctrl-C 代替恢复。该差异在首次真实抬瓶前必须向 operator 明示。

2026-09-02：operator 决定暂不把 `real_failure_recovery` 自动接入 VisualGraspExecutor。现有完整视觉链运行时可额外启动**单独** `rebotarm_dashboard/TeleopStatusPanelNode`，复用既有 controller/action/services，作为异常后的人工 stop/status/safe_home 控制面；不得同时启动 `rebotarm_app.launch.py` 或 `teleop_system.launch.py`，前者重复 MoveIt/controller，后者在 `use_hardware=true` 重复controller、在false时引入同名状态发布路径。网页 `safe_home` 会先请求 `trajectory_stop` 再调用 `/rebotarm/safe_home`，但不是MoveIt碰撞规划，必须在状态健康、控制循环有效、现场路径清空后使用。Open3D `view_graspnet_scene_cloud.py` 是只读订阅器，可与完整视觉/真机执行并行；其默认 `/grasp/graspnet_candidates` 叠加的是 camera frame 中 Top-N raw GraspNet poses，不代表 candidate IK filter最终选中、base-axis/TCP变换后的实际执行pose。

2026-09-02：真实完整视觉启动现场发现命令组合 `start_visual_ready` 默认true + `move_to_visual_ready_on_start=false` 会卡住 post-visual-ready launch gate。`VisualReadyNode.main()` 只有进入 `auto_move_on_start` 分支后才检查 `exit_after_startup_move`；auto move为false时直接永久spin，故 `OnProcessExit` 不触发，Ubuntu vision、GraspNet ROS client、candidate IK filter、PoseExecution与VisualGraspExecutor均未启动。MoveIt本身实际已在运行，现场存在 `/move_group`、`/plan_kinematic_path`、`/compute_ik`、`/check_state_validity`、`/move_action` 与真实 `/rebotarm/follow_joint_trajectory`；表象上的“未规划”来自后半链未放行。当前无需自动移动到visual-ready的正确运行时绕行是显式 `start_visual_ready:=false`（该条件直接启动post actions），保留 `move_to_visual_ready_on_start:=false`；未获修复授权前不改source。现场controller仍为disabled/IDLE、status0、无errors。另确认状态topic真实名称为 `/rebotarm/arm_status`，此前回复中的 `/rebotarm/status` 是错误命令，应更正。

2026-09-04：完成夹爪突发全行程/反馈陈旧的软件修复，未进行实体运动。operator 已确认替换齿条后的映射与网页遥操作正常，继续沿用约 `0..90 mm <-> 0..-5 rad`，本轮未改映射、零点或机械限位。两份失败报告均确定停在初始 `SetGripper(0.080 m, 1.5 N.m)`，当时代码已移除旧 ramp 并一步下发最终约 `-4.44 rad`，这是突发开夹的已确认软件机理；固定约3秒完成超时、反馈异常被吞、无反馈更新时间且读取异常仍可能报 status0 也均为已确认缺陷。实体实际运动较远而报告 `2.324/2.668 mm` 是否由 stale feedback 直接造成仍属高概率推断，须经授权后的受控真机复测确认。修复采用独立可配置 position ramp `0.5 rad/s`、position move 默认 torque cap `1.0 N.m`（参考当前 Web 遥操作默认，硬上限仍为 `1.5 N.m`）、距离/速度加 `1.5 s` margin 的超时、`0.25 s` feedback stale gate；每次命令从显式 request+poll+validated state 后的当前位置起坡。反馈异常或 stale 时立即终止目标推进、neutralize、返回明确失败/status255并记录命令/反馈/时间戳/原因；service 与 GripperCommand action 共用同一完成、取消和失败判定。motorbridge Python/ABI `0.4.6` 未暴露底层 native fresh-state timestamp API，故本轮不升级依赖，以成功 request/poll/validated get_state 的应用层 monotonic 时间标记新鲜度。软件验证：layering `18 passed`；全量 `681 passed, 15 skipped`；要求的 compileall、controller/launch compileall与 scoped diff-check 均通过。当前只读检查无项目硬件进程、ROS节点或 `/dev/ttyACM0` 占用，因此无法在不启动controller的前提下重新确认电机实时 disabled/status0；最后两份失败证据记录为 disabled/IDLE/status0。实体复测继续等待 operator 明确授权。

2026-09-04：网页遥操作同一命令在清理全部 ROS/project 残留后仍连续卡在 controller connect，较早一次明确日志为 `arm feedback refresh failed: joint1 feedback unavailable after 3 attempts`；网页节点本身均已在 `127.0.0.1:8088` 启动。高可信根因回归位于 `init_gripper()`：当前版本在六轴首次 fresh-feedback validation 前启动了夹爪 500 Hz request/poll loop，与共享 `/dev/ttyACM0` 上的六轴初始化事务竞争；这也违背 P0 已验证的“闲置夹爪 loop 延迟启动”方案。TDD 用真实 `init_gripper()` 行为测试先复现 `_gripper_loop_running=True`，随后移除初始化启动；position/grasp 两个实际命令入口仍按需启动 loop。软件验证：P0 `46 passed`、action `3 passed`、configuration `2 passed`、layering `18 passed`、全量 `681 passed,15 skipped`；正确 ROS overlay 下 visual wiring `73 passed`；`rebotarmcontroller` 与 `rebotarm_bringup` symlink build、要求的 compileall、launch `--show-args`、scoped diff-check均通过。安装空间回读 `init_starts_loop=False / position_starts_loop=True / grasp_starts_loop=True`。本轮未启动controller、未enable、未发送动作；清场后无ROS/project进程、8081/8088监听或串口占用。真实网页启动是否恢复须由operator随后执行命令确认。

2026-09-04：operator/另一终端加载修复后的安装空间完成两次真实 `rebotarm_app.launch.py` 启动，controller 两次均在约0.5秒内记录 `lifecycle=CONNECTED_DISABLED; explicit enable required`，原 joint1 feedback unavailable/连接卡住未再出现，因此本次启动回归已由真实启动闭环确认。第一次运行约77秒后由该终端Ctrl-C，第二次当前继续运行，网页 `127.0.0.1:8088` 可访问且API已有六轴/夹爪反馈；期间出现的 enable、夹爪位置命令与随后 disable 均来自外部会话，不是Codex发送。最后只读API快照为 arm `enabled=false/IDLE/no errors`，但 gripper 项仍缓存 `status_code=1`，不能把它当作新的 disabled fresh feedback；当前controller继续占用tty，因属于清场后新启动的operator/并发线程会话而未擅自关闭。

2026-09-04：按 operator 确认完成夹爪/六轴共享总线的软件调度修复，工作位于独立 `codex/gripper-unified-bus-fix` worktree，未覆盖并发线程的原工作区。对照官方 controller `e134941b71236523e831f15b470bc81186b0649f` 与 SDK `1bcd81b22c182ec257bf04f5746e1e3d556a5f1c` 后，将六轴和夹爪命令统一到唯一 500 Hz vendor control loop；移除独立夹爪 500 Hz writer；七电机按 controller 先集中 request、再单次 poll，默认 50 Hz（可配置 20..100 Hz），ROS 状态定时器默认 100 Hz 且只消费验证缓存。50 Hz 是硬件反馈采样率，不是夹爪运动速度；夹爪仍由 500 Hz loop 发送，position ramp 保持 Web 遥操作基线 `0.5 rad/s`，position torque cap 保持 Web 默认 `1.0 N.m`/硬上限 `1.5 N.m`，映射和零点未改。反馈更新时间统一使用成功完整批次后的 monotonic 时间，默认 stale gate 收紧为 `0.15 s`；失败批次不推进任何时间戳，正在执行的夹爪命令停止目标推进并由唯一 writer 发 neutral，service/action waiters 只排队不直接写串口。SDK 缓存读异常、批次错误或 stale 均向状态返回 `255/unknown`，不会再伪装成 status0/1；ArmStatus 同步带 `ARM_FEEDBACK`/`GRIPPER_FEEDBACK` 原因，且不把 stale joint positions 重打新时间戳。MotorBridge Python/ABI 仍为 `0.4.6`，无 Python `request_fresh_state`/原生 timestamp 暴露，未升级依赖。共享总线竞争拓扑和旧代码可造成 callback/status starvation 已确认；两次真实过度打开是否完全由该竞争导致仍是高可信推断，必须等待 operator 授权后的受控实体复测。自动测试覆盖单 writer、500:50 调度、七电机 batch、ROS cache-only、异常/stale/status fail-closed、service/action completion 与网页入口参数透传；本轮未启动/restart controller、未 enable、未发送动作。

2026-09-04：operator 实测统一总线修复后确认 `1.5 rad/s` position ramp 可接受，并要求同步网页遥操作与视觉抓取夹取启动。真实硬件 launch `driver_only`、`interactive_system`、`moveit_hardware`、`rebotarm_app`、`visual_grasp_system` 的 `gripper_position_max_speed_rad_s` 默认统一从 `0.5` 改为 `1.5 rad/s`；其中 `visual_grasp_system` 继续把该参数透传给实际 controller。代码级 `reBotArmController`/`HardwareManager` fallback 仍为 `0.5 rad/s`，position torque cap 仍为默认 `1.0 N.m`/上限 `1.5 N.m`，50 Hz feedback、0.15 s stale gate、映射与零点均未改。本次仅固化 launch 默认值，不重启当前 controller、不发送动作。

2026-09-04：完成 `real_bottle_grasp_20260904_110718` 失败的软件修复，未做实体运动。直接失败不是未规划：瓶子 plan confidence `0.551736 > 0.4`，pregrasp 与 approach 均已生成；初始开夹命令目标 `0.080 m/-4.444444 rad`，实际反馈 `0.077904551 m/-4.328031 rad`，误差 `0.116414 rad/2.095449 mm` 小于 `_G_ARRIVE_TOL=0.12 rad/2.16 mm`。到位释放逻辑却把 `_gripper_goal_angle` 从原目标改写为实际值，已持有原目标的 waiter 因 ownership mismatch 返回 false，造成“夹爪打开后停止”，且 `gripper_command_error=None`。现保留原 goal 作为命令所有权，只有唯一 500 Hz writer 成功发 neutral 后才返回 success；in-tolerance 但不等于目标的并发回归测试已覆盖。另修正共享反馈批次时间戳：freshness 从完整 request/poll/validate 成功完成时起算，不再从可能耗时的批次开始时起算，`0.15 s` stale gate 未放宽。恢复链现对 `trajectory_stop` 失败禁止继续受控回程；回程抛异常或返回失败后重新检查状态，stale/通信/控制器错误等 critical status 才尝试 protective disable，健康状态仍保持 enabled hold 要求 operator 恢复；disable 自身无法验证时记录 `disable_failure`，不谎报成功。TDD 先得到三项现场回归失败，修复后聚焦测试 `74 passed`，后续恢复分支测试 `8 passed`；最终验证为 layering `18 passed`、全量 `703 passed, 15 skipped`、要求的 compileall 与 `git diff --check` 均通过。真实夹取是否恢复仍须 operator 另行授权后受保护复测，当前不得把软件测试计作硬件验收。

2026-09-04：operator 指出夹爪电机具备零位记录能力，并报告网页遥操作期间无论实际开合，界面反馈始终为零；此前将异常直接归因于“掉电不保存零位”证据不足，已撤回该断言。operator 随后确认夹爪已机械闭合并授权重新设零。复用其当前运行的 `rebotarm_app.launch.py`，只读预检为 arm `enabled=false/IDLE/control_loop_active=false`、六轴 status 全 0、无 error，gripper status0，且 `/rebotarm/gripper/cmd` publisher count为0；调用 `/rebotarm/set_zero` `{joint_name: gripper}` 返回 `success=True/set_zero complete`。校零后5/5位置样本均为 `3.4332275390624997e-06 m`（约0.0034mm），arm状态不变，无六轴trajectory或夹爪开合命令。源码追踪确认 dashboard 不创建 `SetZero` client，只订阅并转存 `/rebotarm/gripper/state`；项目源码中硬件写零仅位于 `HardwareManager.set_zero("gripper") -> motor.set_zero_position()`，visual launch/runner 与网页set-gripper路径不会自动写零。因此当前没有证据证明网页或视觉程序覆盖零位；若实际开合而topic仍为0，故障位于dashboard上游的 controller/motorbridge/电机feedback链。由于静态闭合后的零值本身无法验证动态反馈，本轮不宣称问题修复；下一步须取得单独授权，以一次小行程开合同时记录 action结果、`/rebotarm/gripper/state`与底层raw state，定位反馈在哪一层停止变化。证据：`Agent/evidence/P6/2026-09-04-gripper-zero-reset-and-feedback-audit.md`。

2026-09-04：反馈 freshness 修复 Task 4 完成纯软件实现并通过独立审查，未安装 patched MotorBridge、未启动 ROS/controller、未访问串口或硬件。所有 connected consumer 改为只读 controller-owned verified sample；MotorBridge `get_state_with_sequence()` 仅保留在采集路径，两个 `arm.get_state()` 仅用于明确 disconnected fallback。重力补偿以同一原子六轴 sample tuple 构造 q/qd，并在任何动力学计算或 `arm.mit()` 前拒绝 feedback error/stale、非法值、越界和非 enabled status。硬件 tick 顺序为 feedback scheduler -> arm health gate -> arm callback -> gripper tick；通信/stale 属 critical condition 时，唯一 writer 先终止夹爪 progression 并尽力发 neutral，再停止 writer 与 trajectory producer ownership，按 controller identity 去重请求 protective disable。未验证失能时保持 `enabled=true/lifecycle=DISABLING`，neutral/disable 失败写入 error codes，不谎报安全状态。Task 4 相关 P0 + gripper action 软件测试最终为 96 passed；真实硬件反馈是否恢复仍须 Task 5 安装依赖及另行授权的受控验收。

2026-09-05：反馈 freshness 修复 Task 5 完成软件构建与用户级运行时安装，未启动 ROS/controller、未实例化 MotorBridge Controller、未打开 `/dev/ttyACM0`、未 enable/disable/zero 或发送任何动作。根因已收口为：上游 MotorBridge `0.4.6` Python/ABI 只返回可重复读取的缓存状态，无新接收帧 identity；项目旧采集路径因此可把旧缓存重新打时间戳为 fresh。修复固定上游 `v0.4.6@38b8a5681887514b301dbcab96e01a473cbd7173`，应用 SHA256 `e04d1dacce15eaee4713517f4ea1a4b20ed3299be124f0e7e8df0e2bf5de23a5` 的 source-only patch，新契约为 `Motor.get_state_with_sequence() -> (state, uint64 sequence)` 且 `abi_capabilities().features.feedback_sequence=true`；只有 sequence 前进才能刷新 controller verified cache。`--build-only` 在临时 venv 通过；最终安装 wheel 为 `motorbridge-0.4.6+rebotarm.1-cp312-cp312-linux_x86_64.whl`、SHA256 `659d0957d1f3837cefbbeb73b4d0bfb6846a47dc50ad313c21107bedb2fbfe4a`；`--install-user` 已用其替换用户级 vanilla `0.4.6`，`--check-installed` 回读 version `0.4.6+rebotarm.1`/capability true/method callable。Task 5 软件验证：layering `18 passed`；显式 worktree source/ROS overlay 环境下全量 `756 passed, 15 skipped`；要求的 package/controller compileall 和 `git diff --check` 通过。首次裸环境全量收集因未 source workspace install 而缺少 `rebotarm_msgs`，补齐环境并强制 worktree source 后通过，不是代码回归。最终分支审计又发现 source patch 作为嵌套 unified diff 时的9个空白 context prefix 会使外层 `git diff --check` 报 trailing whitespace；新增 artifact gate 先 RED，清理后 GREEN，补丁正反向 apply 检查与 setup `22 passed`均通过，语义未改。这些自动化证据不构成真机验收；真实反馈是否不再卡零/陈旧、sequence 是否随实际开合前进，仍须 operator 另行明确授权的受控小行程硬件验证。

## 2026-09-06 闭合反馈容差按用户批准改为等效1mm

用户在讨论+0.015rad反馈后明确要求约1mm容差。按既有5rad对应90mm计算，主项目_G_CLOSED_FEEDBACK_TOL_RAD和Star Arm hardware_specs的对应常量均为0.001*5/0.09，即0.05555555555555556rad（约3.18度）。只扩大闭合端反馈接受范围，保留raw、目标<=0、张开端现有规则及严格显式校零验收；不改零点或控制增益。Star Arm期间已有张开端范围对齐改动，予以保留而未覆盖。补充实测+0.01583099365、1mm边界和边界外测试，主项目822 passed/8 skipped，Star .venv443 passed，compileall通过；controller部署到install_coupling_audit。无真实串口访问或运动，未重启用户进程，运行实例需下次启动加载。0.01rad旧记录为历史，本节为当前值。P0-P6关闭范围不变，不把容差放宽视为设备偏差根因或硬件验收闭环。

## 2026-09-06 闭合反馈容差与底层安全修复完成

用户批准闭合反馈容差0.01rad，已同步本工作树controller和Star Arm跟随工程；保留raw和严格target<=0，张开端门及约0.000763rad的显式校零验收不变。MotorBridge新增帧格式/长度/CAN与载荷ID校验，移除默认disabled_hint，显式置零必须新请求status0且生命周期串行化，不自动失能。新版本0.4.6+rebotarm.2已可复现构建；待用户原跟随PID365732退出且串口空闲后，分别安装用户级与Star .venv，两处运行契约检查通过。主项目安装后820 passed/8 skipped（含12项真实ABI虚拟串口测试），Star .venv414 passed，Rust32 passed；controller重建至install_coupling_audit。未操作真机或零位，未提交，不把软件测试视作硬件验收，历史+2pi原因仍未闭环。P0-P6范围不变。产物SHA、部署和后续边界见Agent/evidence/maintenance/2026-09-06-gripper-tolerance-and-motorbridge-safety-fix.md。

## 2026-09-06 夹爪底层与05:25联合跟随中止诊断

operator补充校零后测试、故障失能、手动闭合、重新使能，明确不是24V断电。新找到combined-follow-LX5xCa日志：目标到0时实际仍约1.91rad/s，最终新反馈+0.00362396/status1后立即CRITICAL_STOP并全轴失能。Star Arm的gripper_contact实际反馈复用严格target<=0验证，live_follow将observe异常一概归critical；真实函数离线复现同一报错。05:26只读strace证明+0.00743866来自合法CAN0x17/DLC8反馈，RID80/81也约+0.007，CTRL_MODE/PMAX/VMAX/TMAX正确，两个offset与05:21校准后完全相同；不支持零位被覆盖或掉电丢零。底层另复现上游MotorBridge的宽松报文路由/DLC不校验污染反馈，以及新句柄disabled_hint=true使status1仍可显式置零两个独立缺口；虚拟串口验证，不是已证实的本次根因。Rust现有16tests通过但不覆盖它们。本轮未改控制代码或库，真机仅查询；详细证据和修复建议见Agent/evidence/maintenance/2026-09-06-gripper-lowlevel-and-follow-stop-audit.md。历史+2pi和具体机械闭合重复性因素尚未闭环，P0-P6范围不变。

## 2026-09-06 05:21 再次显式校准夹爪闭合零位

用户中断越界排查，确认当前闭合并要求先校准。串口空闲、七电机status0和静止检查通过后，仅CAN7调用一次置零；校准前五帧为+0.21610546112060547rad，校准后20帧及软件重连5帧全部为-0.00019073486328125rad，0.5秒内连续三个新帧验收通过，六轴最大变化0rad，串口已释放。未enable/disable/运动/切模式/保存参数。置零前offset仍与04:14基线完全相同，不能认定零点寄存器曾被覆盖；本次授权置零后新基线为angle_offset=-1.469793677330017、motor_offset=4.820605754852295，CTRL_MODE仍1、PMAX仍12.5。05:14与05:21之间无连续反馈/机械操作记录，不用两次不同位置断言漂移。当前校准成功不等于越界根因修复；P0-P6范围不变。证据：Agent/evidence/maintenance/2026-09-06-052151-gripper-zero-calibration.md。

## 2026-09-06 夹爪闭合零位重新校准

用户确认夹爪机械闭合并明确授权校准。校准前 5 个新鲜原始反馈均为 6.283092498779297 rad，六轴及夹爪均 status0；串口无人占用。仅向 gripper CAN ID 7 发送一次 set_zero_position，抓包核对仅 1 个置零帧，其余发送均为反馈查询。校准后 20/20、关闭串口重建连接后 5/5 均为 -0.00019073486328125 rad（量化零附近，换算宽度 0.003433 mm），六轴反馈最大变化 0 rad，所有状态保持 0，串口最终释放。未发送 enable、disable、运动、模式或参数保存指令；未改产品源码。该结果验证本次闭合零点及软件重连保持，不证明断电保持，也未确定此前约 +2pi 偏移根因。后续若再次偏移应先抓取原始反馈和电源操作记录，不自动补偿一圈或重置零位。P0-P6 范围关闭状态保持。证据：`Agent/evidence/maintenance/2026-09-06-gripper-zero-calibration.json` 与同名 `.trace`。

## 2026-09-06 反复夹爪零位问题追溯

用户明确纠正：校准后 -0.07229 rad 是其操作后位置，不是漂移。历史假零/反馈卡住与本次原始新回包 +2pi 应分开：旧缓存重打fresh、历史software-origin解释是已知软件问题；当前抓包为设备上报49238->6.2830925rad，接近一圈误差不足半LSB，优先核验电机重上电后的零点/圈数基准，但缺少事件时电源和寄存器对照，尚未证实触发根因。已实读七电机PMAX=12.5，关节模式2/夹爪模式1符合代码；夹爪固件版本寄存器与Joint5/6相同，不支持直接归因模式/量程不匹配。当前仍有raw正值clip成0mm和标准set_zero缺乏post-zero新帧验收两项软件缺口，本轮只诊断未修复。9月5日22:46主机启动、22:59tty枚举不等于24V断电证据；MotorBridge文档提及掉电零点不保持，但不能直接推定本机固件持久语义。详细证据及下一步判因条件：`Agent/evidence/maintenance/2026-09-06-gripper-zero-root-cause-audit.md`。本轮无写零/模式/参数或运动；P0-P6范围不变。

## 2026-09-06 夹爪坐标和置零验收软件修复

上述两项确定的软件缺口已按用户要求修复：保留新鲜raw，映射有效域为[-5,0]rad加两个反馈刻度的端点容差，明显越界上报NaN/255及具体错误并阻止夹爪动作；运行中异常通过唯一writer中性退出夹爪，不直接失能健康六轴。显式置零仅写一次，随后要求0.5秒内连续三个新鲜status0且近零的样本；失败在当前实例中保持未知/阻塞，直到再次显式置零验收成功。网页JSON/SSE将NaN变为null，并防止前端将null转成0或通过旧目标下发夹爪命令。全量781 passed/8 skipped，layering20，compileall/diff通过，controller/dashboard两包在install_decoupling构建并核实模块解析为当前worktree。未改主安装空间、未打开串口/实例化硬件、未启动真机或重新校准，未提交。P0-P6范围不变，电机此前+2pi的触发原因仍待独立验证。证据和下一次启动overlay说明：`Agent/evidence/maintenance/2026-09-06-gripper-coordinate-and-zero-verification-fix.md`。

## 2026-09-06 按序解除剩余耦合

第1项资源归属已迁移：唯一URDF及12个原始网格归moveit_config，bringup不再安装重复URDF/mesh；组合参数从interactive_control迁入bringup，默认launch全部更新，旧Python兼容入口仍保留。MoveIt demo直接使用teleop桥，manifest消除bringup/compat反向边，当前内部manifest依赖图无环。正式MuJoCo节点间接使用motor_control，已纠正此前“仅离线使用”的判断：仿真参考固件参数冻结在simulation配置，数值不变，不再偷读bringup硬件文件；新加载器支持source/ament安装资源。第1项focused189、全量785 passed/8 skipped、layering20及resource4通过，五包install_decoupling构建通过；安装模型20个mesh引用可读、编译/diff检查通过。尚待第2项Dashboard编排、第3项录制统一、第4项视觉运动归属、第5项依赖引用测试；全程软件维护，无硬件操作，P0-P6关闭范围不变。

第2至5项随后完成：Dashboard示教生命周期迁入teach的独立TeachReplayWorkflow，净减少615行且保留Web权限门；controller内置录制删除，标准launch用同一TeachRecorderNode，反馈批次identity/稳定header和录制同批新鲜度验证防止重复采样，发布锁防reentrant timer乱序。visual_ready节点与参数迁入motion，旧vision入口兼容。补齐manifest及AST/资源URI/依赖环检查。最终804 passed/8 skipped，layering+resources26，编译/diff通过；新非symlink install_coupling_audit共13包构建通过，controller末次重建通过。从/tmp校验新安装模块/资源并用已有MuJoCo venv成功加载nq15/nu8模型；隔离ROS domain153完成空闲节点、合成录制去重、HTTP GET/禁用POST400验证后全部退出。无串口或真实动作，未提交。旧安装空间不代表本轮结果，人工后续启动改用install_coupling_audit；实体录制吞吐/运行切换仍未验收，夹爪设备侧+2pi根因仍未闭环，P0-P6范围不变。证据：Agent/evidence/maintenance/2026-09-06-remaining-coupling-refactor.md；迁移说明：docs/coupling_migration.md。
