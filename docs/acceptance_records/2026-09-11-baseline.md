# 2026-09-11 架构维护基线验证

结论：**本地边界检查和选定离线测试通过，严格部署检查未通过；不构成实机或整阶段验收。**

## 1. 验证环境和范围

- 日期：2026-09-11，Asia/Shanghai；最终回归结果在当日上午记录。
- 工作区：`/home/huangbin/robotarm_ros2`。
- Git HEAD：`0dd373ad05a13b944cfb2ba368206c1badf5950a`，**dirty**；包含接手时的文档/测试删除、RViz/控制器等未提交修改，以及本次架构维护和语音移除。HEAD 本身不能复现本次工作区。
- Python：3.12.3；ROS：本机 Jazzy；通过 `tools/source_ubuntu_env.bash` 使用 `.venv-ros`、`install-venv` 与可选本地 overlay。
- MuJoCo：3.13.0；使用源码 `src/rebotarm_simulation/models/rebotarm/scene.xml`。
- 仓库扫描：12 个 ROS 包、293 个 Python 源文件。语音包不在其中。
- 执行者：Codex 本地自动检查；维护者/实机操作者尚未复核。本次没有连接硬件或发送 ROS 运动请求。

下列输出是本次工具执行结果的摘要，未留存完整工作区补丁、全部第三方环境快照或实机原始数据，因此用于当前本地维护基线，不作为可发布的版本验收凭据。后续阶段验收应按[模板](TEMPLATE.md)留存完整证据。

## 2. 包边界与部署检查

```bash
python3 tools/check_architecture.py
python3 tools/check_architecture.py --strict-deployment
colcon list --base-paths src --topological-order --names-only
```

| 项目 | 结果 | 退出码 |
| --- | --- | --- |
| 普通架构门禁 | `boundaries=PASS; deployment=NOT READY`；未登记边界错误 0 | 0 |
| 严格部署门禁 | 相同报告；已登记资源/SDK 依赖债务阻止通过 | 1 |
| colcon 包清单/排序 | 发现 12 包，已声明包依赖可拓扑排序，无语音包 | 0 |

严格检查共报告 6 条债务说明：ARCH-01 的 4 条未声明资源边、一个资源依赖环示例、ARCH-03 的旧预览 SDK kinematics 导入。不是六个独立完成阶段。ARCH-02 配置迁移和其他手工验收项另见[架构债务表](../project_architecture_zh.md)。

普通门禁只防止未登记的回归，不能用其退出码宣称部署环已经消除。CI 配置已加入仓库，本次只验证本地命令，未触发或核实远端 CI。

## 3. 回归测试

```bash
python3 -m unittest discover -s tests -p 'test_architecture_guard.py' -v
source tools/source_ubuntu_env.bash
python -m pytest -q tests/test_architecture_guard.py tests/test_rviz_execute_workflow.py
```

结果：标准库独立运行 12 项检查器测试通过；最终 pytest **27 passed in 0.44s**，退出码 0。

| 范围 | 数量 | 证明范围 |
| --- | --- | --- |
| 架构检查器 | 12 | 正常/BOM 源码、反向依赖与环、runtime 声明、静态/动态 SDK 导入、未知包、资源引用/mesh、例外限制/过期、兼容层新增实现 |
| RViz 执行假硬件回归 | 15 | 接受目标不使能、执行时按需使能、非法/起点偏差拒绝、默认策略、故障、取消、enable 失败、仲裁/抢占、反馈重验及两个 launch 包装器 |

测试使用当前源码：控制器与仿真 Python 包的 `__file__.resolve()` 均指向工作区 `src/`。这不是整个历史测试套件的通过数量，也未覆盖独立主从工具的测试。

另用 Python 加载 MoveIt `demo.launch.py` 并调用 `generate_launch_description()`，检查只有一个 `GripperVisualJointStateNode` 且来自 `rebotarm_teleop`，同时确认旧兼容入口的 `main` 与正式包的 `main` 为同一对象，结果通过。此检查只构造 launch，未启动任何节点。配置加载器输出 URDF/SRDF 无法自动推断的警告后使用显式 `config/rebotarm.urdf` / `config/rebotarm.srdf`，不据此宣布实际规划已验收。

## 4. MuJoCo 无头验收

```bash
source tools/source_ubuntu_env.bash
PYTHONPATH="$PWD/src/rebotarm_simulation:$PYTHONPATH" python -m rebotarm_simulation.mujoco_acceptance --skip-renderer
```

退出码 0，总体 `ok=true`，四个子步骤均 `ok=true`：

| 子步骤 | 输出字段摘录 | 本次结论 |
| --- | --- | --- |
| health | 8 joints / 8 actuators；`physics_step_finite=true`；仿真时间 `0.002 s` | 模型加载并推进一步 |
| headless_reach_batch | 1 episode / 5 steps / seed 0；`success_count=0` | 环境可运行；不是 Reach 任务成功 |
| headless_pick_environment | 1 episode / 5 steps / seed 0；`action_magnitude=0.0`；`success_count=0` | 环境可运行、此轮安全采样无违例；不是抓取成功 |
| cube_contact | 600 contact steps；`finger_contact_steps=263` ≥100；`max_cube_contact_force_n=2.2549491661786965` ≤20；`max_cube_step_jump_m=0.0005028136238027174` ≤0.005；`min_cube_z_m=0.01932348270208652` ≥0.018 | 指定场景接触回归通过 |

`renderer_available=false`、`renderer_error="renderer check skipped"`。本轮未检查 GUI/Renderer、ROS 接口或 MoveIt 执行。

## 5. 合成 Real2Sim mirror 检查

```bash
source tools/source_ubuntu_env.bash
PYTHONPATH="$PWD/src/rebotarm_simulation:$PYTHONPATH" python -m rebotarm_simulation.real2sim_acceptance --mode mirror
```

退出码 0。输出摘要：

```json
{
  "ok": true,
  "hardware_connected": false,
  "source": "synthetic_joint_state",
  "mode": "mirror",
  "steps": 200,
  "rate_hz": 100.0,
  "max_tracking_error_rad": 0.0,
  "tracking_error_limit_rad": 1e-8,
  "max_gripper_error_m": 0.0001733283403148808,
  "simulation_time": 2.0000000000000013,
  "finite_state": true
}
```

这证明合成关节状态的镜像映射。夹爪误差字段是观测值，不参与此工具的总体通过判据；没有真实设备、总线延迟、丢帧或 physics 模式的结论。

## 6. 语音包退出与文档检查

- 删除 `src/rebotarm_voice_control/` 下 40 个受版本管理的文件；旧实现可从上述 Git 历史检索。
- 从文档入口、当前能力、架构规则中移除语音，仅保留退役/未来重做决策。
- 旧 `build`、`install`、`build-venv`、`install-venv` 中该包及源码 Python 缓存可恢复地移至 `.codex_tmp/removed-voice.jQXHhp/`，没有清理其他包。
- source 当前工作区后，`ament_index_python.packages.get_packages_with_prefixes()` 中 `rebotarm_voice_control` 不存在。
- 共享 `MoveRelative` / `ExecuteGrasp` 类型未删除；不把它们计为已实现的任务服务器。
- `git diff --check` 退出 0。

## 7. 关键输入指纹

以下 SHA-256 用于识别本次实际验证的核心文件，不能替代完整版本和配置快照：

```text
b860d27025770958f90d6880451cadfdfc4b4febb1957af2b42228c5d5fca053  tools/check_architecture.py
1821049bdb99f915d42c7a58775728f2756cb78d39986af626436fe56bea089e  tools/architecture_rules.json
1102b4c446f06c348b7ee6bc76ef50e0761d91765782901bb75b8d07c52b521e  tests/test_architecture_guard.py
47b604e33dc8b6d8419fa59d92864e52025116f3364c18629350896e5b6030fd  tests/test_rviz_execute_workflow.py
a307401a9311ce6b9590f6babd3c1acb60037d7ab2c669073b679852e0673a5f  src/rebotarmcontroller/rebotarmcontroller/ros_actions.py
9e0146ea7e9925f2173d5e68155c0ae88f535bc727da79311cc3e219bb854e3d  src/rebotarmcontroller/rebotarmcontroller/hardware_manager.py
e3ef73e5908291f2a724f2c20bb6f485053c7dc223af027e14ee4bfeb96a1b0a  src/rebotarm_simulation/models/rebotarm/scene.xml
de64d2dd8e0fc35eff81fcceed953684aac334590d39655082539d5bc016de99  src/rebotarm_simulation/models/rebotarm/robot.xml
```

## 8. 未通过和未执行项

ENG-02 严格部署门禁仍未通过；完整干净构建/安装、全部控制器边界回归、ROS/MoveIt/GUI 联调、真实反馈/运动/停止/夹爪、示教集成、现场标定和视觉抓取统计未在本次执行。SIM-01 已有局部通过记录，SIM-04 只有合成 mirror 子项，不将整个 S2 标为通过。

当前总阶段保持 **S1 进行中，部分 S2 离线证据已建立**。
